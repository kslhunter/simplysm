# formatEsbuildMessage 적용 — LLM 검증

## 검증 항목

- [x] client.worker.ts: import 추가 확인 — 라인 11에 `import { formatEsbuildMessage }` 추가됨
- [x] client.worker.ts:176 catch 블록: `buildErrors.map(formatEsbuildMessage)` 적용 + 타입 캐스트에 notes 포함
- [x] client.worker.ts:330 watch 에러: `result.errors.map(formatEsbuildMessage)` 적용
- [x] client.worker.ts:334 watch 경고: `result.warnings.map(formatEsbuildMessage)` 적용
- [x] client.worker.ts:346 초기 빌드 에러: `result.errors.map(formatEsbuildMessage)` 적용
- [x] server-build.worker.ts: 라인 7에 import 추가 + 174-175에 적용
- [x] server-esbuild-context.ts: 라인 4에 import 추가 + 102, 105에 적용
- [x] 기존 `.map((e) => e.text)` 패턴이 src/ 내에 없음 (Grep 확인)
