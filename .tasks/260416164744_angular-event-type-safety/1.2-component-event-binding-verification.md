# Feature 1.2 전체 컴포넌트 이벤트 바인딩 정합성 검증

## 참조 자료

- WBS: [wbs.md](./wbs.md)
- 이벤트 directive 정의: `packages/angular/src/core/events/` (sd-resize, sd-intersection, sd-events)
- 커맨드 directive 정의: `packages/angular/src/core/commands/sd-command.ts`
- 참조 구현: `packages/angular/src/data/sheet/sd-sheet.ts` (hostDirectives + SdEvents 패턴)

### 설계 결정

| # | 결정사항 | 선택 | 근거 |
|---|---|---|---|
| D1 | 코드 변경 필요 여부 | 변경 없음 | 타입체크 0 에러, 전수 검증 결과 모든 바인딩 올바름 |

## 검증 결과

### 타입체크

`pnpm typecheck -t angular` 실행 결과: **0 에러, 0 경고**

### host binding 컴포넌트 검증

| 컴포넌트 | 이벤트 | directive | hostDirectives 선언 | 상태 |
|---|---|---|---|---|
| sd-modal | (sdResize) | SdResizeDirective | ✓ outputs: ["sdResize"] | 정상 |
| sd-dock | (sdResize) | SdResizeDirective | ✓ outputs: ["sdResize"] | 정상 |
| sd-echarts | (sdResize) | SdResizeDirective | ✓ outputs: ["sdResize"] | 정상 |
| sd-data-detail | (sdRefreshCommand), (sdSaveCommand) | SdCommandDirective | ✓ outputs: ["sdRefreshCommand", "sdSaveCommand"] | 정상 |
| sd-data-sheet | (sdRefreshCommand), (sdSaveCommand) | SdCommandDirective | ✓ outputs: ["sdRefreshCommand", "sdSaveCommand"] | 정상 |
| sd-sheet | (keydown.capture), (focus.capture), (blur.capture) | SdEvents | ✓ outputs: ["keydown.capture", "focus.capture", "blur.capture"] | 정상 |

### template binding 컴포넌트 검증

| 컴포넌트 | 이벤트 | directive | imports 선언 | 상태 |
|---|---|---|---|---|
| sd-kanban | (sdResize) | SdResizeDirective | ✓ | 정상 |
| sd-sheet | (sdResize) | SdResizeDirective | ✓ | 정상 |
| sd-modal | (sdResize) | SdResizeDirective | ✓ | 정상 |
| sd-dropdown-popup | (sdResize) | SdResizeDirective | ✓ | 정상 |
| sd-collapse | (sdResize) | SdResizeDirective | ✓ | 정상 |

### 특수 케이스

| 컴포넌트 | 이벤트 | 처리 방식 | 상태 |
|---|---|---|---|
| sd-kanban | (document:drop.capture) | SdOptionEventPlugin (document-level, hostDirectives 불필요) | 정상 |
| sd-sheet | (scroll.passive) | SdOptionEventPlugin (파라미터 미사용) | 정상 |

### 불필요한 import

없음 — 모든 이벤트 관련 import가 사용 중

## 요구명세

```gherkin
Feature: 1.2 전체 컴포넌트 이벤트 바인딩 정합성 검증

  Background:
    Given Angular 컴파일러 strict 옵션이 활성화됨 (strictTemplates, typeCheckHostBindings, strictDomEventTypes)
    And Feature 1.1에서 SdResizeEvent.target 타입이 HTMLElement로 변경됨

  Rule: host binding 커스텀 이벤트는 hostDirectives에 선언 필수
    # 근거: Angular typeCheckHostBindings 활성 상태
    Scenario: host binding sdResize 사용 컴포넌트 검증
      Given sd-modal, sd-dock, sd-echarts가 host에서 (sdResize) 바인딩을 사용함
      When hostDirectives에 SdResizeDirective가 선언되어 있는지 확인하면
      Then 3개 컴포넌트 모두 hostDirectives에 SdResizeDirective가 선언되어 있음

    Scenario: host binding sdCommand 사용 컴포넌트 검증
      Given sd-data-detail, sd-data-sheet가 host에서 (sdRefreshCommand), (sdSaveCommand) 바인딩을 사용함
      When hostDirectives에 SdCommandDirective가 선언되어 있는지 확인하면
      Then 2개 컴포넌트 모두 hostDirectives에 SdCommandDirective가 선언되어 있음

    Scenario: host binding .capture 수식어 사용 컴포넌트 검증
      Given sd-sheet가 host에서 (keydown.capture), (focus.capture), (blur.capture) 바인딩을 사용함
      When hostDirectives에 SdEvents가 선언되어 있는지 확인하면
      Then SdEvents hostDirective에 해당 output이 모두 선언되어 있음

  Rule: template binding 커스텀 이벤트는 imports에 선언 필수
    # 근거: Angular strictTemplates 활성 상태
    Scenario: template binding sdResize 사용 컴포넌트 검증
      Given sd-kanban, sd-sheet, sd-modal, sd-dropdown-popup, sd-collapse가 template에서 (sdResize) 바인딩을 사용함
      When imports에 SdResizeDirective가 선언되어 있는지 확인하면
      Then 5개 컴포넌트 모두 imports에 SdResizeDirective가 선언되어 있음

  Rule: 이벤트 핸들러 파라미터 타입은 directive output 타입과 일치해야 함
    # 근거: strictTemplates
    Scenario: 전체 이벤트 핸들러 파라미터 타입 검증
      When pnpm typecheck -t angular를 실행하면
      Then 이벤트 관련 타입 에러 0건

  Rule: document-level 이벤트는 hostDirectives 불필요
    # 근거: WBS 설계 결정
    Scenario: sd-kanban document:drop.capture 검증
      Given sd-kanban이 host에서 (document:drop.capture) 바인딩을 사용함
      When document-level 이벤트이므로 hostDirectives 없이 SdOptionEventPlugin이 처리
      Then 현재 상태가 올바름
```

## 구현계획

### 배경

Feature 1.1에서 `SdResizeEvent.target` 타입을 `HTMLElement`로 변경하고 `as HTMLElement` 캐스팅을 제거했다. 이후 전체 컴포넌트의 이벤트 바인딩 정합성을 검증해야 한다. [근거: WBS Feature 1.2 범위]

### 목표

- angular 패키지 전체 이벤트 바인딩 타입 에러 0건 확인 [근거: Rule "이벤트 핸들러 파라미터 타입 일치"]
- 모든 컴포넌트의 hostDirectives/imports 설정 검증 [근거: Rule "host/template binding directive 선언"]

### 비목표

- SdIntersectionDirective 소비 프로젝트 적용 [근거: WBS 경계]
- SdOptionEventPlugin 구조 변경 [근거: WBS 경계]
- 이벤트 시스템 외 타입 에러 수정 [근거: WBS 경계]

### 설계

검증 전용 Feature — 코드 변경 없음. 타입체크 실행 및 수동 코드 검토로 정합성 확인. [근거: D1 결정]

### Vertical Slicing

#### Slice 1: 타입체크 실행 및 전수 검증

- [x] **구현 내용:** `pnpm typecheck -t angular` 실행, 9개 대상 컴포넌트의 이벤트 바인딩 설정 수동 확인, 불필요한 import 확인
- **Scenarios:** 전체 (검증만 수행, 코드 변경 없음)
