# Feature 1.1 구현 리뷰 — LLM 검증

## 검증 항목

- [x] `SideEffectScssOptions`에 `sideEffectScssDeps` 필드가 optional로 추가됨: `ngtsc-build-core.ts:64` 확인. `sideEffectScssDeps?: Map<string, Set<string>>` — 기존 필드와 동일 패턴
- [x] `writeEmitResults` 내부 갱신 로직이 `compileSideEffectScss`의 패턴과 동일함: `ngtsc-build-core.ts:270-271` vs `ngtsc-build-core.ts:149-150` — 둘 다 `new Set(result.dependencies)`로 저장
- [x] `sideEffectScssDeps` 미제공 시 null guard로 안전하게 스킵: `ngtsc-build-core.ts:270` `if (scss.sideEffectScssDeps != null)` — `== null` 패턴 사용 (코딩 규칙 준수)
- [x] `library-build.worker.ts` `build()` 호출부에 `sideEffectScssDeps` 전달: line 103
- [x] `library-build.worker.ts` `buildWatchEvent()` 호출부에 `sideEffectScssDeps` 전달: line 202
- [x] `cleanup`에서 `sideEffectScssDeps` 초기화: line 61 `sideEffectScssDeps = new Map()` — 기존 코드에서 이미 처리됨
- [x] 기존 테스트 회귀 없음: ngtsc-build-core-write-emit(21), library-build-worker(28), ngtsc-build-core(4) — 53개 전부 통과
- [x] typecheck/lint 에러 없음: `pnpm check -t sd-cli` — 0 에러, 0 경고
