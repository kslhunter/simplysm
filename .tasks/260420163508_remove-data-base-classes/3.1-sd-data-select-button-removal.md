# Feature 3.1 SdDataSelectButton 추상화 제거 및 SdSharedDataSelectButton 재구현

## 참조 자료

- WBS: [wbs.md](./wbs.md) — Epic 3 / Feature 3.1 (158~193행)
- 의존 Feature (모두 완료):
  - 1.1 완료: [1.1-sd-data-sheet-removal.md](./1.1-sd-data-sheet-removal.md) — Slice 4분할·tests 전량 삭제 정책(D2)·레시피 신설 패턴
  - 4.1 완료: [4.1-sd-base-container-removal.md](./4.1-sd-base-container-removal.md) — page/modal 분기 패턴
  - 5.1 완료: [5.1-sd-sheet-cumulative-selection.md](./5.1-sd-sheet-cumulative-selection.md) — `cumulativeSelection` input
- 기존 레시피 스타일 참조:
  - `packages/angular/docs/recipes/page-modal-container.md` (10349 bytes) — Overview/언제/완성예제/분해/주의사항 스타일
  - `packages/angular/docs/recipes/crud-list.md` (35663 bytes) — 변형 스니펫·관용 규칙 박제 패턴
- 삭제 대상 src 2개:
  - `packages/angular/src/data/data-select-button/sd-data-select-button.base.ts:1` (105줄) — `SdDataSelectButtonBase<TItem, TKey, TMode>` 추상 클래스. value→load effect, doShowModal, doInitialValue, isNoValue computed
  - `packages/angular/src/data/data-select-button/sd-data-select-button.ts:1` (106줄) — `SdDataSelectButton` 프레젠테이션 컴포넌트. `injectParent<SdDataSelectButtonBase<any, any>>()` (`:84`) 역참조
- 재구현 대상 src 1개:
  - `packages/angular/src/data/shared-data/sd-shared-data-select-button.ts:1` (62줄) — 현재 `extends SdDataSelectButtonBase<TItem, string|number, TMode>` (`:50`). `<sd-modal-select-button>` 컴포지션 기반 독립 컴포넌트로 재작성
- `packages/angular/src/index.ts` export 제거 대상:
  - `:247` `export { SdDataSelectButton } from "./data/data-select-button/sd-data-select-button";`
  - `:248` `export { SdDataSelectButtonBase } from "./data/data-select-button/sd-data-select-button.base";`
- 삭제 대상 tests 2개 (`tests/data/data-select-button/` 디렉토리 전체):
  - `sd-data-select-button-test.fixture.ts` (37줄) — `DSBTestHost extends SdDataSelectButtonBase` 호스트
  - `data-select-button.spec.ts` (460줄) — 5개 describe(자동 로드/모달 선택/초기화/isNoValue/required validation/host attribute), 모두 Base 동작 검증
- 재작성 대상 tests 2개:
  - `tests/data/shared-data/sd-shared-data-select-button-test.fixture.ts` (44줄) — 재구현된 컴포넌트 기반으로 재작성
  - `tests/data/shared-data/shared-data-select-button.spec.ts` (108줄) — `extends` 기반 검증 → 재구현된 표준 컴포넌트 검증으로 전환
- 삭제 대상 docs:
  - `packages/angular/docs/features-data-select-button.md` (481줄)
- 정리 대상 docs:
  - `packages/angular/docs/features.md:50-67` Data View Abstractions 섹션 (선택 버튼 행 + 설명) + `:110-120` SdSharedDataSelectButton 섹션 (재구현된 컴포넌트 설명 갱신)
  - `packages/angular/docs/ui-data.md` — `SdDataSelectButton`/`SdSharedDataSelectButton` 언급 검색 결과 0건 확인(Grep, 2026-04-20). 변경 없음
  - `packages/angular/README.md:165` `SdDataSelectButtonBase` 행 + `:166` `SdDataSelectButton` 행 제거. `:168` `SdSharedDataSelectButton` 행은 유지하되 features-data-select-button.md 링크 무효화 영향 확인
- 신설 대상 docs:
  - `packages/angular/docs/recipes/data-select-button.md` — 사용 패턴 3종(`<sd-modal-select-button>` 직접 / `<sd-shared-data-select-button>` 활용 / 사용자 정의 select-button) + 주의사항
- 유지 대상 (이 Feature에서 건드리지 않음):
  - `packages/angular/src/controls/button/sd-modal-select-button.ts:148-218` (재구현의 컴포지션 기반)
  - `packages/angular/src/core/shared-data/sd-shared-data.provider.ts` (`SharedDataBase` 타입)
  - `packages/angular/src/core/select-modal-output-result.ts` (`SelectModalOutputResult<T>`)
  - `packages/angular/src/data/shared-data/sd-shared-data-select.ts`, `sd-shared-data-select-list.ts` (자매 컴포넌트, 상속 의존 없음)
  - `tests/controls/button/sd-modal-select-button*` (자체 spec 유지)
  - `tests/data/shared-data/sd-shared-data-select*` 자매 fixture/spec/verify (Feature 3.1 범위 외)
- 모노레포 외 사용처 grep 결과 (2026-04-20): `Grep "SdDataSelectButton|SdSharedDataSelectButton" packages/angular` → 자체 정의/테스트/문서 8개 파일만, 외부 src 참조 0

## 기준 코드

### 유사 기능 1: SdModalSelectButton (재구현 컴포지션 기반)

- 컴포넌트: `packages/angular/src/controls/button/sd-modal-select-button.ts:148` — `SdModalSelectButton<T, K, M>` 클래스
- inputs/model: `:155-167`
  - `modal = input.required<SdSelectModalInfo<SdSelectModal<T>>>()` (`:155`)
  - `value = model<SelectModeValue<K>[M]>()` (`:157`)
  - `selectedItems = model<T[]>([])` (`:158`)
  - `disabled`/`required`/`inset`/`size`/`selectMode`/`modalOptions` (`:160-165`)
  - `searchIcon = input(tablerSearch)` (`:167`)
- 핵심 메서드:
  - `onSearchClick(event)` (`:193-216`) — 모달 호출 + `selectedItemKeys: (selectMode==="multi" ? value : [value]).filterExists()` 전달 + 결과 반영
  - `onEraseClick()` (`:188-191`) — value/selectedItems 리셋
  - `setupInvalid` (`:179-185`) — required 시 "선택된 항목이 없습니다." 메시지
- 템플릿 (`:49-65`) — `<div class="_content"><ng-content /></div>` + 검색/erase 버튼

### 유사 기능 2: SdSharedDataSelect (자매 컴포넌트)

- 컴포넌트: `packages/angular/src/data/shared-data/sd-shared-data-select.ts:40` — `SdSharedDataSelect<TItem, TMode, TModal>` 클래스
- 컴포지션 패턴: `<sd-select>` 컴포지션 + 외부 input forward (`:54-69`) + `<sd-select-button>` 추가 슬롯 + `<ng-template #headerTpl>` 검색
- generic 시그니처 — `<TItem extends SharedDataBase<string|number>, TMode extends keyof SelectModeValue<...>, TModal extends SdSelectModal<any>>` (3개 generic, SdSharedDataSelectButton과 동일 패턴)
- inputs forward: `value` (`:56-57`)/`disabled`/`required`/`inset`/`inline`/`size`/`selectMode`/`contentClass` 등 직접 선언 후 `[value]="..."` 형태로 컴포지션 전달

### 유사 기능 3: SdModalSelectButton 양방향 바인딩 패턴

- 테스트 fixture: `packages/angular/tests/controls/button/sd-modal-select-button-test.fixture.ts:46` — `[(selectedItems)]="selectedItems"` 형태로 `selectedItems = signal<TestModalItem[]>([])` (`:55`) WritableSignal에 양방향 바인딩 가능 확인. wrap 컴포넌트 내부 signal에 양방향 바인딩 동일 적용 가능

### 유사 기능 4: 1.1·4.1 레시피 스타일

- 4.1 레시피: `packages/angular/docs/recipes/page-modal-container.md:1` — Overview/언제/완성예제/분해/뷰분기/타이틀/주의사항 (7섹션)
- 1.1 레시피: `packages/angular/docs/recipes/crud-list.md:1` — Overview/언제/완성예제/분해/변형 5종/뷰분기/주의사항/관용 규칙 (8섹션)
- 1.1 plan D5: `injectViewTypeSignal()` 신 시그니처 전제 + 경고 헤더 박스 — 레시피 첫 줄에 `> **선행 조건 주의**: ...` 박스 (`page-modal-container.md:5`)

### 프로젝트 컨벤션

- 컴포넌트 구조: `@Component({ selector, changeDetection: OnPush, encapsulation: None, standalone: true, imports: [...], template: "..." })` [근거: `packages/angular/CLAUDE.md`의 "Component Structure" 섹션]
- Signal input: `input()`, `input.required<T>()`, `input(false, { transform: booleanAttribute })`; two-way: `model()` [근거: 동일]
- 컴포넌트 명칭/파일: `Sd{Name}` / `sd-{name}.ts` / selector `sd-{kebab-case}` [근거: CLAUDE.md "Naming Conventions"]
- 디렉토리: 컨텐츠 분류별 (`src/data/shared-data/`) [근거: 기존 SdSharedDataSelectButton 위치 유지]
- `import type` 필수, `.js` 확장자 금지 [근거: CLAUDE.md]
- `===`/`!==` 기본, `== null`만 예외 [근거: CLAUDE.md]
- 아이콘: `@ng-icons/tabler-icons` + `<ng-icon [svg]="tablerXxx" />` [근거: `sd-modal-select-button.ts:23-24, 61`]
- 테스트: Vitest + Angular TestBed, `sd-{feature}-test.fixture.ts` + `{feature}.spec.ts` [근거: CLAUDE.md "Testing"]

### 설계 결정

| #  | 결정사항 | 선택 | 근거 |
|----|---------|------|------|
| D1 | SdSharedDataSelectButton 재구현 패턴 | **A: SdModalSelectButton wrap (컴포지션)** | 사용자 결정(2026-04-20). wbs 168행 "SdModalSelectButton 기반 독립 컴포넌트로 재작성" 명시. 모달/erase/invalid 로직 재사용으로 코드 중복 0 |
| D2 | selectedItems 외부 노출 여부 | **B: signal (외부 미노출, 기존 호환)** | 사용자 결정(2026-04-20). 기존 SdDataSelectButtonBase의 `selectedItems = signal<TItem[]>([])` (`base.ts:52`) 동작 100% 호환. wrap 내부 WritableSignal에 SdModalSelectButton의 model을 양방향 바인딩 (`tests/controls/button/sd-modal-select-button-test.fixture.ts:46,55`로 패턴 검증) |
| D3 | tests/data/data-select-button/ 처리 방식 | **A: 전량 삭제** | 사용자 결정(2026-04-20). Feature 1.1 D2와 동일 정책. fixture+spec 모두 SdDataSelectButtonBase 동작 검증으로 이관 가치 0. SdModalSelectButton 동작은 `tests/controls/button/sd-modal-select-button-test.fixture.ts` + 자체 spec에 이미 존재 |
| D4 | data-select-button.md 레시피 구조 | **A: 단순 가이드 (패턴별 3종)** | 사용자 결정(2026-04-20). 사용 패턴 3종(직접 모달/공유 데이터/사용자 정의)이 본질적으로 분리되어 있어 단일 완성 예제(crud-list.md 스타일)보다 패턴별 분리가 자연스러움. ~250줄 분량 |
| D5 | Slice 분할 | **4개 Slice** (레시피 신설 → src+tests+index.ts → docs → 검증) | Feature 1.1 D14와 동일 패턴. 작업 경계가 자연스럽게 4덩어리 |
| D6 | 재구현 클래스 generic 시그니처 | **기존 동일** `<TItem extends SharedDataBase<string\|number>, TMode extends keyof SelectModeValue<string\|number>, TModal extends SdSelectModal<any>>` | 외부 사용처 호환성. 자매 SdSharedDataSelect와 동일 패턴(`sd-shared-data-select.ts:40` 3-generic) |
| D7 | 재구현 effect 동작 | **value/items/selectMode 의존 effect** — value 변경 시 items.filter(by __valueKey)로 selectedItems set | 기존 SdDataSelectButtonBase의 effect (`base.ts:57-74`)와 동일 동작 패턴. items가 source of truth(메모리 검색)이므로 비동기 load 불필요. 단 items 자체 변경 시에도 재계산 |
| D8 | 재구현 itemTplRef API | **`contentChild.required<...>(SdItemOfTemplate, { read: TemplateRef })`** 유지 | 기존 `sd-shared-data-select-button.ts:54` 동일. 외부 사용처 호환성 |
| D9 | features-data-select-button.md 링크가 살아있는 README:168 SdSharedDataSelectButton 행 | **`SdSharedDataSelectButton` 행은 유지하되 링크는 features.md 또는 recipes/data-select-button.md로 교체** | 컴포넌트 자체는 유지(재구현)되고 features-data-select-button.md만 삭제됨. 링크 끊김 방지 |
| D10 | 레시피 헤더 경고 | **Feature 6.1 완료 후의 `injectViewTypeSignal()` 신 시그니처 경고 헤더 미수록** | 본 레시피는 `injectViewTypeSignal` 미사용(SdSharedDataSelectButton 동작에 무관). page-modal-container.md/crud-list.md와 다른 정책 |

## 요구명세

```gherkin
Feature: 3.1 SdDataSelectButton 추상화 제거 및 SdSharedDataSelectButton 재구현

  Background:
    Given @simplysm/angular 패키지
    And SdDataSelectButton·SdDataSelectButtonBase 2개 소스 파일이 삭제 대상
    And SdSharedDataSelectButton은 SdDataSelectButtonBase 상속을 끊고 SdModalSelectButton 컴포지션으로 재구현
    And Feature 1.1·4.1·5.1은 모두 완료됨

  Rule: 삭제 후 모노레포 내부에 SdDataSelectButton·SdDataSelectButtonBase 참조가 남지 않는다
    # 근거: wbs 168-169행 + D1

    Scenario: src/index.ts에서 SdDataSelectButton·SdDataSelectButtonBase export 2종 제거
      Given packages/angular/src/index.ts:247-248 범위에 export 2종이 존재한다
      When 해당 export 라인을 모두 제거한다
      Then pnpm typecheck -t angular가 통과한다
      And 모노레포 내부 import 검색 결과가 0건이다

    Scenario: src/data/data-select-button/ 디렉토리 전체가 삭제된다
      Given data-select-button 디렉토리에 파일 2개(base.ts + .ts)가 존재한다
      When 디렉토리 전체를 삭제한다
      Then pnpm check -t angular가 통과한다

    Scenario: 모노레포 내 Grep으로 잔여 참조 0건 확인
      When Grep "SdDataSelectButton\|SdDataSelectButtonBase" packages/
      Then 결과가 0건 (재구현 새 파일 제외)

  Rule: SdSharedDataSelectButton 재구현 후 외부 인터페이스 호환을 유지한다
    # 근거: wbs 168행, D2, D6, D8

    Scenario: 외부 사용처 템플릿 무변경 동작
      Given <sd-shared-data-select-button [(value)]="..." [items]="..." [modal]="..." [selectMode]="..."> 형식으로 사용 중인 코드
      When SdSharedDataSelectButton을 재구현한다
      Then 외부 코드 변경 없이 동일하게 동작한다

    Scenario: value+items 변경 시 selectedItems 자동 동기화
      Given items() = [{__valueKey: 1, name: "A"}, {__valueKey: 2, name: "B"}]
      When value.set(1)
      Then 컴포넌트 내부 _selectedItems()가 [{__valueKey:1, name:"A"}]로 set된다
      And itemTplRef로 "A"가 표시된다

    Scenario: items만 변경되어도 selectedItems 재계산
      Given value() = 1
      When items.set([{__valueKey: 1, name: "Aa"}])
      Then _selectedItems()가 [{__valueKey: 1, name: "Aa"}]로 갱신된다

    Scenario: 모달 선택 후 value 갱신
      Given 사용자가 검색 버튼 클릭 → 모달에서 항목 선택
      When SdModalSelectButton이 selectedItemKeys=[2]를 반환
      Then value()가 2가 된다

    Scenario: erase 버튼 클릭 시 value 리셋
      Given value() = 1, required=false
      When erase 버튼 클릭
      Then value()가 undefined가 된다

    Scenario: itemTplRef 외부 템플릿이 selectedItems 각 item에 대해 렌더링
      Given <sd-shared-data-select-button>에 <ng-template [itemOf]="items()" let-item><span>{{ item.name }}</span></ng-template>
      When _selectedItems()에 2개 item이 들어 있을 때
      Then 외부 템플릿이 2회 렌더링되고 사이에 ", " 구분자가 표시된다

  Rule: 재구현 시 injectParent를 사용하지 않는다
    # 근거: wbs 배경, Feature 6.1에서 삭제 예정

    Scenario: 재구현된 컴포넌트의 injectParent 호출 0건
      When Grep "injectParent" packages/angular/src/data/shared-data/sd-shared-data-select-button.ts
      Then 결과가 0건

  Rule: tests/data/data-select-button/ 디렉토리는 전량 삭제된다
    # 근거: D3 사용자 결정(2026-04-20)

    Scenario: fixture·spec 2개 파일이 모두 삭제된다
      Given tests/data/data-select-button/에 fixture 1개 + spec 1개(460줄)
      When 디렉토리 전체를 삭제한다
      Then vitest run tests/data/ 가 통과한다
      And SdModalSelectButton 회귀 검증은 tests/controls/button/sd-modal-select-button*에서 계속 커버된다

  Rule: tests/data/shared-data/shared-data-select-button.spec·fixture는 재구현 컴포넌트 기반으로 재작성된다
    # 근거: wbs 172-173행

    Scenario: fixture에서 SdDataSelectButtonBase import 제거
      Given sd-shared-data-select-button-test.fixture.ts (현재 SdDataSelectButtonBase 임포트 없음 — host는 SdSharedDataSelectButton만 사용)
      When fixture를 재구현된 컴포넌트에 맞춰 검토
      Then SdSharedDataSelectButton의 외부 인터페이스 변경이 없으면 그대로 통과 (호환 유지 결과 fixture 그대로 가능)

    Scenario: spec에서 ctrl.load() 직접 호출 케이스 제거
      Given shared-data-select-button.spec.ts:53-71의 "load" describe — 현재 ctrl.load(keys) 직접 호출
      When 재구현 후 load 메서드는 더 이상 외부 노출되지 않음
      Then 해당 describe 제거 또는 "value 설정 후 _selectedItems 자동 동기화" 검증으로 재작성

    Scenario: 모달 선택 흐름 spec 유지 또는 재작성
      Given shared-data-select-button.spec.ts:78-104의 "모달 선택 흐름" describe — value 설정 시 selectedItems 자동 채워짐 + doShowModal 결과 반영
      When 재구현 후 ctrl.doShowModal()은 SdModalSelectButton에 위임됨
      Then SdModalSelectButton의 onSearchClick 또는 검색 버튼 클릭 시뮬레이션으로 재작성. 또는 SdSharedDataSelectButton 자체 검증을 "value+items effect" 중심으로 재구성

  Rule: data-select-button.md 레시피는 사용 패턴 3종을 박제한다
    # 근거: D4, wbs 175행

    Scenario: 패턴 1 — `<sd-modal-select-button>` 직접 사용 예제
      Given 레시피 문서 packages/angular/docs/recipes/data-select-button.md
      When "## 3. 패턴 1: <sd-modal-select-button> 직접 사용" 섹션을 연다
      Then SdModalSelectButton + SdSelectModal 구현 모달 컴포넌트 + value/selectedItems model 양방향 바인딩 예제가 존재한다

    Scenario: 패턴 2 — `<sd-shared-data-select-button>` 사용 예제 (공유 데이터)
      Given "## 4. 패턴 2: <sd-shared-data-select-button> (공유 데이터)" 섹션
      When 소비자가 공유 데이터 선택 예제를 찾는다
      Then SharedDataBase 기반 items + value 양방향 + itemTplRef 예제가 존재한다

    Scenario: 패턴 3 — 사용자 정의 select-button 예제 (LotSelectButton 패턴)
      Given "## 5. 패턴 3: 사용자 정의 select-button" 섹션
      When 소비자가 비동기 load 패턴 예제를 찾는다
      Then SdModalSelectButton 컴포지션 + 비동기 load(keys) effect 예제가 존재한다 (예: ORM 쿼리 기반)

    Scenario: 주의사항 섹션이 금지 API 목록을 명시
      Given "## 6. 주의사항" 섹션
      When 소비자가 금지 API를 확인한다
      Then "injectParent 호출 금지", "SdDataSelectButtonBase 상속 금지(이미 삭제됨)" bullet이 존재한다

    Scenario: cross-reference 섹션
      Given 레시피 마지막
      When 소비자가 관련 자료를 찾는다
      Then crud-list.md "변형 2: 선택 모달 뷰" 링크 + features.md SdSharedDataSelect/SdSharedDataSelectList 링크가 존재한다

  Rule: 삭제 관련 문서가 정리되고 레시피 링크로 교체된다
    # 근거: wbs 174-176행

    Scenario: docs/features-data-select-button.md가 삭제된다
      When 해당 파일을 삭제한다
      Then repo 내 다른 문서에서 이를 참조하는 링크도 제거 또는 교체된다

    Scenario: docs/features.md 정리
      Given features.md:50-67 "Data View Abstractions" 섹션 + :110-120 "SdSharedDataSelectButton" 섹션 존재
      When SdDataSelectButtonBase·SdDataSelectButton 관련 행 제거 + SdSharedDataSelectButton 섹션의 features-data-select-button.md 링크 무효화 처리
      Then features.md에서 삭제 대상 심볼 언급 0건

    Scenario: README.md 정리
      Given README.md:165 SdDataSelectButtonBase 행 + :166 SdDataSelectButton 행 존재
      When 2행 모두 제거 + :168 SdSharedDataSelectButton 행 유지하되 링크는 recipes/data-select-button.md로 교체(원래 링크 없음, "공유 데이터 모달 선택 버튼" 설명 그대로)
      Then README.md에서 삭제 대상 심볼 언급 0건

  Rule: Feature 완료 시 전체 검증을 통과한다
    # 근거: wbs 177행

    Scenario: pnpm check + vitest 통과
      When pnpm check -t angular 실행
      Then typecheck 및 lint 모두 통과
      When vitest run packages/angular/tests/ 실행
      Then 모든 spec이 통과
```

## 구현계획

### 배경

현재 `SdDataSelectButtonBase`(`sd-data-select-button.base.ts:24-104`)는 5개 input(disabled/required/inset/size/selectMode), 1 model(value), 1 signal(selectedItems), 1 computed(isNoValue), value→load effect, doShowModal/doInitialValue 메서드를 포함한 추상 클래스다. `SdDataSelectButton`(`sd-data-select-button.ts:83-103`)은 `injectParent<SdDataSelectButtonBase<any, any>>()` (`:84`)로 부모를 역참조하여 템플릿을 렌더한다. `SdSharedDataSelectButton`(`sd-shared-data-select-button.ts:46-62`)이 이 Base를 상속하고 `<sd-data-select-button>`을 컴포지션하는 구조다.

본 Feature는 (1) Base + 프레젠테이션 컴포넌트 2개를 삭제하고, (2) `SdSharedDataSelectButton`을 `<sd-modal-select-button>` 컴포지션 기반 독립 컴포넌트로 재구현한다. SdModalSelectButton(`sd-modal-select-button.ts:148-218`)이 이미 동일한 모달 호출/erase/invalid 로직을 보유하고 있어, value→items.filter(by __valueKey) 자동 동기화 effect만 추가하면 된다. 외부 인터페이스(`value` model, `items` input, `modal` input, `selectMode`/`disabled` 등)는 100% 호환 유지하며, `selectedItems`는 내부 signal로 숨겨 기존 SdDataSelectButtonBase 패턴을 따른다.

대체 가이드는 `docs/recipes/data-select-button.md`에 박제한다 — 사용 패턴 3종(직접 모달/공유 데이터/사용자 정의).

### 목표

- 2개 소스 파일(`sd-data-select-button.base.ts` + `sd-data-select-button.ts`) + 1개 디렉토리(`tests/data/data-select-button/`) + 1개 features 문서 제거 [근거: Rule "삭제 후 모노레포 내부에 참조가 남지 않는다"]
- `SdSharedDataSelectButton`을 SdModalSelectButton 컴포지션 기반으로 재구현, 외부 인터페이스 호환 100% [근거: Rule "재구현 후 외부 인터페이스 호환을 유지한다"]
- `tests/data/shared-data/shared-data-select-button.{spec,fixture}.ts`를 재구현 컴포넌트 동작 검증으로 재작성 [근거: Rule "재구현 컴포넌트 기반으로 재작성된다"]
- `docs/recipes/data-select-button.md` 신설 — 패턴 3종 + 주의사항 + cross-reference [근거: Rule "data-select-button.md 레시피는 사용 패턴 3종을 박제한다"]
- `docs/features.md` · `README.md` 정리 [근거: Rule "삭제 관련 문서가 정리되고 레시피 링크로 교체된다"]
- `pnpm check -t angular` 및 `vitest run` 통과 [근거: Rule "Feature 완료 시 전체 검증을 통과한다"]

### 비목표

- `SdModalSelectButton` 자체 수정 [근거: WBS "경계" 181행]
- `SdSelectModal<T>` · `SelectModalOutputResult<T>` · `SdSharedDataProvider` 수정 [근거: WBS "경계" 182-183행]
- `<sd-shared-data-select>` · `<sd-shared-data-select-list>` 수정 (Feature 3.1 범위 외, 자매 컴포넌트 상속 의존 없음) [근거: WBS "경계" 184행]
- `tests/data/shared-data/sd-shared-data-select{,-list}*` 자매 fixture/spec 수정 [근거: WBS "경계" 185행]
- 새로운 composition API(`useSelectButton()`) 신설 [근거: WBS "제외 사항"의 정신 — Base 추상화 재도입 방지]
- `injectParent` 자체 삭제 (Feature 6.1 범위) [근거: WBS Feature 6.1]

### 설계

#### 1. 재구현된 SdSharedDataSelectButton 클래스 시그니처

```typescript
@Component({
  selector: "sd-shared-data-select-button",
  changeDetection: ChangeDetectionStrategy.OnPush,
  encapsulation: ViewEncapsulation.None,
  standalone: true,
  imports: [SdModalSelectButton, NgTemplateOutlet, SdItemOfTemplate],
  template: `...`,
})
export class SdSharedDataSelectButton<
  TItem extends SharedDataBase<string | number>,
  TMode extends keyof SelectModeValue<string | number>,
  TModal extends SdSelectModal<any>,
> {
  // 외부 입력 (기존 인터페이스 호환)
  value = model<SelectModeValue<string | number>[TMode]>();
  items = input<TItem[]>([]);
  modal = input.required<SdSelectModalInfo<TModal>>();
  selectMode = input<TMode>("single" as TMode);
  disabled = input(false, { transform: booleanAttribute });
  required = input(false, { transform: booleanAttribute });
  inset = input(false, { transform: booleanAttribute });
  size = input<"sm" | "lg">();

  // 컨텐츠 템플릿 (기존 호환)
  itemTplRef = contentChild.required<any, TemplateRef<SdItemOfTemplateContext<TItem>>>(
    SdItemOfTemplate,
    { read: TemplateRef },
  );

  // 내부 signal (외부 미노출, D2)
  protected readonly _selectedItems = signal<TItem[]>([]);

  constructor() {
    effect(() => {
      const v = this.value();
      const items = this.items();
      const mode = this.selectMode();

      if (mode === "multi" && Array.isArray(v) && v.filterExists().length > 0) {
        const keys = v.filterExists() as (string | number)[];
        this._selectedItems.set(items.filter((it) => keys.includes(it.__valueKey)));
      } else if (mode === "single" && !Array.isArray(v) && v != null) {
        this._selectedItems.set(items.filter((it) => it.__valueKey === v));
      } else {
        this._selectedItems.set([]);
      }
    });
  }
}
```

#### 2. 재구현된 템플릿

```html
<sd-modal-select-button
  [(value)]="value"
  [(selectedItems)]="_selectedItems"
  [modal]="modal()"
  [disabled]="disabled()"
  [required]="required()"
  [inset]="inset()"
  [size]="size()"
  [selectMode]="selectMode()"
>
  @for (item of _selectedItems(); track item; let index = $index) {
    @if (index !== 0) {
      <span>,&nbsp;</span>
    }
    <ng-template
      [ngTemplateOutlet]="itemTplRef()"
      [ngTemplateOutletContext]="{
        $implicit: item,
        item: item,
        index: index,
        depth: 0,
      }"
    />
  }
</sd-modal-select-button>
```

**SdModalSelectButton의 selectedItems model에 wrap의 내부 signal을 양방향 바인딩.** 모달 결과로 SdModalSelectButton이 `selectedItems.set(result.selectedItems)` 호출 시 wrap의 `_selectedItems`도 갱신됨. 단, 다음 effect 실행 시 `value→items.filter` 결과로 다시 set되어 일관성 유지.

#### 3. data-select-button.md 레시피 구조 (D4, ~250줄)

```
1. Overview          — 제거된 추상화 / 대체 컴포넌트 매트릭스 / 유지되는 조력자
2. 언제 사용하는가     — 조건표 (직접 모달 / 공유 데이터 / 사용자 정의 / 단순 enum 등)
3. 패턴 1: <sd-modal-select-button> 직접 사용
   - SdSelectModal<T> 구현 모달 컴포넌트 + value/selectedItems model 바인딩
   - implements SdSelectModal<T>의 close.emit({selectedItemKeys, selectedItems}) 패턴
4. 패턴 2: <sd-shared-data-select-button> (공유 데이터)
   - SharedDataBase 기반 items + value + itemTplRef
   - selectedItems 자동 동기화 동작 설명
5. 패턴 3: 사용자 정의 select-button (LotSelectButton 패턴)
   - SdModalSelectButton 컴포지션 + 비동기 load(keys) effect
   - ORM 쿼리 기반 표시 데이터 채우기
6. 주의사항 (자주 하는 실수)
   - injectParent 호출 금지 (Feature 6.1에서 삭제 예정)
   - SdDataSelectButtonBase 상속 금지 (이미 삭제됨)
   - selectMode 동적 변경 시 value 타입 충돌 주의
7. Cross-reference
   - crud-list.md "변형 2: 선택 모달 뷰" — 선택 모달 측 구현
   - features.md SdSharedDataSelect / SdSharedDataSelectList — 자매 컴포넌트
```

#### 4. tests 재작성 상세

**`sd-shared-data-select-button-test.fixture.ts`** — 현재 (Read 결과) `SdDataSelectButtonBase` 임포트 없음, `SDSBTestHost`는 `SdSharedDataSelectButton`만 사용. 외부 인터페이스 호환 유지로 **fixture는 변경 없이 그대로 통과**할 가능성 높음. 단, `selectMode` 시그니처가 `signal<"single"|"multi">`인데 SdSharedDataSelectButton의 `selectMode`가 `input<TMode extends keyof SelectModeValue<...>>`이라 typecheck 통과 확인 필요.

**`shared-data-select-button.spec.ts`** — 재작성 필요:
- "load" describe(`:53-71`): 삭제. `ctrl.load(keys)`는 더 이상 외부 메서드가 아님. 동등 동작은 "value+items 설정 → _selectedItems 자동 채워짐" 시나리오로 흡수
- "모달 선택 흐름" describe(`:78-104`): 부분 재작성. `ctrl.doShowModal()`은 더 이상 외부 메서드가 아님 → 검색 버튼 클릭 시뮬레이션 또는 SdModalProvider mock 호출 검증으로 변경
- 신규 describe: "value+items 자동 동기화" — value 설정 시 _selectedItems가 items에서 자동 추출되는지 검증
- 신규 describe: "items 변경 시 _selectedItems 재계산"

검증 접근: `_selectedItems`는 protected 필드이므로 외부에서 직접 접근 불가. 대신 **DOM 쿼리로 `.item-name` 클래스 (fixture 템플릿) 개수·내용 검증**.

#### 5. 삭제 작업 상세

**`src/data/data-select-button/` 디렉토리 전체 삭제 (2개 파일)**:
1. `packages/angular/src/index.ts:247-248` 두 라인 제거
2. `packages/angular/tests/data/data-select-button/` 디렉토리 전체 삭제 (`rm -r`)
3. `packages/angular/src/data/data-select-button/` 디렉토리 전체 삭제 (`rm -r`)

순서는 동시 수행 가능. 각 Slice 단위에서 `pnpm check -t angular` 통과해야 함.

**docs 삭제/정리**:
- `packages/angular/docs/features-data-select-button.md` 삭제
- `packages/angular/docs/features.md`:
  - `:50-67` "Data View Abstractions" 섹션의 `SdDataSelectButtonBase` 행(:59) 제거
  - cross-reference 부분(`:67`)의 `features-data-select-button.md` 링크 제거
  - `:110-120` "SdSharedDataSelectButton" 섹션은 유지 (재구현 후에도 컴포넌트 자체는 유지)
- `packages/angular/README.md:165-166` 2행 제거 (`SdDataSelectButtonBase`, `SdDataSelectButton`). `:168` `SdSharedDataSelectButton` 행은 유지

### 대안 검토

#### 대안 A: SdModalSelectButton 코드 복사 후 직접 구현 (D1 옵션 B)
- **이유 기각**: 사용자 결정 D1 옵션 A 채택. 코드 중복으로 향후 SdModalSelectButton 변경 시 동기화 부담.

#### 대안 B: selectedItems를 외부 model로 노출 (D2 옵션 A)
- **이유 기각**: 사용자 결정 D2 옵션 B 채택. 기존 외부 인터페이스 호환성 우선. 외부 set과 내부 effect 충돌 위험.

#### 대안 C: tests 일부 이관 (D3 옵션 B)
- **이유 기각**: 사용자 결정 D3 옵션 A 채택. 이관 가치 있는 케이스가 SdModalSelectButton spec에 이미 존재. Feature 1.1 D2와 동일 정책.

#### 대안 D: crud-list.md 스타일 통합 완성 예제 (D4 옵션 B)
- **이유 기각**: 사용자 결정 D4 옵션 A 채택. 사용 패턴 3종이 본질적으로 분리된 케이스라 단일 예제 강제 시 부자연스러움.

#### 대안 E: SdDataSelectButton 유지 + 상속만 끊기 (`SdSharedDataSelectButton`만 재구현)
- **이유 기각**: WBS Feature 3.1 범위(167-169행)가 `SdDataSelectButton`/`SdDataSelectButtonBase` 모두 삭제 명시. 외부 사용처 grep 결과 0이라 유지 명분 없음.

#### 대안 F: 새로운 composition API(`useSelectButton()`)로 SdSharedDataSelectButton 재구성
- **이유 기각**: WBS "제외 사항" 정신과 충돌(Base 추상화 재도입 방지). 단순 effect로 충분.

### Vertical Slicing

#### [x] Slice 1: `data-select-button.md` 레시피 신설 (패턴 3종 + 주의사항 + cross-reference)

- **의존:** 없음 (레시피는 독립 작성 가능. 삭제·재구현보다 먼저 수행하여 리뷰·검토 시간 확보)
- **구현 내용:**
  - `packages/angular/docs/recipes/data-select-button.md` Write
  - 구조: Overview → 언제 → 패턴 1 (SdModalSelectButton 직접) → 패턴 2 (SdSharedDataSelectButton) → 패턴 3 (사용자 정의 LotSelectButton 패턴) → 주의사항 → Cross-reference
  - 모든 코드 블록은 TypeScript 정합성 확인 (import 경로, 타입 이름, signal 사용)
  - 패턴 3의 사용자 정의 예제는 SdModalSelectButton 컴포지션 + 비동기 load(keys) effect 패턴 (기존 features-data-select-button.md:382-419 LotSelectButton 패턴을 SdModalSelectButton 기반으로 재작성)
- **Scenarios:**
  - "패턴 1 — `<sd-modal-select-button>` 직접 사용 예제"
  - "패턴 2 — `<sd-shared-data-select-button>` 사용 예제 (공유 데이터)"
  - "패턴 3 — 사용자 정의 select-button 예제 (LotSelectButton 패턴)"
  - "주의사항 섹션이 금지 API 목록을 명시"
  - "cross-reference 섹션"

#### [x] Slice 2: SdSharedDataSelectButton 재구현 + `src/data/data-select-button/` 삭제 + tests 재작성/삭제 + index.ts 정리

- **의존:** Slice 1 (레시피가 선행 존재해야 docs 링크 교체가 유효)
- **구현 내용:**
  1. `packages/angular/src/data/shared-data/sd-shared-data-select-button.ts` 재작성:
     - `extends SdDataSelectButtonBase` 제거
     - SdModalSelectButton 컴포지션 + value/items effect + itemTplRef 유지
     - 외부 인터페이스(value model, items/modal/selectMode/disabled/required/inset/size input, itemTplRef contentChild) 100% 호환
     - generic `<TItem extends SharedDataBase<string|number>, TMode, TModal extends SdSelectModal<any>>` 유지
  2. `packages/angular/tests/data/data-select-button/` 디렉토리 전체 삭제 (fixture + spec 460줄)
  3. `packages/angular/src/data/data-select-button/` 디렉토리 전체 삭제 (base.ts + .ts)
  4. `packages/angular/src/index.ts:247-248` 2라인 제거
  5. `packages/angular/tests/data/shared-data/shared-data-select-button.spec.ts` 재작성:
     - "load" describe 삭제 (`ctrl.load()`는 더 이상 외부 메서드 아님)
     - "모달 선택 흐름" describe 부분 재작성 (검색 버튼 click 시뮬레이션 또는 SdModalProvider mock 검증)
     - 신규 describe: "value+items 자동 동기화" (DOM `.item-name` 검증)
     - 신규 describe: "items 변경 시 _selectedItems 재계산"
  6. `packages/angular/tests/data/shared-data/sd-shared-data-select-button-test.fixture.ts`는 외부 인터페이스 호환으로 그대로 통과 가능성 높으나 typecheck 결과에 따라 selectMode 시그니처 캐스팅 등 미세 조정 가능
  7. 연쇄 확인: `pnpm check -t angular`(typecheck + lint) 통과 + `vitest run packages/angular/tests/` 통과
- **Scenarios:**
  - "src/index.ts에서 SdDataSelectButton·SdDataSelectButtonBase export 2종 제거"
  - "src/data/data-select-button/ 디렉토리 전체가 삭제된다"
  - "외부 사용처 템플릿 무변경 동작"
  - "value+items 변경 시 selectedItems 자동 동기화"
  - "items만 변경되어도 selectedItems 재계산"
  - "모달 선택 후 value 갱신"
  - "erase 버튼 클릭 시 value 리셋"
  - "itemTplRef 외부 템플릿이 selectedItems 각 item에 대해 렌더링"
  - "재구현된 컴포넌트의 injectParent 호출 0건"
  - "fixture·spec 2개 파일이 모두 삭제된다"
  - "spec에서 ctrl.load() 직접 호출 케이스 제거"
  - "모달 선택 흐름 spec 유지 또는 재작성"

#### [x] Slice 3: docs 정리 (`features-data-select-button.md` 삭제, `features.md` + `README.md` 정리)

- **의존:** Slice 1 (레시피 파일이 존재해야 링크 유효)
- **구현 내용:**
  - `packages/angular/docs/features-data-select-button.md` 삭제
  - `packages/angular/docs/features.md`:
    - `:50-67` "Data View Abstractions" 섹션의 `SdDataSelectButtonBase` 행(`:59`) 제거
    - cross-reference 영역(`:65-67`)의 `features-data-select-button.md` 링크 제거
    - `:110-120` `SdSharedDataSelectButton` 섹션은 유지 (재구현 후에도 컴포넌트 자체는 유지)
  - `packages/angular/README.md:165-166` 2행 제거 (`SdDataSelectButtonBase`, `SdDataSelectButton`). `:168` `SdSharedDataSelectButton` 행 유지
  - 기타 docs(`packages/angular/docs/utils.md`, `packages/angular/CLAUDE.md`)에서 삭제 심볼 언급 Grep → 발견 시 수정
  - `packages/angular/docs/features-data-detail.md` 등 cross-link 영향 확인
- **Scenarios:**
  - "docs/features-data-select-button.md가 삭제된다"
  - "docs/features.md 정리"
  - "README.md 정리"

#### [x] Slice 4: 최종 검증 + 레시피 품질 점검

- **의존:** Slice 1, 2, 3
- **구현 내용:**
  - `pnpm check -t angular` (typecheck + lint, 최초 통과)
  - `vitest run packages/angular/tests/` (모든 spec 통과)
  - 레시피 자체 읽기 검토: 금지 API 호출(`injectParent`/`SdDataSelectButtonBase`) 0건 Grep, `.js` 확장자 import 0건 Grep
  - 레시피 코드 블록의 심볼 실제 존재 확인(`SdModalSelectButton`, `SdSelectModal`, `SdSelectModalInfo`, `SelectModalOutputResult`, `SdSharedDataSelectButton`, `SdItemOfTemplate`, `SharedDataBase`)이 모두 `packages/angular/src/index.ts`에 export되어 있는지 Grep 확인
  - 모노레포 내 `SdDataSelectButton`/`SdDataSelectButtonBase` Grep 결과 0건 확인 (재구현 새 파일/주석 제외)
  - `injectParent` Grep 결과: SdSharedDataSelectButton 재구현 파일에서 0건 확인
- **Scenarios:**
  - "Grep으로 잔여 참조 0건 확인"
  - "pnpm check + vitest 통과"
  - (레시피 품질은 sd-review 단계에서 재확인)
