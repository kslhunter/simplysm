# Feature 3.1 아키텍처 문서 업데이트 — LLM 검증

## 검증 항목

- [x] Architecture 트리 esbuild/ 섹션에 `esbuild-tsc-plugin.ts` 항목이 역할 설명과 함께 존재한다: line 58 확인
- [x] workers/ 섹션의 `server-build.worker.ts` 설명에 tsc 플러그인 통합이 반영되어 있다: line 38 `← server 패키지 esbuild + tsc 플러그인 통합 빌드`
- [x] workers/ 섹션의 `server-esbuild-context.ts` 설명에 tsc 플러그인 관리가 반영되어 있다: line 39 `← esbuild watch context + tsc 플러그인 관리`
- [x] Key Patterns 섹션에 "esbuild tsc 플러그인" 패턴이 존재하고, onStart microtask/onEnd 결과 수집/getter 조회가 설명되어 있다: lines 240-261
- [x] Key Patterns의 코드 스니펫이 실제 코드(`esbuild-tsc-plugin.ts`)의 API와 일치한다: `TscPluginResult` 인터페이스의 `plugin`, `getErrors()`, `getProgram()`, `getDiagnostics()`, `getAffectedFiles()` 모두 일치
- [x] Testing 섹션 테스트 디렉토리 트리에 `esbuild/` 항목이 존재한다: line 276
- [x] 수정 후 마크다운 구조(들여쓰기, 트리 기호)가 기존 포맷과 일관된다: `│`, `├──` 기호 및 들여쓰기 일관
