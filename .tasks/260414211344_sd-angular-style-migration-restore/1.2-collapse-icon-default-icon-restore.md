# Feature 1.2 sd-collapse-icon 기본 아이콘 복원

## 참조 자료
- WBS: [wbs.md](./wbs.md)
- v12 원본: `D:\workspaces-12\simplysm\packages\sd-angular\src\ui\navigation\collapse\sd-collapse-icon.control.ts`
- v14 대상: `packages/angular/src/controls/collapse/sd-collapse-icon.ts`

### 설계 결정
| # | 결정사항 | 선택 | 근거 |
|---|---------|------|------|
| - | - | - | 결정사항 없음 (단순 복원) |

## 요구명세

```gherkin
Feature: 1.2 sd-collapse-icon 기본 아이콘 복원

  Rule: 기본 아이콘은 tablerChevronDown
    # 근거: v12 원본 sd-collapse-icon.control.ts:34
    Scenario: icon을 지정하지 않으면 tablerChevronDown이 사용된다
      Given sd-collapse-icon 컴포넌트가 렌더링될 때
      And [icon] input이 지정되지 않은 경우
      Then 내부 ng-icon의 svg는 tablerChevronDown이다

    Scenario: icon을 명시적으로 지정하면 해당 아이콘이 사용된다
      Given sd-collapse-icon 컴포넌트가 렌더링될 때
      And [icon]에 tablerChevronRight가 지정된 경우
      Then 내부 ng-icon의 svg는 tablerChevronRight이다
```

## 구현계획

### 배경
`SdCollapseIcon` 컴포넌트는 접기/펼치기 상태를 시각적으로 나타내는 아이콘이다. v14 마이그레이션 시 기본 아이콘이 `tablerChevronDown`에서 `tablerChevronRight`로 변경되었다.

### 목표
- 기본 아이콘을 `tablerChevronDown`으로 복원 [근거: Rule "기본 아이콘은 tablerChevronDown"]

### 비목표
- transition 방식 변경 복원 [근거: wbs 경계 "transition 방식 변경은 v14 개선"]
- `numberAttribute` transform 복원 [근거: wbs 경계 "numberAttribute transform 추가도 개선"]

### 설계
- `sd-collapse-icon.ts`의 import를 `tablerChevronRight` → `tablerChevronDown`으로 변경
- `icon` input의 기본값을 `tablerChevronDown`으로 변경

### 대안 검토
없음. 단순 기본값 복원이므로 대안 검토 불필요.

### Vertical Slicing

#### [x] Slice 1: 기본 아이콘 복원
- **구현 내용:** import 변경 (`tablerChevronRight` → `tablerChevronDown`) + input 기본값 변경
- **Scenarios:** "icon을 지정하지 않으면 tablerChevronDown이 사용된다", "icon을 명시적으로 지정하면 해당 아이콘이 사용된다"
