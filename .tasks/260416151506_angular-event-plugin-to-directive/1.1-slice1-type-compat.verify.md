# SdResizeEvent 타입 호환성 — LLM 검증

## 검증 항목

- [x] `heightChanged: boolean` 필드 동일: 기존(`sd-resize-event.plugin.ts:57`) → 신규(`sd-resize.ts:50`) 동일
- [x] `widthChanged: boolean` 필드 동일: 기존(`:58`) → 신규(`:51`) 동일
- [x] `target: Element` 필드 동일: 기존(`:59`) → 신규(`:52`) 동일
- [x] `contentRect: DOMRectReadOnly` 필드 동일: 기존(`:60`) → 신규(`:53`) 동일
- [x] 인터페이스 이름 동일: `SdResizeEvent`
