# electron.ts 마이그레이션 — LLM 검증

## 검증 항목
- [x] import 정리: `import path from "path"` 제거됨, `posixResolve`가 `@simplysm/core-node`에서 import됨 — `grep "path\." electron.ts` 결과 0건 확인
- [x] 모든 path.resolve 교체: 원본 22건 path.resolve → 22건 posixResolve 호출로 교체 확인
- [x] 모든 path.join 교체: 원본 2건 path.join (symlink 테스트) → 2건 posixResolve 호출로 교체 확인
- [x] 총 posixResolve 호출 24건 = 원본 path.resolve(22) + path.join(2) 일치
- [x] 기존 테스트 25건 전체 통과
