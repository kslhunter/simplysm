# Feature 3.2 ngtsc/client 빌드 워커 마이그레이션 — LLM 검증

## 검증 항목
- [x] ngtsc-build.worker.ts에 ad-hoc `.replace(/\\/g, "/")` 패턴이 0건: Grep 확인 완료, 매칭 없음
- [x] posix() 호출이 4건 존재 (라인 101, 123, 141, 302): Grep 확인 완료
- [x] `posix` import가 `@simplysm/core-node`에 추가됨: `import { createWorker, FsWatcher, posix } from "@simplysm/core-node"` 확인
- [x] client.worker.ts 미변경: `.replace(/\\/g, "/")` 패턴 Grep 결과 0건, 원본 상태 유지 확인
