# Feature 1.1 workspace 패키지 이름 충돌 감지

## 참조 자료

- [review.md](review.md) — LOGIC-001

### 설계 결정

| # | 결정사항 | 선택 | 근거 |
|---|---------|------|------|
| D1 | 충돌 처리 방식 | SdError throw | 무경고 덮어쓰기보다 조기 실패가 안전 |

## 요구명세

```gherkin
Feature: 1.1 workspace 패키지 이름 충돌 감지

  Rule: 동일 이름의 workspace 패키지가 여러 디렉토리에 존재하면 에러 발생

    Scenario: packages/와 tests/ 하위에 동일 이름 디렉토리 존재
      Given packages/foo/package.json이 존재한다
      And tests/foo/package.json이 존재한다
      When discoverWorkspacePackages를 호출한다
      Then "Duplicate workspace package name: foo" 에러가 발생한다

    Scenario: 이름이 다른 경우 정상 동작
      Given packages/orm-node/package.json이 존재한다
      And tests/orm/package.json이 존재한다
      When discoverWorkspacePackages를 호출한다
      Then Map에 orm-node → packages/orm-node, orm → tests/orm이 포함된다
```

## 구현계획

- [x] Slice 1 (완료): `discoverWorkspacePackages`에서 Map.set 전 중복 검사, 중복 시 SdError throw. 테스트 추가.
