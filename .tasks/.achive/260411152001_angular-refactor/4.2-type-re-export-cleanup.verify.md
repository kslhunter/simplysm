# Feature 4.2 타입 re-export 정리 — LLM 검증

## 검증 항목

- [x] sd-app-structure.types.ts에서 @simplysm/service-common re-export 제거됨: 파일에 `@simplysm/service-common` import/export가 없음. SdMenu, SdFlatMenu, SdPermission 인터페이스만 남아있음
- [x] sd-app-structure.provider.ts에서 AppStructureItem import 경로 변경: `import type { AppStructureItem } from "@simplysm/service-common"` 확인
- [x] sd-app-structure.utils.ts에서 AppStructureItem import 경로 변경: `import type { AppStructureItem } from "@simplysm/service-common"` 확인, 기존 런타임 import(`getFlatPermissions` 등)는 유지
- [x] index.ts에서 AppStructureItem, FlatPermission export 제거: `SdMenu`, `SdFlatMenu`, `SdPermission`만 export됨
- [x] 테스트 파일 import 경로 변경: `app-structure-provider.spec.ts`에서 `import type { AppStructureItem } from "@simplysm/service-common"` 확인
- [x] 로컬 타입 보존: SdMenu, SdFlatMenu, SdPermission 인터페이스가 sd-app-structure.types.ts에 그대로 존재
- [x] 기존 테스트 회귀 없음: 142 파일 통과, 1316 테스트 통과. 실패 1건은 pre-existing (Feature 4.1 관련 `shouldProcessCommandEvent` 미구현)
