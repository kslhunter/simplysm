# Feature 1.5 features/ 카테고리 생성

## 참조 자료
- WBS: [wbs.md](./wbs.md)
- 코드베이스 import 의존성 분석:
  - `visual/` → `core/` (format.pipe, template/sd-item-of-template)
  - `editor/` → `core/` (validation/setupInvalid)
  - `address/` → `core/` (busy/sd-busy-container, modal/sd-modal.provider)
  - `theme/` → `controls/` (dropdown, button, checkbox)
  - `core/provideSdAngular.ts` → `theme/sd-theme-provider`
- 4개 디렉토리 간 상호 참조: 없음

### 설계 결정
| # | 결정사항 | 선택 | 근거 |
|---|---|---|---|
| D1 | tests/ 구조 미러링 | src/와 동일하게 features/ 하위로 이동 | wbs 설계 결정 D1 계승 |
| D2 | 그룹 내 상호 참조 경로 | 해당 없음 (상호 참조 없음) | grep 분석 결과 4개 디렉토리 간 cross-reference 없음 |

## 요구명세

```gherkin
Feature: 1.5 features/ 카테고리 생성

  Background:
    Given Feature 1.4까지 완료되어 core/, controls/, layout/, data/ 카테고리가 존재한다

  Rule: 4개 디렉토리를 features/ 하위로 이동
    # 근거: wbs "visual/, editor/, address/, theme/"
    Scenario: src/features/ 디렉토리 생성 및 4개 디렉토리 이동
      When 4개 디렉토리를 src/features/ 하위로 이동한다
      Then src/features/ 아래에 visual/, editor/, address/, theme/ 이 존재한다
      And 원래 위치의 디렉토리는 존재하지 않는다

  Rule: import 경로가 새 위치를 반영
    # 근거: 이동 시 depth 1 증가
    Scenario: 이동된 파일의 core/ 참조 import 경로 수정
      When 이동된 파일에서 core/ 참조 import를 확인한다
      Then "../core/" 가 "../../core/" 로 변경된다

    Scenario: theme/의 controls/ 참조 import 경로 수정
      When theme/sd-theme-selector.ts에서 controls/ 참조를 확인한다
      Then "../controls/" 가 "../../controls/" 로 변경된다

  Rule: core/provideSdAngular.ts의 theme 참조 수정
    # 근거: 코드에서 확인 — "../theme/sd-theme-provider"
    Scenario: provideSdAngular의 theme import 경로 수정
      When core/provideSdAngular.ts의 theme import를 확인한다
      Then "../theme/sd-theme-provider" 가 "../features/theme/sd-theme-provider" 로 변경된다

  Rule: index.ts re-export 경로 수정
    # 근거: wbs "index.ts re-export 경로 수정"
    Scenario: index.ts에서 features/ prefix 추가
      When src/index.ts의 re-export 경로를 확인한다
      Then 4개 디렉토리 관련 export가 "./features/{dir}/..." 경로를 사용한다

  Rule: tests/ 디렉토리도 src/ 구조 미러링
    # 근거: wbs 설계 결정 D1
    Scenario: 4개 테스트 디렉토리 이동
      When 4개 테스트 디렉토리를 tests/features/ 하위로 이동한다
      Then tests/features/ 아래에 visual/, editor/, address/, theme/ 이 존재한다
      And 테스트 파일의 import 경로가 새 위치를 반영한다

  Rule: CLAUDE.md 구조 문서 업데이트
    # 근거: wbs "packages/angular/CLAUDE.md 구조 문서 업데이트"
    Scenario: 아키텍처 문서에 features/ 카테고리 반영
      When packages/angular/CLAUDE.md를 업데이트한다
      Then 디렉토리 구조에 features/ 카테고리와 하위 디렉토리가 반영된다
```

## 구현계획

### 배경
Feature 1.1~1.4에서 core/, controls/, layout/, data/ 카테고리가 생성되었다. 현재 src/ top-level에 남은 feature 디렉토리 4개를 features/ 카테고리로 묶는다. [근거: wbs "다양한 계층에 의존하는 독립 기능 컴포넌트"]

### 목표
- 4개 독립 기능 디렉토리를 features/ 하위로 이동 [근거: Rule "4개 디렉토리를 features/ 하위로 이동"]
- 모든 import 경로가 새 위치를 정확히 반영 [근거: Rule "import 경로가 새 위치를 반영"]
- public API(index.ts re-export) 유지 [근거: wbs 기술적 제약]
- CLAUDE.md 구조 문서 업데이트 [근거: wbs 범위]

### 비목표
- 디렉토리 내부 파일 구조 변경 [근거: wbs 경계]
- 코드 로직 변경 [근거: wbs 경계]

### 설계

**디렉토리 이동 매핑:**

| 원본 | 대상 |
|---|---|
| src/visual/ | src/features/visual/ |
| src/editor/ | src/features/editor/ |
| src/address/ | src/features/address/ |
| src/theme/ | src/features/theme/ |

**import 경로 변경 규칙:**
- 이동된 파일 → core/: `../core/` → `../../core/`
- 이동된 파일 → controls/: `../controls/` → `../../controls/`
- core/provideSdAngular.ts → theme: `../theme/` → `../features/theme/`
- index.ts: `./visual/` → `./features/visual/`, `./editor/` → `./features/editor/`, `./address/` → `./features/address/`, `./theme/` → `./features/theme/`

**테스트 디렉토리 이동:**

| 원본 | 대상 |
|---|---|
| tests/visual/ | tests/features/visual/ |
| tests/editor/ | tests/features/editor/ |
| tests/address/ | tests/features/address/ |
| tests/theme/ | tests/features/theme/ |

**테스트 import 경로 변경:**
- `../../src/visual/` → `../../../src/features/visual/`
- `../../src/editor/` → `../../../src/features/editor/`
- `../../src/address/` → `../../../src/features/address/`
- `../../src/theme/` → `../../../src/features/theme/`
- `../src/theme/` → `../src/features/theme/` (tests/ 루트의 spec 파일)

### 대안 검토
없음 — 순수 파일 이동 + 경로 수정이므로 설계 대안이 발생하지 않는다.

### Vertical Slicing

#### [x] Slice 1: src/ 디렉토리 이동 + import 경로 수정
- **구현 내용:** 4개 디렉토리를 src/features/로 이동, 내부 파일의 외부 참조 import 경로 수정, core/provideSdAngular.ts의 theme import 수정, src/index.ts re-export 경로 수정
- **Scenarios:** src/features/ 디렉토리 생성 및 4개 디렉토리 이동, core/ 참조 import 경로 수정, controls/ 참조 import 경로 수정, provideSdAngular의 theme import 경로 수정, index.ts에서 features/ prefix 추가

#### [x] Slice 2: tests/ 디렉토리 이동 + import 경로 수정
- **의존:** Slice 1
- **구현 내용:** 4개 테스트 디렉토리를 tests/features/로 이동, 테스트 파일의 import 경로 수정, tests/ 루트 spec 파일의 theme import 수정
- **Scenarios:** 4개 테스트 디렉토리 이동

#### [x] Slice 3: CLAUDE.md 업데이트
- **의존:** Slice 1
- **구현 내용:** packages/angular/CLAUDE.md의 디렉토리 구조를 최종 5개 카테고리 구조로 업데이트
- **Scenarios:** 아키텍처 문서에 features/ 카테고리 반영
