# Feature 1.1 리뷰 이슈 일괄 수정

## 참조 자료

- WBS: [wbs.md](./wbs.md)
- 리뷰 리포트: `.tasks/260411144018_review-sd-cli-refactor/review.md`
- CLAUDE.md: `packages/sd-cli/CLAUDE.md`

### 설계 결정

| # | 결정사항 | 선택 | 근거 |
|---|---------|------|------|
| D1 | CLAUDE.md의 삭제된 파일(ViteEngine, vite-*) 처리 | 함께 반영 | git status에서 삭제 확인. CLAUDE.md는 현재 코드 상태를 반영해야 함 |

## 요구명세

```gherkin
Feature: 1.1 리뷰 이슈 일괄 수정

  Background:
    Given sd-cli 리팩토링이 완료되어 DevWatchOrchestrator가 삭제되고 새 구조로 대체되었다

  Rule: CLAUDE.md가 현재 코드 구조를 정확히 반영해야 한다
    # 근거: CONSIST-001 "CLAUDE.md가 리팩토링 결과를 반영하지 않음"

    Scenario: Architecture 섹션이 현재 파일 구조를 반영
      Given CLAUDE.md의 Architecture 섹션이 존재한다
      When orchestrators/ 하위 파일 목록을 확인한다
      Then BaseOrchestrator.ts, WatchOrchestrator.ts, DevOrchestrator.ts, ServerRuntimeManager.ts가 기재되어 있다
      And DevWatchOrchestrator.ts가 기재되어 있지 않다

    Scenario: Key Patterns 섹션의 코드 예시가 현재 구조를 반영
      Given Orchestrator 생명주기 섹션이 존재한다
      Then WatchOrchestrator 또는 DevOrchestrator 사용 예시가 표시된다

    Scenario: 새로 생긴 유틸 파일이 기재
      Given utils/ 섹션이 존재한다
      Then package-classify.ts, collect-deps.ts, replace-deps-resolve.ts, engine-watch-events.ts가 기재되어 있다

  Rule: 삭제된 클래스를 참조하는 코멘트는 현재 구조를 반영해야 한다
    # 근거: CONSIST-002 "stale DevWatchOrchestrator 코멘트"

    Scenario: src 파일의 stale 코멘트 업데이트
      Given TypecheckOrchestrator.ts, orchestrator-utils.ts, package-classify.ts에 DevWatchOrchestrator 참조가 있다
      When 코멘트를 현재 구조에 맞게 업데이트한다
      Then DevWatchOrchestrator 참조가 WatchOrchestrator/DevOrchestrator/BaseOrchestrator로 대체된다

    Scenario: test 파일의 stale 코멘트 업데이트
      Given orchestrator-utils.spec.ts에 DevWatchOrchestrator 코멘트가 있다
      When 코멘트를 업데이트한다
      Then 현재 구조를 반영한다

  Rule: dead code 파라미터를 제거해야 한다
    # 근거: DESIGN-001 "packagesForValidation 파라미터가 dead code"

    Scenario: loadAndValidateConfig의 packagesForValidation 제거
      Given loadAndValidateConfig 함수에 packagesForValidation 옵션 파라미터가 있다
      And 어떤 호출자도 이 파라미터를 전달하지 않는다
      When 파라미터를 제거한다
      Then 함수 시그니처에서 packagesForValidation이 없어진다
      And JSDoc에서 DevWatchOrchestrator 관련 설명이 제거된다
```

## 구현계획

### 배경

sd-cli 리팩토링으로 `DevWatchOrchestrator`가 `BaseOrchestrator` + `WatchOrchestrator` + `DevOrchestrator` + `ServerRuntimeManager`로 분리되었다. 코드 구현은 완료되었으나 CLAUDE.md와 코멘트가 이전 구조를 참조하고 있다.

### 목표

- CLAUDE.md를 현재 코드 구조에 맞게 업데이트 [근거: Rule "CLAUDE.md가 현재 코드 구조를 정확히 반영"]
- stale 코멘트를 현재 구조에 맞게 수정 [근거: Rule "삭제된 클래스를 참조하는 코멘트"]
- dead code 파라미터 제거 [근거: Rule "dead code 파라미터를 제거"]

### 비목표

- Low severity 이슈 수정 [근거: wbs 경계 "Low severity 이슈는 이 Feature에서 수정하지 않음"]
- 코드 동작 변경 [근거: wbs "코드 동작 변경 없이 문서/코멘트/dead code만 수정"]

### 설계

#### CLAUDE.md 변경 (CONSIST-001)

orchestrators/ 섹션:
```
├── orchestrators/         ← 커맨드-엔진 조율
│   ├── BaseOrchestrator.ts      ← watch/dev 공통 기반 (추상 클래스)
│   ├── WatchOrchestrator.ts     ← watch 모드 (라이브러리 빌드 + 스크립트 훅)
│   ├── DevOrchestrator.ts       ← dev 모드 (서버 런타임 + 클라이언트 dev server)
│   ├── ServerRuntimeManager.ts  ← dev 모드 서버 런타임 워커 관리
│   ├── BuildOrchestrator.ts     ← 프로덕션 빌드 (일회성)
│   └── TypecheckOrchestrator.ts ← TypeScript 타입체크
```

engines/ 섹션: `createTypecheckEngine` 추가, `ViteEngine.ts` 제거 (삭제됨 — D1)

utils/ 섹션: 새 파일 추가, 삭제된 파일 제거 (D1), package-utils.ts 설명 변경

Key Patterns: Orchestrator 생명주기 예시를 WatchOrchestrator/DevOrchestrator로 변경

Testing: orchestrators 설명 업데이트

자주하는 실수: DevWatchOrchestrator → WatchOrchestrator/DevOrchestrator

#### stale 코멘트 수정 (CONSIST-002)

| 파일 | 라인 | 현재 | 변경 후 |
|------|------|------|---------|
| `TypecheckOrchestrator.ts` | 75 | "BuildOrchestrator/DevWatchOrchestrator와 동일한 ..." | "BuildOrchestrator/WatchOrchestrator/DevOrchestrator와 동일한 ..." |
| `orchestrator-utils.ts` | 7 | "BuildOrchestrator.initialize()와 DevWatchOrchestrator.initialize()에서 호출" | "BuildOrchestrator.initialize()에서 호출" |
| `orchestrator-utils.ts` | 9 | "DevWatchOrchestrator는 tests/ 패키지를 merge한 결과에 대해 검증해야 하므로" | (제거 — packagesForValidation 파라미터 제거와 함께) |
| `package-classify.ts` | 81 | "DevWatchOrchestrator._classifyWatchPackages에서 추출" | "WatchOrchestrator에서 사용하는 standalone 함수" |
| `package-classify.ts` | 122 | "DevWatchOrchestrator._classifyDevPackages에서 추출" | "DevOrchestrator에서 사용하는 standalone 함수" |
| `orchestrator-utils.spec.ts` | 49-50 | 'DevWatchOrchestrator가 공유 초기화 유틸을 사용한다' | 'BaseOrchestrator가 공유 초기화 유틸을 사용한다' (또는 제거) |

#### dead code 제거 (DESIGN-001)

`orchestrator-utils.ts`:
- `packagesForValidation` 파라미터 제거
- JSDoc에서 관련 설명 제거
- `validateTargets` 호출에서 `params.packagesForValidation ?? config.packages` → `config.packages`

`orchestrator-utils.spec.ts`:
- "works with dev:true for DevWatchOrchestrator" 테스트의 이름 변경 (코멘트만)

### Vertical Slicing

#### [x] Slice 1: CLAUDE.md + stale 코멘트 + dead code 일괄 수정

- **구현 내용:** CLAUDE.md 업데이트, 5개 src 파일의 stale 코멘트 수정, orchestrator-utils.ts의 packagesForValidation 제거, 테스트 코멘트 수정
- **Scenarios:** 전체 (Architecture 반영, Key Patterns 반영, 새 유틸 파일 기재, src stale 코멘트, test stale 코멘트, dead code 제거)
