# sd-claude auth CLI — LLM 검증

## Slice 1: CLI 엔트리포인트

- [x] cli.mjs shebang `#!/usr/bin/env node` 존재: 1행 확인
- [x] package.json bin `"sd-claude": "scripts/cli.mjs"` 설정: 13-15행 확인
- [x] `sd-claude auth save` → argv[2]="auth", argv[3]="save" → auth.save() 호출: cli.mjs 6-9행
- [x] `sd-claude auth switch` → argv[2]="auth", argv[3]="switch" → auth.switch_() 호출: cli.mjs 10-11행
- [x] `sd-claude unknown` → else → printUsage() + exit(1): cli.mjs 16-18행
- [x] `sd-claude auth` (action 없음) → argv[3]=undefined → else → printUsage() + exit(1): cli.mjs 12-14행

## Slice 2: auth save

- [x] execSync("claude auth status") → JSON.parse → orgName 추출: auth.mjs 119-121행
- [x] orgName 실패 시 catch → error + exit(1): auth.mjs 123-125행
- [x] orgName 빈 문자열 → error + exit(1): auth.mjs 127-129행
- [x] getRefreshToken() — .credentials.json에서 claudeAiOauth.refreshToken 읽기: auth.mjs 28-33행
- [x] refreshToken 빈 문자열 → error + exit(1): auth.mjs 132-134행
- [x] loadProfiles() — profiles.json의 accounts 필드 존재 확인 후 반환, 없으면 기본값: auth.mjs 14-21행
- [x] saveProfiles() — JSON.stringify + writeFileSync: auth.mjs 23-25행
- [x] getUsage() — statusline-cache.json의 rate_limits 변환: auth.mjs 36-52행
- [x] 저장 시 refreshToken + usage + current 갱신: auth.mjs 137-141행

## Slice 3: auth switch

- [x] non-TTY 감지: `!process.stdin.isTTY` → error + exit(1): auth.mjs 145-148행
- [x] 계정 없음: Object.keys(accounts).length === 0 → 메시지 출력 + return: auth.mjs 154-157행
- [x] 현재 계정만: selectable.length === 0 → 메시지 출력 + return: auth.mjs 164-167행
- [x] printList: 번호 + 마커(*) + 사용량 표시: auth.mjs 77-94행
- [x] 현재 계정은 실시간 사용량(getUsage()), 다른 계정은 저장 시점 사용량(info.usage): auth.mjs 90-91행
- [x] formatRemaining: null→"N/A", 과거→"리셋됨", days/hours/minutes 포맷: auth.mjs 54-63행
- [x] formatUsage: null→"[사용량 정보 없음]", isLive 여부에 따른 suffix: auth.mjs 66-75행
- [x] promptSelection: readline으로 번호 입력, 범위 외 → null: auth.mjs 96-112행
- [x] 현재 계정 백업: refreshToken + usage 갱신 후 saveProfiles: auth.mjs 185-195행
- [x] spawnSync("claude", ["auth", "login"]) + CLAUDE_CODE_OAUTH_REFRESH_TOKEN 환경변수: auth.mjs 198-207행
- [x] 전환 후 갱신된 refreshToken 저장 + current 업데이트: auth.mjs 210-216행

## Slice 4: sd-auth 스킬 삭제

- [x] .claude/skills/sd-auth/ 폴더 삭제됨: ls 명령으로 "No such file or directory" 확인
- [x] 다른 파일에서 sd-auth 참조 없음: grep 결과 스킬 폴더 내부 파일만 매칭 (이미 삭제됨)
- [x] postinstall.mjs: sd-* 패턴 탐색이므로 sd-auth 삭제에 영향 없음
- [x] sync.mjs: .claude/ 에서 sd-* 수집하므로 sd-auth 없으면 자연스럽게 제외
