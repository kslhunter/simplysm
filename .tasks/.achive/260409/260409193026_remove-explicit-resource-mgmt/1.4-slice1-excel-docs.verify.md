# Slice 1: excel 패키지 문서 업데이트 — LLM 검증

## 검증 항목

### Scenario: excel 문서의 await using 코드 예시 변경
- [x] packages/excel/CLAUDE.md: `await using` 코드 예시 3건이 try-finally + await close() 패턴으로 변경됨 — grep 결과 `await using` 0건
- [x] .claude/references/sd-simplysm14/excel/usage.md: `await using` 코드 예시 3건이 try-finally + await close() 패턴으로 변경됨 — grep 결과 `await using` 0건

### Scenario: excel core-classes.md에서 Symbol.asyncDispose 시그니처 및 설명 제거
- [x] core-classes.md API 시그니처에서 `[Symbol.asyncDispose]()` 행이 제거됨 — grep 결과 `Symbol.asyncDispose` 0건
- [x] core-classes.md 메서드 설명 섹션에서 `[Symbol.asyncDispose]()` 설명이 제거됨 — grep 결과 확인

### Scenario: excel 문서의 리소스 관리 안내 문구 변경
- [x] packages/excel/CLAUDE.md: `await using` 참조가 try-finally 안내로 변경됨 — "반드시 `try-finally` 블록에서 `close()`를 호출한다" 확인
- [x] .claude/references/sd-simplysm14/excel/docs/wrapper.md: `await using` 참조가 try-finally + close() 안내로 변경됨 — "`try-finally` 블록에서 `close()`를 호출하여 리소스를 관리해야 한다" 확인
- [x] .claude/references/sd-simplysm14/excel/docs/core-classes.md: `await using` 참조가 try-finally + close() 안내로 변경됨 — "반드시 `try-finally` 블록에서 `close()`를 호출해야 한다" 확인
