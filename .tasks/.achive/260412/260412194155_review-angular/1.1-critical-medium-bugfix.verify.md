# Feature 1.1 Critical/Medium 이슈 수정 — LLM 검증

## 검증 항목

- [x] LOGIC-001: `@tiptap/extension-underline`이 `package.json` dependencies에 ���가됨: `pnpm -C packages/angular add @tiptap/extension-underline` 실행 완료
- [x] LOGIC-001: `sd-tiptap-editor.ts`에서 `Underline`을 import하고 `DEFAULT_EXTENSIONS`에 포함: `import Underline from "@tiptap/extension-underline"` + 배열 마지막에 `Underline` 추가 확인
- [x] LOGIC-002: `useSheetCellAgent.ts:196`에서 `td.textContent ?? ""` null 방어 확인
- [x] LOGIC-003: `sd-textfield.ts:onInput`에서 파싱 실패 시 `inputEl.value = this.controlValue()` 추가 확인 (`onInputPaste`와 동일 패턴)
- [x] LOGIC-004: `sd-base-container.ts`에 `SdSystemLogProvider` inject 추가 + catch 블록에서 `void this._systemLog.writeAsync("warn", ...)` 호출 확인
- [x] DESIGN-001: `sd-global-error-handler.plugin.ts` catch 블록의 `appRef.destroy()`가 try-catch로 래핑됨 확인
- [x] DESIGN-002: `sd-shared-data.provider.ts`에 `generation` 카운터 추가, `_loadAndListen`에서 generation 캡처 후 비동기 결과 적용 전 검증, `_onEvent`에서 generation 불일치 시 이벤트 무시 확인. 공개 API(`register` 시그니처) 변경 없음 확인
