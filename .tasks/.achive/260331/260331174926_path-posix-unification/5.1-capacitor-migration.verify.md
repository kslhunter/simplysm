# capacitor.ts 마이그레이션 — LLM 검증

## 검증 항목
- [x] import 정리: `posixResolve`, `posix`가 `@simplysm/core-node`에서 import됨. `import path from "path"` 유지 (`path.dirname` 3건 사용)
- [x] 모든 path.resolve/path.join 교체: `grep "path\.(resolve|join)\("` 결과 0건. posixResolve 호출 39건
- [x] ad-hoc replace 교체: `sdkPath.replace(/\\/g, "/")` → `posix(sdkPath)` 교체 확인 (line 563)
- [x] Java 이스케이프 유지: `java21Path.replace(/\\/g, "\\\\")` 변경 없음 확인 (line 525)
- [x] 기존 테스트 40건 전체 통과 (5개 파일)
