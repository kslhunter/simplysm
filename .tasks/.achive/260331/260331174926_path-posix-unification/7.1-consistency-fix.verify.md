# Feature 7.1 path-posix-unification 잔존 일관성 수정 — LLM 검증

## Slice 1: vitest-plugin.ts normalizePath 대체

- [x] `pathx` import가 `@simplysm/core-node`에서 정상 추가됨: line 4 `import { pathx } from "@simplysm/core-node";` 확인
- [x] `normalizePath(filePath)` → `pathx.posix(filePath)` 변경됨: line 104 확인
- [x] `normalizePath(id)` → `pathx.posix(id)` 변경됨: line 114 확인
- [x] `normalizePath` 함수 정의 삭제됨: 파일에 `normalizePath` 문자열 존재하지 않음 확인
- [x] `path` import 유지됨: lines 59-60에서 `path.join()` 사용 중이므로 정상

## Slice 2: ngtsc-build.worker.ts watch 경로 통일

- [x] lines 236-248의 모든 `path.join()` → `pathx.posixResolve()` 변경 (8건): 확인
- [x] `pathx`는 이미 line 3에서 import 중: `import { createWorker, FsWatcher, pathx } from "@simplysm/core-node";` 확인
- [x] `path` import 유지됨: line 122에서 `path.join(info.pkgDir, "src")` 사용 중이므로 정상
- [x] library-build.worker.ts와 패턴 일치 확인: 동일하게 `pathx.posixResolve()` 사용
