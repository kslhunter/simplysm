# Feature 1.5 features/ 카테고리 생성 — LLM 검증

## 검증 항목

- [x] src/features/ 디렉토리 생성 및 4개 디렉토리 이동: `ls src/features/` → address/, editor/, theme/, visual/ 확인
- [x] 원래 위치에 디렉토리 없음: `ls src/` → controls/, core/, data/, features/, index.ts, layout/, scss.d.ts만 존재. visual/editor/address/theme/ 없음
- [x] 이동된 파일 내부 import 경로 수정: `grep -r 'from "\.\./core\|from "\.\./controls'` → 모두 `../../core/`, `../../controls/`로 변경 확인
- [x] core/provideSdAngular.ts theme 참조: `../features/theme/sd-theme-provider`로 변경 확인
- [x] index.ts re-export 경로: 4개 디렉토리 모두 `./features/{dir}/` prefix 확인
- [x] 구 경로 참조 잔존 여부: `grep -r 'src/(visual|editor|address|theme)/'` → 0건
- [x] tests/features/ 디렉토리 이동: address/, editor/, theme/, visual/ 확인
- [x] 테스트 import 경로 수정: 모두 `../../../src/features/` 또는 `../src/features/`로 변경 확인
- [x] tests/ 루트 spec 파일 theme 참조: 3개 파일 모두 `../src/features/theme/sd-theme-provider`로 변경 확인
- [x] CLAUDE.md 업데이트: 5개 카테고리(core, controls, layout, data, features) 구조로 업데이트 확인
- [!] 인라인 SCSS @use 경로: visual/ 내 4개 파일의 `@use "../../scss/"` → `@use "../../../scss/"`로 수정 필요 발견 → 수정 완료
- [x] sd-check 통과: typecheck 0에러, lint 0에러, test 146파일 1339건 전체 통과
