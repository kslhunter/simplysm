# Feature 2.2 EsbuildClientEngine 초기 빌드 warnings 테스트 추가

## 참조 자료

- WBS: [wbs.md](wbs.md)
- `packages/sd-cli/src/engines/EsbuildClientEngine.ts:129-151` — 초기 빌드 warnings → ResultCollector 저장 로직
- `packages/sd-cli/tests/engines/esbuild-client-engine.spec.ts` — 기존 테스트 (startWatch warnings 테스트 없음)

### 설계 결정

없음 (테스트 추가만)

## 요구명세

```gherkin
Feature: 2.2 EsbuildClientEngine 초기 빌드 warnings 테스트 추가

  Background:
    Given EsbuildClientEngine.startWatch()가 worker.startWatch() 결과를 처리한다
    And ResultCollector에 결과를 저장한다

  Rule: 초기 빌드 결과의 warnings가 ResultCollector에 올바르게 저장된다
    # 근거: 리뷰 DESIGN-003 "초기 빌드 warnings 전달에 대한 테스트 부재"

    Scenario: 성공 + warnings → ResultCollector에 success + warnings 저장
    Scenario: 실패 + warnings → ResultCollector에 error + message + warnings 저장
    Scenario: 성공 + warnings 없음 → ResultCollector에 추가 저장 없음
```

## 구현계획

### Vertical Slicing

#### [x] Slice 1: startWatch() warnings 테스트 3건 추가

- **구현 내용:** 기존 `esbuild-client-engine.spec.ts`에 3개 테스트 추가
- **Scenarios:** 모든 Scenario
