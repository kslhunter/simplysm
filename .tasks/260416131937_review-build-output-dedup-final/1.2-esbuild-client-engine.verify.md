# EsbuildClientEngine 상태 정리 — LLM 검증

## 검증 항목

- [x] resolveInitialBuild 구조 분해: `EsbuildClientEngine.ts:104` — `const { resolveInitialBuild } = setupWatchEvents(...)` 확인. setupWatchEvents 반환값에서 올바르게 추출 ✓
- [x] resolveInitialBuild 즉시 호출: `EsbuildClientEngine.ts:111` — `resolveInitialBuild()` 호출이 setupWatchEvents 직후, worker.startWatch() await 이전에 위치 ✓
- [x] resolveInitialBuild 동작: `engine-watch-events.ts:107-111` — `isInitialBuild`가 true일 때 false로 설정하고 `initialBuildResolve?.()` 호출. 아무도 대기하지 않으므로 무해 ✓
- [x] 후속 빌드 영향: resolveInitialBuild() 호출 후 isInitialBuild=false이므로, 후속 빌드의 "build" 이벤트 핸들러(line 80-83)에서 `if (isInitialBuild)` 분기 미진입 → 불필요한 initialBuildResolve 호출 방지 ✓
- [x] 기존 테스트 호환성: `esbuild-client-engine.spec.ts`에서 setupWatchEvents는 engine-watch-events 모듈 모킹을 통해 처리. 반환값을 사용하든 안 하든 기존 테스트에 영향 없음 ✓
