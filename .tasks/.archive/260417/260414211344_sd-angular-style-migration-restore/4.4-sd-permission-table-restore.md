# Feature 4.4 sd-permission-table 복원

## 참조 자료
- WBS: [wbs.md](./wbs.md)
- v12 원본: `D:\workspaces-12\simplysm\packages\sd-angular\src\features\permission-table\sd-permission-table.control.ts` (line 163: `[open]="getIsPermCollapsed(item)"`)
- v14 대상: `packages/angular/src/data/permission-table/sd-permission-table.ts` (line 155: `[open]="!getIsPermCollapsed(item)"`)

### 설계 결정
| # | 결정사항 | 선택 | 근거 |
|---|----------|------|------|
| — | 없음 | — | 단일 논리 반전으로 설계 결정 불필요 |

## 요구명세

```gherkin
Feature: 4.4 sd-permission-table [open] 논리 복원

  Background:
    Given sd-permission-table에 자식이 있는 2단계 계층 권한 데이터가 바인딩되어 있다

  Rule: collapse-icon open 바인딩은 getIsPermCollapsed(item) 값을 그대로 전달한다
    # 근거: v12 원본 sd-permission-table.control.ts:163

    Scenario: 초기 펼침 상태에서 아이콘이 회전하지 않는다
      Given 모든 항목이 펼쳐진 상태이다 (collapsedItems가 비어있음)
      When 테이블이 렌더링된다
      Then sd-collapse-icon의 open은 false이다 (회전 없음)

    Scenario: 접힌 상태에서 아이콘이 회전한다
      Given 부모 항목을 클릭하여 접은 상태이다
      When 테이블이 렌더링된다
      Then sd-collapse-icon의 open은 true이다 (90도 회전)
```

## 구현계획

### 배경
`sd-permission-table` 컴포넌트의 템플릿에서 `sd-collapse-icon`의 `[open]` 바인딩에 v14 마이그레이션 시 `!` 부정이 잘못 추가되어, 아이콘 회전 방향이 v12와 반대로 동작한다.

### 목표
- `[open]` 바인딩을 v12와 동일하게 복원 [근거: Rule "collapse-icon open 바인딩은 getIsPermCollapsed(item) 값을 그대로 전달"]

### 비목표
- `[icon]` prop 변경 — 이미 Feature 1.2에서 기본값 `tablerChevronDown` 복원 완료 [근거: wbs 경계]

### 설계
- `sd-permission-table.ts:155` — `!getIsPermCollapsed(item)` → `getIsPermCollapsed(item)`
- `permission-table.acc.spec.ts:50-73` — 기존 테스트의 기대값 반전 (펼침 → 미회전, 접힘 → 회전)

### 대안 검토
없음. 단일 논리 반전이므로 다른 접근 방식이 없다.

### Slice

#### Slice 1: [open] 논리 복원 및 테스트 갱신
- [x] **구현 내용:** 템플릿의 `!` 제거, 기존 테스트의 기대값 반전
- **Scenarios:** 초기 펼침 상태 아이콘 미회전, 접힌 상태 아이콘 회전
