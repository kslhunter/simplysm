# client.worker.ts catch 블록 개선 — LLM 검증

## 검증 항목

- [x] 초기 빌드 catch 시 `sender.send("error")` 미호출: `client.worker.ts:319` — `if (!isInitialBuild)` 조건 안에 `sender.send`가 있으므로, `isInitialBuild === true`일 때 `sender.send`는 실행되지 않음. `else` 분기에서 `initialBuildResolve`만 호출됨 ✓
- [x] 후속 빌드 catch 시 `sender.send("error")` 호출: `client.worker.ts:319-320` — `!isInitialBuild`이 true이면 `sender.send("error", { message })` 호출. `else` 분기 미진입으로 `initialBuildResolve` 미호출 ✓
- [x] catch에서 warnings 전달: `client.worker.ts:326-329` — `result.warnings.length > 0`이면 `result.warnings.map(formatEsbuildMessage)` 전달. 정상 경로(line 311-314)와 동일 패턴 ✓
- [x] catch에서 warnings 생략: `client.worker.ts:326-329` — `result.warnings.length === 0`이면 `undefined` 전달 ✓
- [x] `result` 접근 가능성: `result`는 `createDevBuildEndHandler` 반환 함수의 파라미터(line 262)이므로 catch 블록에서 항상 접근 가능 ✓
- [x] `isInitialBuild = false` 설정: `client.worker.ts:322` — `else` 분기(초기 빌드)에서 `isInitialBuild = false`를 `initialBuildResolve` 호출 전에 설정. 정상 경로(line 304)와 동일 순서 ✓
- [x] 정상 경로와의 패턴 일관성: catch 블록의 `if (!isInitialBuild) { ... } else { ... }` 구조가 정상 경로(line 288, 303)의 `if (!isInitialBuild) { ... }` / `if (isInitialBuild) { ... }` 분기와 동일한 의미론 ✓
