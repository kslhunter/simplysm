# Feature 1.1 files 필드 기반 화이트리스트 복사

## 참조 자료

- WBS: [wbs.md](./wbs.md)
- `packages/sd-cli/src/deps/replace-deps/replace-deps.ts` — 현재 복사 필터 로직
- `packages/sd-cli/src/deps/replace-deps/replace-deps-resolve.ts` — 교체 대상 해석 로직
- `packages/sd-cli/tests/utils/replace-deps.spec.ts` — resolveReplaceDepEntries, parseWorkspaceGlobs 테스트
- `packages/sd-cli/tests/utils/replace-deps-watch.spec.ts` — watchReplaceDeps onChanged 테스트
- `fsx.copy(source, target, filter)` — filter는 절대경로를 받으며, 디렉토리에서 false 반환 시 하위 전체 프루닝
- `FsWatcher.watch(paths, options)` — paths에 디렉토리/파일 경로 배열 전달 가능
- 전체 패키지 files 필드: 모두 단순 디렉토리명 (dist, src, scss, android, lib, scripts, claude, tests)

### 설계 결정

| # | 결정사항 | 선택 | 근거 |
|---|---------|------|------|
| D1 | 복사 필터 방식 | 화이트리스트 (files 필드 기반) | 사용자 제안, 불필요 파일(CLAUDE.md, tsconfig.json) 제거 |
| D2 | npm 기본 파일 포함 여부 | 포함 (README*, LICENSE*, CHANGELOG*) | 사용자 선택 |
| D3 | glob 패턴 지원 수준 | 단순 디렉토리/파일명 매칭만 | 현재 모든 패키지가 단순명 사용 |
| D4 | watch 감시 범위 | files 항목 경로만 감시 | 사용자 선택 "복사할 파일만 감시" |
| D5 | files 필드 미존재 시 | 경고 로그 후 건너뜀 | 사용자 선택 "files 필수 가정" |

## 요구명세

```gherkin
Feature: 1.1 files 필드 기반 화이트리스트 복사

  Background:
    Given 소스 패키지 디렉토리에 package.json이 존재한다
    And package.json에 files 필드가 ["dist", "src"]로 정의되어 있다
    And 소스 디렉토리에 dist/, src/, tsconfig.json, CLAUDE.md, node_modules/ 가 존재한다

  Rule: files 필드에 명시된 항목만 복사한다
    # 근거: wbs "소스 패키지의 package.json에서 files 필드를 읽어 복사 대상 화이트리스트 구성"
    Scenario: files에 포함된 디렉토리가 복사된다
      When setupReplaceDeps를 실행한다
      Then 대상 경로에 dist/ 와 src/ 디렉토리가 복사된다

    Scenario: files에 없는 파일은 복사되지 않는다
      When setupReplaceDeps를 실행한다
      Then 대상 경로에 tsconfig.json이 복사되지 않는다
      And 대상 경로에 CLAUDE.md가 복사되지 않는다
      And 대상 경로에 node_modules/가 복사되지 않는다

  Rule: npm 기본 포함 파일은 files 필드와 무관하게 복사된다
    # 근거: 사용자 선택 "npm 기본 파일도 포함"
    Scenario: README 파일이 복사된다
      Given 소스 디렉토리에 README.md가 존재한다
      When setupReplaceDeps를 실행한다
      Then 대상 경로에 README.md가 복사된다

    Scenario: LICENSE 파일이 복사된다
      Given 소스 디렉토리에 LICENSE가 존재한다
      When setupReplaceDeps를 실행한다
      Then 대상 경로에 LICENSE가 복사된다

  Rule: package.json은 항상 복사에서 제외된다
    # 근거: wbs "package.json 자체는 복사 제외 유지"
    Scenario: package.json이 복사되지 않는다
      When setupReplaceDeps를 실행한다
      Then 대상 경로의 기존 package.json이 보존된다

  Rule: files 필드가 없으면 경고 후 건너뛴다
    # 근거: 사용자 선택 "files 필수 가정"
    Scenario: files 필드 없는 패키지는 건너뛴다
      Given 소스 패키지의 package.json에 files 필드가 없다
      When setupReplaceDeps를 실행한다
      Then 해당 패키지에 대한 경고 로그가 출력된다
      And 해당 패키지의 복사가 수행되지 않는다

  Rule: watch 감시 대상은 files 항목 경로로 제한된다
    # 근거: 사용자 선택 "복사할 파일만 감시"
    Scenario: files에 포함된 디렉토리의 변경이 감지된다
      When watchReplaceDeps를 실행한다
      And src/index.ts 파일을 변경한다
      Then 변경이 감지되어 대상 경로에 복사된다

    Scenario: files에 없는 파일의 변경은 무시된다
      When watchReplaceDeps를 실행한다
      And tsconfig.json 파일을 변경한다
      Then 변경이 감지되지 않는다
```

## 구현계획

### 배경

`replace-deps.ts`는 `EXCLUDED_NAMES` Set과 `replaceDepsCopyFilter` 함수로 블랙리스트 기반 복사 필터를 구현. `setupReplaceDeps`는 `fsx.copy(source, target, filter)`로 전체 복사, `watchReplaceDeps`는 `FsWatcher.watch`로 전체 감시 후 `EXCLUDED_NAMES` 필터 적용.

`fsx.copy`의 filter는 절대경로를 받으며, 디렉토리에서 false 반환 시 하위 전체가 프루닝됨. 따라서 소스 루트의 직접 자식에 대해서만 화이트리스트 검사를 하면 하위는 자동으로 포함/제외됨.

### 목표

- `EXCLUDED_NAMES` 블랙리스트를 `files` 필드 기반 화이트리스트로 교체 [근거: Rule "files 필드에 명시된 항목만 복사"]
- npm 기본 파일(README*, LICENSE*, CHANGELOG* 등) 패턴 매칭 포함 [근거: Rule "npm 기본 포함 파일"]
- package.json 복사 제외 유지 [근거: Rule "package.json은 항상 제외"]
- files 미존재 시 경고 후 건너뜀 [근거: Rule "files 없으면 건너뜀"]
- watch 감시 대상을 files 항목으로 제한 [근거: Rule "watch 감시 대상 제한"]

### 비목표

- npm full glob 패턴 지원 [근거: wbs 경계 "glob 패턴 미지원"]
- `fsx.copy` 함수 자체 변경 [근거: wbs 경계]
- `replace-deps-resolve.ts` 변경 [근거: wbs 경계]

### 설계

**1. 화이트리스트 구성 함수** (`replace-deps.ts`)

```typescript
/** npm publish 시 files 필드와 무관하게 항상 포함되는 파일 패턴 (대소문자 무시) */
const NPM_DEFAULT_FILE_PATTERN = /^(readme|license|licence|changelog|history)/i;

/**
 * 소스 패키지의 package.json에서 files 필드를 읽어 반환한다.
 * files 필드가 없으면 undefined를 반환한다.
 */
async function loadFilesField(sourcePath: string): Promise<string[] | undefined> {
  const pkgJsonPath = pathx.posix(path.join(sourcePath, "package.json"));
  const pkgJson = JSON.parse(await fs.promises.readFile(pkgJsonPath, "utf-8"));
  return pkgJson.files as string[] | undefined;
}
```

**2. 복사 필터 팩토리** (`replace-deps.ts`)

```typescript
/**
 * files 필드 기반 화이트리스트 복사 필터를 생성한다.
 * 소스 루트 기준 상대경로의 첫 번째 세그먼트로 판단한다.
 *  - files에 포함 → 허용
 *  - npm 기본 파일 패턴 매칭 → 허용
 *  - package.json → 제외
 *  - 그 외 → 제외
 */
function createCopyFilter(
  sourcePath: string,
  allowedNames: Set<string>,
): (itemPath: string) => boolean {
  return (itemPath: string) => {
    const relativePath = path.relative(sourcePath, itemPath);
    const firstSegment = relativePath.split(path.sep)[0];

    if (firstSegment === "package.json") return false;
    if (allowedNames.has(firstSegment)) return true;
    if (NPM_DEFAULT_FILE_PATTERN.test(firstSegment)) return true;

    return false;
  };
}
```

핵심: `fsx.copy`의 프루닝 동작 덕분에 첫 번째 세그먼트만 검사하면 충분.

**3. setupReplaceDeps 변경**

- 각 entry 처리 전에 `loadFilesField(resolvedSourcePath)` 호출
- files 없으면 `logger.warn` 후 `continue`
- `createCopyFilter(resolvedSourcePath, new Set(files))` 생성
- `fsx.copy(resolvedSourcePath, actualTargetPath, filter)` 호출

**4. watchReplaceDeps 변경**

- 각 entry 처리 전에 `loadFilesField(resolvedSourcePath)` 호출
- files 없으면 경고 후 `continue`
- 감시 대상 경로 구성:
  - `files.map(f => pathx.posix(path.join(sourcePath, f)))` — files 항목
  - 소스 루트에서 `NPM_DEFAULT_FILE_PATTERN`에 매칭되는 파일을 `fs.promises.readdir`로 찾아 추가
- `FsWatcher.watch(watchPaths, { followSymlinks: false })` — `ignored` 옵션 제거
- onChange 핸들러: `EXCLUDED_NAMES` 기반 필터 체크 제거 (감시 대상 자체가 이미 화이트리스트)
- onChange 핸들러의 `fsx.copy` 호출에서 `replaceDepsCopyFilter` 제거 (단일 파일 복사이므로 filter 불필요)

**5. 삭제 대상**

- `EXCLUDED_NAMES` 상수
- `replaceDepsCopyFilter` 함수

### 대안 검토

| 접근법 | 선택여부 | 이유 |
|--------|---------|------|
| 전체 감시 + onChange 필터링 | 미선택 | 불필요한 파일 변경에 반응, 사용자가 "복사할 파일만 감시" 선택 |
| npm packlist 라이브러리 사용 | 미선택 | 외부 의존성 추가 불필요, 단순 매칭으로 충분 |

### Vertical Slicing

#### [x] Slice 1: 화이트리스트 필터 구현 및 setupReplaceDeps 적용

- **구현 내용:**
  - `NPM_DEFAULT_FILE_PATTERN` 상수 추가
  - `loadFilesField()` 함수 구현
  - `createCopyFilter()` 팩토리 함수 구현
  - `EXCLUDED_NAMES`, `replaceDepsCopyFilter` 제거
  - `setupReplaceDeps`에서 entry별 files 로드 → 필터 생성 → 복사
  - files 미존재 시 경고 후 건너뜀
- **변경 파일:** `packages/sd-cli/src/deps/replace-deps/replace-deps.ts`
- **테스트 파일:** `packages/sd-cli/tests/deps/replace-deps/replace-deps-setup.spec.ts` (신규)
- **Scenarios:** files 필드 항목 복사, 비허용 파일 제외, npm 기본 파일 복사, package.json 제외, files 미존재 건너뜀

#### [x] Slice 2: watchReplaceDeps 감시 대상 변경

- **의존:** Slice 1
- **구현 내용:**
  - watch 대상 경로를 files 항목 + npm 기본 파일로 변경
  - `EXCLUDED_NAMES` 기반 ignored/필터 로직 제거
  - onChange 핸들러에서 불필요한 필터 체크 제거
  - files 미존재 시 경고 후 건너뜀
- **변경 파일:** `packages/sd-cli/src/deps/replace-deps/replace-deps.ts`
- **테스트 파일:** `packages/sd-cli/tests/utils/replace-deps-watch.spec.ts` (기존 수정)
- **Scenarios:** files 디렉토리 변경 감지, 비허용 파일 변경 무시
