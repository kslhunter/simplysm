# Feature 1.1 QueryableRecord 타입 통일 — LLM 검증

## 검증 항목

- [x] NullableQueryableRecord 타입이 src/에서 완전 제거됨: `Grep "NullableQueryableRecord" src/` → No matches found
- [x] nn() 함수가 src/에서 완전 제거됨: `Grep "export.*nn" src/` → No matches found
- [x] __nnOriginalData symbol이 src/에서 완전 제거됨: 위 검색에 포함 확인
- [x] index.ts에서 별도 export 조치 불필요: `export * from "./exec/queryable"` 로 자동 처리됨
