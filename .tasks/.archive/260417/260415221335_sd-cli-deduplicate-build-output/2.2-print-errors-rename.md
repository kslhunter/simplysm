# Feature 2.2 printErrors 함수명 변경

## 참조 자료

- WBS: [wbs.md](wbs.md)
- `packages/sd-cli/src/utils/output-utils.ts:31` — `printErrors()` 함수 정의
- `packages/sd-cli/src/orchestrators/DevOrchestrator.ts:9,220,262` — import + 호출 2곳
- `packages/sd-cli/src/orchestrators/WatchOrchestrator.ts:9,79,139` — import + 호출 2곳
- `packages/sd-cli/tests/utils/output-utils.spec.ts` — 테스트
- `packages/sd-cli/tests/orchestrators/dev-orchestrator.spec.ts:21,120,400` — mock + import + assert
- `packages/sd-cli/tests/orchestrators/watch-orchestrator.spec.ts:21,107,319,329` — mock + import + assert

### 설계 결정

| # | 결정사항 | 선택 | 근거 |
|---|---------|------|------|
| D1 | 새 함수명 | `printDiagnostics` | 에러+경고를 모두 포괄. `printBuildMessages`는 기존 `formatBuildMessages`와 혼동 가능 |

## 요구명세

```gherkin
Feature: 2.2 printErrors 함수명 변경

  Rule: printErrors의 모든 참조를 printDiagnostics로 변경한다
    # 근거: 리뷰 DESIGN-001 "함수명이 실제 동작과 불일치"

    Scenario: 함수 정의 이름 변경
      Given output-utils.ts에 printErrors 함수가 정의되어 있다
      When 함수명을 printDiagnostics로 변경한다
      Then export된 함수명이 printDiagnostics이다
      And 함수의 동작은 변경되지 않는다

    Scenario: 호출처 업데이트
      Given DevOrchestrator와 WatchOrchestrator가 printErrors를 import하여 호출한다
      When 함수명이 printDiagnostics로 변경된다
      Then 모든 import문과 호출이 printDiagnostics를 참조한다

    Scenario: 기존 테스트가 모두 통과한다
      Given printErrors를 참조하는 테스트 파일이 3개 있다
      When 모든 참조를 printDiagnostics로 변경한다
      Then 기존 테스트가 모두 통과한다
```

## 구현계획

### 배경

Feature 1.1에서 경고 출력 기능이 추가되었으나 함수명이 `printErrors`로 남아 의미가 불일치한다. [근거: 리뷰 DESIGN-001]

### 목표

- 함수명을 실제 동작(에러+경고 출력)에 맞게 `printDiagnostics`로 변경한다 [근거: Rule "모든 참조를 변경"]

### 비목표

- 함수 동작 변경 [근거: wbs 경계 "이름만 변경"]

### 설계

전체 파일 대상 `printErrors` → `printDiagnostics` 일괄 치환:

| 파일 | 변경 내용 |
|------|----------|
| `output-utils.ts:31` | 함수 정의명 변경 |
| `DevOrchestrator.ts:9,220,262` | import + 호출 2곳 |
| `WatchOrchestrator.ts:9,79,139` | import + 호출 2곳 |
| `output-utils.spec.ts` | import + describe 내 호출 |
| `dev-orchestrator.spec.ts` | mock + import + assert |
| `watch-orchestrator.spec.ts` | mock + import + assert |

### 대안 검토

없음. 단순 리네이밍이므로 대안 불필요.

### Vertical Slicing

#### [x] Slice 1: 일괄 리네이밍

- **구현 내용:** 소스+테스트 전체에서 `printErrors` → `printDiagnostics` 치환
- **Scenarios:** 모든 Scenario
