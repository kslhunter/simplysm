# Feature 1.1 DOC-002 + LOGIC-004 주석 수정 — LLM 검증

## 배경

주석 텍스트만 수정하는 Feature이므로 자동 테스트(`.spec.ts`)로 검증할 동작 변화가 없다.
정적 코드 확인과 `grep`, 기존 테스트 회귀로 실증한다.

## 검증 항목

- [x] **DOC-002-1**: `packages/sd-cli/src/esbuild/esbuild-worker-plugin.ts:218` JSDoc 문구 교체 확인
  - 현재 라인: `* Angular 컴파일러 플러그인 등 외부에서 직접 호출한다.`
  - `(D2)` 참조 제거됨, 문구 자연화됨

- [x] **DOC-002-2**: `packages/sd-cli/src/esbuild/esbuild-angular-compiler-plugin.ts:548` 인라인 주석 교체 확인
  - 현재 라인: `// Worker 패턴 처리`
  - `(D2)` 참조 제거됨

- [x] **DOC-002-3**: `packages/sd-cli/src/esbuild/` 하위 전체 `(D2)` 잔존 0건 확인
  - 실행: Grep pattern `\(D2\)` path `packages/sd-cli/src/esbuild`
  - 결과: `No matches found`

- [x] **LOGIC-004**: `esbuild-worker-plugin.ts:231-232` 사전 필터 주석에 트레이드오프 2줄 추가 확인
  - 추가된 문구 (sd-inner-review 피드백 반영: 조사 "과"→"와" 교정, 인접 주석과 중복되는 "정확성은 findWorkerPatterns가 담당한다" 구 축약):
    ```
    // new 와 Worker/SharedWorker 사이에 주석이 낀 호출(예: `new /* c */ Worker`)은 의도된
    // 트레이드오프로 탈락한다. 실발생 가능성이 희박하여 현행을 유지한다.
    ```
  - 정규식 본문(`esbuild-worker-plugin.ts:233`)은 `if (!/\b(new\s+Worker|new\s+SharedWorker|import\.meta\.resolve)\b/.test(content)) {` 로 변경 없음

- [x] **회귀**: 기존 78건 테스트 전부 통과
  - 실행: `pnpm vitest run packages/sd-cli/tests/esbuild/esbuild-worker-plugin.spec.ts packages/lint/tests/ts-no-unused-protected-readonly.spec.ts`
  - 결과: `Test Files 2 passed (2)`, `Tests 78 passed (78)` — 회귀 0건
  - (테스트 실행 로그에 나타난 "X [ERROR]" 2건은 `worker-error.js` 픽스처를 의도적으로 파싱 실패시키는 에러 처리 시나리오에서 발생한 것으로 기존부터 존재하던 표시이다)
