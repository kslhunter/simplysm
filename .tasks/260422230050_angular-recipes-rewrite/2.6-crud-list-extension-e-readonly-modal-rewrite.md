# Feature 2.6 `crud-list/extension-e-readonly-modal.md` 재작성

## 참조 자료

- WBS: @.tasks/260422230050_angular-recipes-rewrite/wbs.md (Feature 2.6)
- 지침: @.claude/skills/sd-claude-docs/references/package-docs.md — T1(Recipe) 템플릿, 반-기법, 매뉴얼 품질 검증 축
- 선행 Feature 산출물:
  - 공통 규칙: @packages/angular/docs/recipes/_common-rules.md (Feature 1.1 완료본)
  - 진입점(최소 뼈대): @packages/angular/docs/recipes/crud-list.md (Feature 2.1 완료본)
- 원문: @packages/angular/docs/recipes/crud-list/extension-e-readonly-modal.md (129줄)
- sibling 템플릿 기준: @packages/angular/docs/recipes/crud-list/extension-d-select-modal.md (125줄, diff 스타일)
- 소스 정합성:
  - `SdModalProvider.showAsync<T>(modal: SdModalInfo<T>, options?)`: `packages/angular/src/core/modal/sd-modal.provider.ts:85`
  - `SdModalInfo<T>.inputs`: `packages/angular/src/core/modal/sd-modal.provider.ts:43-50` — `Omit<DirectiveInputSignals<T>, ...>` 형태로 input을 객체로 전달
  - `input.required<T>()` / `input<T | undefined>()`: `@angular/core` signal input API
  - `SdSelectModal<T>` / `SelectModalOutputResult<T>`: `packages/angular/src/core/select-modal-output-result.ts:4`
  - `injectViewTypeSignal`: `packages/angular/src/core/routing/injectViewTypeSignal.ts:7`

## 기준 코드

(이 Feature 구현 시 따라야 할 기존 문서 패턴)

### 유사 기능: 이미 재작성된 sibling `extension-d-select-modal.md`

- 파일 구조: `packages/angular/docs/recipes/crud-list/extension-d-select-modal.md:1-125`
  - L1: `← [CRUD 리스트 레시피 진입점](../crud-list.md)` 브레드크럼
  - L3: `# 확장 {레터}: {이름}` 제목
  - L5: `> **선행:** …` 블록쿼트
  - L7: 1~2문장 요약(+ 형제 확장과 계약 차이 1-hop 링크)
  - L9: `**이 확장이 도입하는 요소:**` 헤더 + 불릿 (imports / 계약 / 생성자 effect / 메서드 / 템플릿 변경)
  - L22-108: 번호가 붙은 코드 블록(`// 1) imports 추가`, `// 2) …`)
  - L110-118: `**포인트:**` 불릿
  - L120-124: `## Cross-reference` — 진입점·선행·관련 확장 1-hop
- 1-level deep 링크 패턴: 진입점/형제 확장 1-hop만 허용, 형제의 형제로 체이닝 금지

### 진입점 2.1(최소 뼈대) 기준 일치 대상

- `packages/angular/docs/recipes/crud-list.md:35-302` — 확장 E는 최소 뼈대에 **부모 식별자 input + `_search` where절**만 얹는 diff로 기술. 이외 구조(imports·권한·signal 필드·effect·`_refresh` 서명)는 최소 뼈대와 정합해야 한다
- 진입점 L194-205 signal 필드 구성: 확장 E는 여기에 `customerId = input.required<number>()` 등 부모 식별자 input 1개만 추가
- 진입점 L213-234 초기 effect: 확장 E는 의존성 목록에 `this.customerId();` 한 줄만 추가
- 진입점 L256-294 `_search`: 확장 E는 `db.customer()` → `db.customerOrder().where((item) => [expr.eq(item.customerId, customerId)])`로 쿼리 시작을 변경

### 공통 규칙 반영

- `_common-rules.md` "🚫 signal 필드 초기값에서 다른 signal을 읽지 않는다" 섹션 존재(L174-194) — 확장 E는 부모 식별자 input을 signal 초기값에서 읽기 쉽다. 본문에 재서술하지 않고 **❌/✅ 1-hop 링크**로 위임한다
- `_common-rules.md` "✅ input 변경을 effect 내부에서 filter·lastFilter·page에 반영한다" 섹션(L77-107) — 확장 E의 `customerId` input 반영 패턴도 여기에 해당. 포인트에서 1-hop 링크로 위임
- `_common-rules.md` "🚫 input 의존 데이터 로딩에 `void this._initAsync()`를 사용하지 않는다"(L150-172) — 본 확장 E는 공통 규칙 "대안" 서술에서 직접 이름이 거명된 대표 사례. 본문에서 재서술 없이 링크만 제공

### 프로젝트 컨벤션

- 3인칭 서술, 단정형("추가한다", "교체한다") [근거: `package-docs.md:22`, sibling `extension-d:110-118` 포인트 문체]
- 링크 깊이 1-level deep [근거: `package-docs.md:91,198`]
- ❌/✅ 코드블록 나란히 + 근거 1줄 [근거: `package-docs.md:190,320-332`, `_common-rules.md` T3 형식]
- 형제 확장 링크는 `./extension-*.md`, 진입점은 `../crud-list.md`, 공통 규칙은 `../_common-rules.md` 상대 경로 [근거: `extension-d:120-124` Cross-reference]
- Component 공통 패턴: `OnPush` / `standalone: true` / signal input [근거: `packages/angular/CLAUDE.md:91-108`]

### 설계 결정

| #  | 결정사항                                                     | 선택                                                                                                                                   | 근거                                                                                                                        |
| -- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| D1 | extension recipe의 전체 템플릿                               | **sibling `extension-d` diff 스타일 유지**(브레드크럼 / 제목 / 선행 / 요약 / 도입 요소 / 번호 코드 / 포인트 / Cross-reference)             | sibling 2.5(확장 D)가 동일 폴더에서 이미 diff 스타일로 확정. T1 진입점 템플릿은 2.1 진입점 전용. Feature 3.2 D1과 동일 판단 |
| D2 | WBS "When to use" 섹션 배치                                  | **최상단 요약 문단 뒤에 짧은 ✅/❌ 2~3줄 가이드로 inline 배치** (별도 `##` 섹션 승격하지 않음)                                           | sibling `extension-d`도 진입점 When to use를 별도 승격하지 않고 요약 + 1-hop으로 처리. 확장 파일의 경량 템플릿 유지          |
| D3 | "✅ 조회 전용 modal" vs "확장 D 선택 모달" 선택 기준 배치    | **요약 문단 + ❌/✅ Anti-patterns 섹션 양쪽에 서술**: 요약 문단에는 판단 기준 1줄, Anti-patterns 섹션에는 "선택 계약 반사 부착" ❌/✅ 블록 | 진입점 2.1에 이미 동일 anti-pattern이 있으나 확장 E 파일 단독 로드 시 정보 유실 방지 (self-contained 원칙)                  |
| D4 | "signal 필드 초기값에서 input signal 읽기 금지" 안티패턴     | **신규 `## 🚫 흔한 실수` 서브섹션에 ❌/✅ 블록 1건 추가 + `_common-rules.md` 1-hop 링크**                                               | WBS 2.6 범위에 명시("❌/✅ signal 초기값에서 input signal 읽기 금지, effect 기반 input 반영")                                |
| D5 | 부모 식별자 input 타입(`input.required<T>()` vs `input<T \| undefined>()`) | **양쪽 모두 노출** — 기본은 `input.required<number>()`, 선택적으로 `input<number \| undefined>()`도 포인트 불릿에 언급                 | 현행 문서(L118)가 이미 두 경로를 모두 노출하고, 호출측 계약 설계 맥락에서 필요                                              |
| D6 | 도메인 예시명                                                | `CustomerOrderHistoryModal` / `customerId` / `db.customerOrder()` 유지                                                                  | 현행 문서에서 사용 중이며 진입점 2.1 `ICustomer`와 연속성(부모-자식). Feature 3.2 D7과 동일 판단                             |
| D7 | `viewType()` 분기 사용 여부 서술                             | **포인트 불릿로 유지** — "page/modal 겸용이 아니면 `viewType()` 분기 제거 가능" 1줄만                                                   | 과도 승격은 반-기법 "Positive-only 지침" 위반. 선택 사항임을 명확히 전달                                                    |
| D8 | 코드 예시 범위                                               | **diff만 노출** — 진입점 최소 뼈대에서 실제 변경되는 5개 지점만(클래스 선언, input, effect 의존성, `_search` where절, template)         | self-contained 완전 컴포넌트 재복사는 진입점과 중복 → 반-기법 회피                                                          |

## 요구명세

```gherkin
Feature: 2.6 crud-list/extension-e-readonly-modal.md 재작성

  Background:
    Given 이 문서의 주 독자는 소비 프로젝트의 Claude Code(LLM 에이전트)이다
    And 타겟 파일은 packages/angular/docs/recipes/crud-list/extension-e-readonly-modal.md이다
    And Feature 1.1의 `_common-rules.md`가 T3 형식으로 존재한다
    And Feature 2.1의 `crud-list.md` 진입점이 T1 형식으로 존재한다
    And sibling Feature 2.5의 `extension-d-select-modal.md`가 diff 스타일 템플릿으로 존재한다

  Rule: extension recipe는 sibling과 동일한 diff 스타일 템플릿을 따른다
    # 근거: 설계 결정 D1, sibling `extension-d-select-modal.md:1-125`

    Scenario: 파일 상단 7개 구성요소가 순서대로 있다
      Then 1행에 진입점으로의 브레드크럼(`← [CRUD 리스트 레시피 진입점](../crud-list.md)`)이 있다
      And `# 확장 E: 조회 전용 modal` 제목이 있다
      And `> **선행:** 없음 (최소 뼈대 §3에 직접 얹음 — 확장 A/B/D 미사용)` 블록쿼트가 있다
      And 1~2문장 요약 문단 + ✅/❌ 판단 기준(선택 모달 확장 D와의 차이)이 있다
      And `**이 확장이 도입하는 요소:**` 불릿 블록이 있다
      And 번호 주석(`// 1)`, `// 2)`, …)이 붙은 TypeScript 코드 블록이 있다
      And 코드 블록 뒤 `**포인트:**` 불릿이 있다
      And `## 🚫 흔한 실수` 서브섹션이 있다
      And `## Cross-reference` 섹션이 마지막에 있다

    Scenario: self-contained 컴포넌트를 중복 복사하지 않는다
      Then 코드 블록은 진입점 최소 뼈대에서 **변경·추가되는 diff**만 노출한다
      And 진입점과 동일한 signal 필드·메서드 본문을 재서술하지 않는다

  Rule: 진입점 2.1 최소 뼈대와 정합한다
    # 근거: WBS "최소 뼈대 §3에 직접 얹음", 진입점 2.1 L35-302

    Scenario: 클래스 선언에서 SdSelectModal 계약 부재
      Then 재작성된 예시 클래스는 `implements SdSelectModal<T>`를 포함하지 않는다
      And `selectMode` / `selectedItemKeys` input, `close` output 선언이 없다
      And `cumulativeSelection` / `selectedItems` / 하단 확인 바 관련 template 조각이 없다

    Scenario: 부모 식별자 input 추가
      Then `input.required<number>()` 형태의 부모 식별자 input 1개가 클래스 상단에 선언된다
      And 동일 지점에 `input<number | undefined>()` 선택지가 포인트 불릿에 언급된다
      And `@angular/core`에서 `input` import 추가 diff가 있다

    Scenario: 초기 effect 의존성에 input 추가
      Then 진입점 최소 뼈대 effect의 의존성 블록에 `this.customerId();` 한 줄을 추가하는 diff가 있다
      And 기존 `lastFilter()` / `page()` / `sortingDefs()` 의존성은 유지된다

    Scenario: _search where절에 하드 필터 반영
      Then `_search` 내부에 `const customerId = this.customerId();`가 추가된다
      And 쿼리 시작이 `db.customer()` → `db.customerOrder().where((item) => [expr.eq(item.customerId, customerId)])`로 변경된다
      And 기존 `filter.searchText` 기반 `search()` 호출은 AND 조건으로 병합된 형태로 유지된다

    Scenario: template에서 선택/편집/하단 바 조각 제거
      Then `<sd-sheet>`에 `selectMode` / `[(selectedItems)]` / `cumulativeSelection` 바인딩이 없다
      And 시트 셀은 `<ng-template [cell]>` 내부에서 `{{ item.name }}` 같은 순수 표시만 포함한다
      And 하단 `<sd-dock [position]="'bottom'">` 확인 바 블록이 없다
      And 상단 inline 도구(등록/선택 삭제·복구) dock 블록도 없다

  Rule: When to use — 조회 전용 modal 선택 기준이 명시된다
    # 근거: WBS 2.6 범위 "'When to use' — 조회 전용 modal 선택 기준", 설계 결정 D2/D3

    Scenario: 요약 문단에 판단 기준이 포함된다
      Then 요약 문단에 "닫기 = SdModal 기본 '{X}'" 및 "선택·확정·저장·이탈 방지가 불필요하다" 핵심 문장이 있다
      And ✅ 조회 전용 사용 상황(부모 레코드 자식 목록·이력 표시)과 ❌ 항목 선택이 필요한 경우(→ 확장 D) 구분이 1줄씩 제공된다
      And 확장 D([extension-d-select-modal.md](./extension-d-select-modal.md))와 확장 F([extension-f-modal-edit.md](./extension-f-modal-edit.md))로의 1-hop 링크가 있다

  Rule: 호출 측 예제 유지
    # 근거: 현행 문서 L9-17, 설계 결정 D8

    Scenario: SdModalProvider.showAsync 호출 예
      Then 상단 요약 근처에 `await this._sdModal.showAsync({ title, type, inputs: { customerId: 123 } })` 호출 예가 보존된다
      And `SdModalInfo<T>.inputs`의 형태(객체로 input 전달)가 소스와 일치한다(`packages/angular/src/core/modal/sd-modal.provider.ts:43-50`)

  Rule: ❌/✅ "signal 초기값에서 input signal 읽기 금지" 안티패턴이 추가된다
    # 근거: WBS 2.6 범위 "❌/✅ signal 초기값에서 input signal 읽기 금지, effect 기반 input 반영", 설계 결정 D4

    Scenario: 🚫 흔한 실수 서브섹션에 항목 추가
      Then `## 🚫 흔한 실수` 섹션이 있다
      And "### signal 필드 초기값에서 부모 식별자 input 읽기" anti-pattern이 포함된다
      And ❌ 예시(`filter = signal({ customerId: this.customerId() })` 같은 초기값 의존)와 ✅ 예시(초기값은 기본값, 반영은 effect)가 나란히 제시된다
      And `→ [공통 규칙: signal 필드 초기값에서 다른 signal을 읽지 않는다](../_common-rules.md#signal-필드-초기값에서-다른-signal을-읽지-않는다)` 1-hop 링크가 있다

  Rule: ❌/✅ "modal = 선택 계약 반사 부착" 안티패턴이 포함된다
    # 근거: 설계 결정 D3, 원문 L117 포인트 1번의 ❌/✅ 블록 승격

    Scenario: 🚫 흔한 실수 섹션에 두 번째 항목 존재
      Then "### modal = 선택 계약으로 반사 부착" anti-pattern이 포함된다
      And ❌ 예시(`implements SdSelectModal<T>` + `selectMode` / `selectedItemKeys` 부착)와 ✅ 예시(계약 없이 부모 식별자 input만)가 나란히 제시된다
      And 근거 1줄과 확장 D로의 1-hop 링크가 포함된다

  Rule: 공통 규칙 중복을 금지한다
    # 근거: Feature 1.1 T3 `_common-rules.md` 위임 원칙

    Scenario: 중복된 규칙 본문을 포함하지 않는다
      Then `void this._initAsync()` 금지, signal 초기값 금지 본문, input effect 반영 본문은 본문에 재서술하지 않는다
      And 필요한 곳에서 `_common-rules.md` 링크로 1-hop 위임한다

  Rule: 시그니처 정합성을 재검증한다
    # 근거: package-docs.md "정확성 검증"

    Scenario: @simplysm/angular import가 소스와 일치한다
      Then `@angular/core`의 `input` import가 있다
      And `SdModalProvider` 호출 예의 `inputs` 필드 형태가 `packages/angular/src/core/modal/sd-modal.provider.ts:46`의 `WithOptional<Omit<DirectiveInputSignals<T>, ...>, ...>`와 정합한다
      And 본문에서 사용한 `implements SdSelectModal<T>` 이름 표기가 `packages/angular/src/core/select-modal-output-result.ts`와 일치한다

  Rule: 3인칭 서술·반-기법 회피
    # 근거: package-docs.md 매뉴얼 품질 검증

    Scenario: 1·2인칭 서술이 없다
      Then "you can", "i can", "당신이", "여러분" 같은 표현이 없다
      And "추가한다", "교체한다", "부착한다" 형태로 서술한다

    Scenario: CRITICAL 남발이 없다
      Then CRITICAL / ALWAYS / NEVER 키워드가 2회 이하로 등장한다
```

## 구현계획

### 배경

현행 `packages/angular/docs/recipes/crud-list/extension-e-readonly-modal.md`(129줄)는 diff 스타일 템플릿(브레드크럼 / 제목 / 선행 / 요약 / 도입 요소 / 번호 코드 / 포인트 / Cross-reference)을 이미 따르고 있다. 그러나 (a) "조회 전용 modal vs 선택 모달(확장 D)" 선택 기준이 요약 문단에 산문으로만 섞여 있고, (b) `_common-rules.md`의 "signal 초기값에서 input signal 읽기 금지"와 "input 변경 effect 반영" 규칙을 본문 포인트에서 산문으로 중복 설명하며 ❌/✅ 블록은 없고, (c) "modal = 선택 계약 반사 부착 금지"도 포인트 산문 1항목으로만 서술되어 시각 식별성이 낮다. 본 Feature는 **단일 파일 전면 재작성**이며, sibling Feature 2.5(`extension-d-select-modal.md`) 템플릿과 Feature 1.1 완료본(`_common-rules.md`)을 기준으로 구조·링크·안티패턴 노출 방식만 정돈한다. 코드 diff 자체(부모 식별자 input, effect 의존성, `_search` where절)는 소스 정합 검증을 거쳐 보존한다.

### 목표

- sibling extension-d와 동일한 diff 스타일 템플릿 유지(브레드크럼 / 제목 / 선행 / 요약 / 도입 요소 / 번호 코드 / 포인트 / Cross-reference) [근거: Rule "sibling과 동일한 diff 스타일 템플릿", D1]
- 요약 문단에 ✅/❌ 판단 기준(조회 전용 vs 확장 D 선택 모달) 1줄 inline 배치 [근거: Rule "When to use", D2/D3]
- 진입점 2.1 최소 뼈대와 diff 정합(부모 식별자 input 1개 추가, effect 의존성에 `this.customerId();` 한 줄 추가, `_search` where절에 하드 필터) [근거: Rule "진입점 2.1 … 정합"]
- `## 🚫 흔한 실수` 서브섹션 신설 — "signal 초기값에서 input 읽기"와 "선택 계약 반사 부착" 두 항목을 ❌/✅ 나란히 블록 + `_common-rules.md` / 확장 D 1-hop 링크로 전환 [근거: Rule "❌/✅ signal 초기값…", Rule "❌/✅ modal = 선택 계약…", D3/D4]
- `_common-rules.md` 중복 본문 제거 및 1-hop 링크 위임(`void this._initAsync()` 금지, signal 초기값 금지, input effect 반영) [근거: Rule "공통 규칙 중복 금지"]
- `@simplysm/angular` / `@angular/core` 소스 시그니처 재검증 [근거: Rule "시그니처 정합성"]
- 3인칭 서술 통일 [근거: Rule "3인칭 서술"]
- 분량: 129줄 → 대략 130~160줄 범위(🚫 섹션 2건 추가, 중복 산문 축약)

### 비목표

- 본 확장의 **동작 변경**(부모 식별자 input 타입, `_search` where절 형태, 닫기 방식) — 현행 설계 유지 [근거: WBS 2.6 범위에 동작 변경 없음]
- 확장 A(inline 편집) / B(선택) / C(inline 삭제) / D(선택 모달) / F(모달 편집) / G(엑셀) 파일 수정 — 각자 Feature 2.2~2.8 범위
- 진입점 2.1 `crud-list.md` 수정 — Feature 2.1에서 확정 완료
- `_common-rules.md` 내용 개정 — Feature 1.1에서 확정 완료
- 신규 anti-pattern 발굴(WBS 명시 2건 외) [근거: 반-기법 "과도한 안티패턴 섹션" 회피]

### 설계

#### 파일 구조(sibling `extension-d`와 동일 템플릿)

```markdown
← [CRUD 리스트 레시피 진입점](../crud-list.md)

# 확장 E: 조회 전용 modal

> **선행:** 없음 (최소 뼈대 §3에 직접 얹음 — 확장 A/B/D 미사용)

{1~2문장 요약: 부모 식별자 input으로 자식 목록·이력을 읽기 전용 표시, 닫기는 SdModal 기본 "X"}

- ✅ 부모 레코드의 자식 목록·이력만 필터링해서 읽기 전용으로 표시할 때
- ❌ 항목을 골라 호출자에게 돌려줘야 할 때 → [확장 D: 선택 모달](./extension-d-select-modal.md)
- ❌ 부모 레코드에 대한 inline 편집·삭제가 필요할 때 → [확장 A](./extension-a-inline-edit.md)(+B) 또는 [확장 F: 모달 편집](./extension-f-modal-edit.md)

호출 예:

\`\`\`typescript
await this._sdModal.showAsync({
  title: "고객 주문 이력",
  type: CustomerOrderHistoryModal,
  inputs: { customerId: 123 },
});
\`\`\`

**이 확장이 도입하는 요소:**

- **imports:** `input`(`@angular/core`) 추가
- **input:** 부모 식별자(예: `customerId = input.required<number>()`)
- **초기 effect 의존성:** 부모 식별자 input 추가
- **`_search` 변경:** where절에 부모 식별자 하드 필터(`expr.eq(...)`), 기존 `filter.searchText` 조건과 AND
- **SdSelectModal<T> 계약 / selectedItems / 하단 바 / cumulativeSelection / canEdit / diffs / setupCanDeactivate / `<sd-form #formCtrl>` 래퍼 등은 부착하지 않는다**

\`\`\`typescript
// 1) imports 추가
// 2) 클래스 선언 — implements 없음
// 3) 부모 식별자 input
// 4) 초기 effect 의존성에 input 추가
// 5) _search where절에 부모 식별자 하드 필터
// 6) template — <sd-sheet>에 selectMode / selectedItems / cumulativeSelection 미사용, 셀은 순수 표시
\`\`\`

**포인트:**

- **부모 식별자는 호출자가 `inputs`으로 전달한다.** 값이 반드시 주어져야 하면 `input.required<T>()`, 없으면 전체 조회로 fallback하는 설계면 `input<T | undefined>()`. 어느 쪽이든 `_search`의 `where` 절과 초기 effect 의존성에 반드시 포함시킨다.
- **닫기 = SdModal 기본 "X".** `close` output이 없으므로 `SdModalProvider.showAsync`의 리턴값도 사용하지 않는다. 호출 측은 `await showAsync(...)` 결과를 버린다.
- **시트는 읽기 전용.** `[cell]` 템플릿에 `<sd-textfield>` 대신 `{{ item.name }}` 같은 순수 표시만 쓰고, `let-edit="edit"` / `[readonly]="!edit"` / `(valueChange)="mark(items)"`는 제거. `canEdit` / `diffs` / `_itemsSnapshot` / `setupCanDeactivate`도 함께 제거.
- **`viewType()` 분기는 쓰지 않아도 된다.** 조회 전용 modal은 보통 modal 전용으로 라우트 없이 등록된다. page/modal 양쪽을 모두 지원해야 할 때만 `viewType()`으로 topbar 영역을 분기한다.
- **input 반영은 effect 내부에서 처리한다.** → [공통 규칙: input 변경을 effect 내부에서 filter·lastFilter·page에 반영한다](../_common-rules.md#input-변경을-effect-내부에서-filterlastfilterpage에-반영한다)
- **`void this._initAsync()` 금지.** → [공통 규칙: input 의존 데이터 로딩에 `void this._initAsync()`를 사용하지 않는다](../_common-rules.md#input-의존-데이터-로딩에-void-this_initasync를-사용하지-않는다)

## 🚫 흔한 실수

### signal 필드 초기값에서 부모 식별자 input 읽기

\`\`\`typescript
// ❌ 필드 이니셜라이저는 클래스 생성 시점에 실행된다 — input 기본값만 반환
filter = signal<IFilter>({
  customerId: this.customerId(),   // 항상 undefined/0
});

// ✅ 기본값은 고정, 부모 식별자는 `_search` 호출부에서 input을 직접 읽는다
filter = signal<IFilter>({});
// _search 내부:
const customerId = this.customerId();
qr1 = qr1.where((item) => [expr.eq(item.customerId, customerId)]);
\`\`\`

**근거**: 필드 이니셜라이저는 부모로부터 input 값이 전달되기 전에 실행된다. → [공통 규칙: signal 필드 초기값에서 다른 signal을 읽지 않는다](../_common-rules.md#signal-필드-초기값에서-다른-signal을-읽지-않는다)

### modal = 선택 계약으로 반사 부착

\`\`\`typescript
// ❌ 조회 전용인데 SdSelectModal 계약 일체를 그대로 이식
export class CustomerOrderHistoryModal implements SdSelectModal<ICustomerOrder> {
  selectMode = input<"single" | "multi">();
  selectedItemKeys = input<any[]>([]);
  close = output<SelectModalOutputResult<ICustomerOrder>>();
  // ... cumulativeSelection, 하단 "선택 해제 / 확인" 바까지 전부
}

// ✅ 계약 없이 부모 식별자 input만 받는다
export class CustomerOrderHistoryModal {
  customerId = input.required<number>();
  // ...
}
\`\`\`

**근거**: 조회 전용 modal은 선택 결과를 돌려주지 않는다. 계약을 부착하면 호출되지 않는 죽은 코드가 된다. 항목 선택이 필요하면 [확장 D: 선택 모달](./extension-d-select-modal.md).

## Cross-reference

- 진입점: [crud-list.md](../crud-list.md)
- 관련: [확장 D: 선택 모달 전환](./extension-d-select-modal.md)(계약이 다른 modal 변형) · [확장 F: 모달 편집 모드](./extension-f-modal-edit.md)(부모 레코드 편집 modal)
```

#### 수정·축약 내역

- **요약 문단 정리:** 현행 L7의 긴 산문(2줄)을 1줄로 줄이고, ✅/❌ 3줄 판단 기준 불릿을 바로 뒤에 배치 (설계결정 D2/D3)
- **🚫 흔한 실수 섹션 신설:** 현행 포인트 1번(선택 계약 반사 부착)과 WBS 명시 "signal 초기값 금지"를 각각 ❌/✅ 블록으로 승격. 공통 규칙 링크는 `_common-rules.md` 1-hop (설계결정 D3/D4)
- **포인트 축약:** 현행 포인트 6항목 → 핵심 5항목으로 정돈. "input 반영 effect 패턴"·"`void this._initAsync()` 금지"는 공통 규칙 링크 1줄씩으로 대체, 산문 중복 제거
- **도입 요소 불릿 정돈:** 현행 L19-25 블록을 3인칭 서술로 미세 조정, 부정 항목("부착하지 않는다")은 맨 마지막 한 불릿으로 모음
- **Cross-reference 확장 F 링크 추가:** 현행 L128은 확장 D만 링크. 요약 문단에서 확장 F도 언급하므로 Cross-reference에 포함
- **코드 주석 번호 6단계 유지:** 기능 동작 변경 없음 (설계결정 D8)

#### 소스 재검증 체크리스트

- `SdModalProvider.showAsync<T>(modal: SdModalInfo<T>, options?)` — `packages/angular/src/core/modal/sd-modal.provider.ts:85`
- `SdModalInfo<T>.inputs` — `Omit<DirectiveInputSignals<T>, "initialized" | "close" | "actionTplRef" | "_optionalModalInputs" | X>` 형태 [근거: `sd-modal.provider.ts:43-50`]
- `input`·`input.required` — `@angular/core` 공식 signal input
- `SdSelectModal<T>` 이름·제네릭 — `packages/angular/src/core/select-modal-output-result.ts`
- `injectViewTypeSignal` — `packages/angular/src/core/routing/injectViewTypeSignal.ts:7` (포인트에서 "분기 생략 가능" 서술용)
- 공통 규칙 앵커 URL 존재 확인:
  - `../_common-rules.md#signal-필드-초기값에서-다른-signal을-읽지-않는다`
  - `../_common-rules.md#input-변경을-effect-내부에서-filterlastfilterpage에-반영한다`
  - `../_common-rules.md#input-의존-데이터-로딩에-void-this_initasync를-사용하지-않는다`

### 대안 검토

- **대안 1: T1 진입점 템플릿(When to use / 전제조건 / 변형 / Anti-patterns / 관련 Entry)으로 확장까지 승격**
  - 기각 사유: 동일 폴더 sibling 2.5(확장 D)가 이미 diff 스타일로 재작성 완료. 같은 폴더에서 템플릿 혼용은 독자(LLM) 인지 부담 증가. WBS 2.2~2.8 모두 diff 스타일 전제
- **대안 2: "When to use"를 별도 `##` 섹션으로 승격**
  - 기각 사유: sibling `extension-d`도 별도 승격 없이 요약 문단 + 1-hop으로 처리. 확장 파일은 경량 템플릿을 유지하는 것이 동일 폴더 일관성에 유리
- **대안 3: "signal 초기값 금지" 안티패턴을 포인트 산문으로만 유지**
  - 기각 사유: WBS 2.6 범위가 "❌/✅" 형식을 명시. 포인트 산문은 시각 식별성이 낮고, 진입점 2.1 / Feature 3.2에서도 ❌/✅ 블록 노출로 통일되어 있음

### Vertical Slicing

단일 Markdown 파일 재작성이며 외부 소비자 API를 변경하지 않으므로 단일 Slice로 충분하다.

#### [x] Slice 1: 파일 전체 재작성(템플릿 정합 + diff 본문 + 🚫 흔한 실수 2건)

- **구현 내용:**
  1. 상단 구성(브레드크럼 / 제목 / 선행 / 요약 + ✅/❌ 판단 기준 / 호출 예 / 도입 요소) 정리 — sibling extension-d 구조와 일치
  2. 코드 블록(번호 주석 6단계) 유지, 내부 주석을 3인칭 서술로 미세 조정, 진입점 최소 뼈대와 diff 정합성 재확인
  3. **포인트** 불릿을 5항목으로 축약·3인칭화. `void this._initAsync()` 금지 / signal 초기값 금지 / input effect 반영 관련 포인트는 공통 규칙 링크 1줄로 대체
  4. **🚫 흔한 실수** 서브섹션 신설 — 2건:
     - "### signal 필드 초기값에서 부모 식별자 input 읽기" ❌/✅ + `_common-rules.md` 링크
     - "### modal = 선택 계약으로 반사 부착" ❌/✅ + 확장 D 링크
  5. Cross-reference 섹션에 확장 F 링크 추가
  6. 소스 시그니처 재검증(`SdModalProvider.showAsync`, `SdModalInfo.inputs` 형태, `input` / `input.required`, `SdSelectModal<T>` 이름)
  7. 공통 규칙 앵커 3건 URL fragment 존재 확인
- **Scenarios:** Gherkin Rule 전 항목

## 다음 단계

`/sd-dev .tasks/260422230050_angular-recipes-rewrite/wbs.md 2.6` (Slice 구현으로 진행 — sd-tdd)
