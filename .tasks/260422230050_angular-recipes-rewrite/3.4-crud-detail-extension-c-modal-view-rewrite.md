# Feature 3.4 `crud-detail/extension-c-modal-view.md` 재작성

## 참조 자료

- WBS: @.tasks/260422230050_angular-recipes-rewrite/wbs.md (Feature 3.4)
- 지침: @.claude/skills/sd-claude-docs/references/package-docs.md — T1 확장 템플릿(sibling), 반-기법, 매뉴얼 품질 검증 축
- 선행 Feature 산출물:
  - 공통 규칙: @packages/angular/docs/recipes/_common-rules.md (Feature 1.1 완료)
  - 진입점: @packages/angular/docs/recipes/crud-detail.md (Feature 3.1 완료)
  - 형제 확장 A: @packages/angular/docs/recipes/crud-detail/extension-a-edit-save.md (Feature 3.2 완료, diff 스타일 + Anti-patterns 1건 선례)
  - 형제 확장 B: @packages/angular/docs/recipes/crud-detail/extension-b-delete-restore.md (Feature 3.3 완료, Anti-patterns 1건 선례 + blockquote 도입부)
- 원문(현행): @packages/angular/docs/recipes/crud-detail/extension-c-modal-view.md (187줄)
- 소스 정합성 (코드 source of truth):
  - `packages/angular/src/layout/dock/sd-dock.ts:97` — `position = input<"top" | "bottom" | "right" | "left">("top")` 기본값 검증
  - `packages/angular/src/core/modal/sd-modal.provider.ts:140-151` — `actionTplRef` setter 프록시 설치 조건(`"actionTplRef" in contentRef.instance`)
  - `packages/angular/src/core/routing/injectViewTypeSignal.ts:7-38` — `SdActivatedModalProvider` 존재 시 `"modal"` 우선 판정
  - `packages/angular/src/core/routing/setupCanDeactivate.ts:5-42` — 모달 존재 시 `sdActivatedModal.canDeactivateFn`에 등록, 라우트는 guard 등록
- 네이밍/컨벤션: @packages/angular/CLAUDE.md

## 기준 코드

(이 Feature 구현 시 따라야 할 기존 문서·코드 패턴)

### 유사 기능: Feature 3.2/3.3 sibling 확장 템플릿

- 상단 구조 정합: brace 1행 브레드크럼 / 제목 / `> **선행:**` / 1~2문장 요약 / `**이 확장이 도입하는 요소:**` 불릿 / 번호 주석 코드 / `**포인트:**` / `## 🚫 흔한 실수 (Anti-patterns)` 순서 [근거: `packages/angular/docs/recipes/crud-detail/extension-a-edit-save.md:1-226`, `extension-b-delete-restore.md:1-140`]
- Anti-patterns 섹션 포맷(sibling B L102-140):
  - `## 🚫 흔한 실수 (Anti-patterns)` 제목
  - 도입 blockquote: "공통 규칙(…)은 [레시피 공통 규칙](../_common-rules.md)을 참조한다. 이 섹션은 **{확장 고유} 실수**만 다룬다."
  - `### {실수 이름}` 3인칭 서술 현재형
  - ❌ 코드 ↔ ✅ 코드 같은 코드블록에 인라인 병기
  - **근거**: 1줄 설명 → 공통 규칙/형제 anchor 1-hop 링크

### 현행 원문에서 이미 준수하는 요소

- `extension-c-modal-view.md:1` 브레드크럼 ✅
- `extension-c-modal-view.md:3` `# 확장 C: modal 뷰` 제목 ✅
- `extension-c-modal-view.md:5` `> **선행:** 확장 A + 확장 B` 블록쿼트 ✅
- `extension-c-modal-view.md:7` 1문장 요약 ✅
- `extension-c-modal-view.md:9-22` `**이 확장이 도입하는 요소:**` 불릿 ✅
- `extension-c-modal-view.md:30-172` 번호 주석(`// 1)` ~ `// 7)`) 코드 ✅
- `extension-c-modal-view.md:174-181` `**포인트:**` 불릿 ✅

### 미반영/수정 필요 요소

1. **Anti-patterns 섹션 미존재**: sibling A·B가 확정한 `## 🚫 흔한 실수 (Anti-patterns)` 형식 도입 필요. WBS 원안 "`injectViewTypeSignal()` 호출 시점"은 `_common-rules.md:16-30`에 ✅ Always `injectViewTypeSignal()`은 생성자·필드 이니셜라이저에서만 호출한다 ❌/✅ 블록으로 이미 수록 → 설계 결정 D1로 재해석.
2. **말미 appendix `## setupCanDeactivate는 뷰 타입에 따라 분기`(L183-186)**: L185의 `sd-data-detail.base.ts:99` 소스 참조가 현 패키지에 부재(레시피 기반 전환 완료로 해당 파일 제거됨). 포인트 L178 "`setupCanDeactivate`는 modal에서 true를 돌려 항상 이탈 허용"에 핵심 전달 완료 → 설계 결정 D3.
3. **HTML 주석 `<!-- MOVE: ... -->`(L24-25)**: 이관 완료된 작업 메모 → 설계 결정 D4.
4. **동어반복 주석(L117)**: `(확장 A/B 동일 — viewTitle을 viewTitle로 교체)` — "X를 X로 교체" 오탈자성 표기. 단순 `(확장 A/B 동일)`로 정리.

### 공통 규칙 반영

- `_common-rules.md:16-30` `injectViewTypeSignal()` 호출 시점 규칙 존재 → Anti-patterns에서 재서술 금지, 필요 지점 1-hop 링크.
- `_common-rules.md:32-55` page 컴포넌트의 topbar 소유 규칙 → 확장 C의 `@if (viewType() === "page") { <sd-topbar>...</sd-topbar> }` 래핑이 이 규칙과 공존 가능한 이유(동일 컴포넌트가 page 시에만 topbar 렌더)를 포인트·본문에 서술 유지.
- `_common-rules.md:139-146` 삭제 방식 규칙 → 확장 B 경로와 동일 서버 삭제/복구 호출 재사용.

### 프로젝트 컨벤션

- 링크 경로: 형제 확장 `./extension-*.md`, 진입점 `../crud-detail.md`, 공통 규칙 `../_common-rules.md`, 상위 docs `../../{provider-types,providers,utils,ui-layout,ui-form}/*.md` [근거: `extension-c-modal-view.md:1,24-28`, `extension-a-edit-save.md:1,198`, `extension-b-delete-restore.md:1,100,140`]
- 3인칭 서술 "추가한다·교체한다·재사용한다" [근거: `extension-b-delete-restore.md:7,95-100`]
- ❌/✅ 한 블록 인라인 병기 [근거: `extension-a-edit-save.md:208-224`, `extension-b-delete-restore.md:108-138`]

### 설계 결정

| # | 결정사항 | 선택 | 근거 |
|---|---|---|---|
| D1 | Anti-patterns 섹션 수록 내용 | **1건**: "`<sd-dock>`의 `[position]='bottom'` 누락(기본값 `top`)" 승격 | 사용자 결정. 확장 C template 도입 요소 고유 함정. 포인트 L176 산문을 sibling A/B 포맷(❌/✅ 블록)으로 승격. WBS 원안 "`injectViewTypeSignal` 호출 시점"은 `_common-rules.md:16-30`에 이미 존재 → 공통 규칙 재서술 회피 |
| D2 | ❌/✅ 예시 형태 | ❌에 `[position]` 속성 전체 누락(기본값 `"top"`으로 하단 바가 topbar 밑에 쌓여 겹침), ✅에 `[position]="'bottom'"` 명시 | silent failure(컴파일 에러 없음) 함정을 시각화. 근거: `sd-dock.ts:97` 기본값 |
| D3 | 말미 appendix `## setupCanDeactivate는 뷰 타입에 따라 분기`(L183-186) | **섹션 전체 삭제** | 사용자 결정. 포인트 L178에 modal에서 true 반환 → 이중 confirm 회피라는 핵심이 이미 서술됨. L185의 `sd-data-detail.base.ts:99` 링크는 실제 파일 부재(레시피 기반 전환 완료)로 깨진 상태. 반-기법 "보조 섹션 나열" 회피 |
| D4 | `<!-- MOVE: ... -->` 주석(L24-25) | **주석만 제거, `→ [...] 참조` 링크 2개 유지** | 사용자 결정. 이관 완료된 작업 이력 메모 제거. `SdModalContentDef` / `SdActivatedModalProvider` 1-hop 링크는 독자 접근성 위해 유지 |
| D5 | 동어반복 주석 L117 `(확장 A/B 동일 — viewTitle을 viewTitle로 교체)` | `(확장 A/B 동일)`로 축약 | 오탈자성 표기 정정. sibling A가 topbar 내부 `{{ viewTitle() }}`를 이미 도입하므로 "교체" 표현은 맥락 불일치 |
| D6 | 코드 블록 범위 | **기존 7단계 diff 유지**(imports / @Component imports / DI / 계약+viewChild+파생 / 생성자 / onSubmit·_toggleDelete 수정 / template) | WBS 범위에 동작 변경 없음. 현행 코드가 이미 self-contained이고 sibling과 일관 |
| D7 | 도메인 예시 | `ICustomer` / `CustomerDetail` 유지 | 진입점 3.1 / 확장 A·B와 독자 연속성 |
| D8 | "modal 뷰 고유 실수"로서 WBS의 injectViewTypeSignal 재해석 | 공통 규칙 anchor로 대체 | 공통 규칙 재서술을 피하고, 확장 C 고유 template 함정인 `[position]` 누락을 승격하여 첫 시도 회피율 극대화 |

## 요구명세

```gherkin
Feature: 3.4 crud-detail/extension-c-modal-view.md 재작성

  Background:
    Given 이 문서의 주 독자는 소비 프로젝트의 Claude Code(LLM 에이전트)이다
    And 타겟 파일은 packages/angular/docs/recipes/crud-detail/extension-c-modal-view.md이다
    And 확장 A·B가 도입한 `busyCount` / `perms` / `_refresh` / `_checkIgnoreChanges` / `isNew` / `canEdit` / `_toggleDelete` / snapshot 기반 변경 감지가 전제된다
    And `_common-rules.md`에 `injectViewTypeSignal()` 호출 시점, page 컴포넌트 topbar 소유 규칙이 존재한다
    And sibling A·B가 `## 🚫 흔한 실수 (Anti-patterns)` 포맷을 확정했다

  Rule: sibling diff 스타일 템플릿을 유지한다
    # 근거: 설계 결정 D6, sibling extension-b:1-100

    Scenario: 파일 상단 구성요소 순서가 유지된다
      Then 1행 브레드크럼
      And `# 확장 C: modal 뷰` 제목
      And `> **선행:** 확장 A + 확장 B` 블록쿼트
      And 1~2문장 요약
      And `**이 확장이 도입하는 요소:**` 불릿
      And 번호 주석(`// 1)` ~ `// 7)`) TypeScript 코드 블록
      And `**포인트:**` 불릿
      And `## 🚫 흔한 실수 (Anti-patterns)` 섹션

    Scenario: 코드 diff 범위가 변경되지 않는다
      Then imports / @Component imports / DI / 계약+viewChild+파생 / 생성자 / onSubmit·_toggleDelete / template 7단계가 유지된다
      And `actionTplRef?: TemplateRef<any>` 필드 선언·effect·`setupCanDeactivate` 조건 갱신·`close.emit(true)` 추가·modal 하단 바·`<ng-template #modalActionTpl>`이 모두 보존된다

  Rule: 정리 대상 요소를 제거한다
    # 근거: 설계 결정 D3·D4·D5

    Scenario: 말미 appendix가 제거된다
      Then `## setupCanDeactivate는 뷰 타입에 따라 분기` 제목·본문이 존재하지 않는다
      And 파일 말미는 `## 🚫 흔한 실수 (Anti-patterns)` 섹션으로 종료된다

    Scenario: 이관 메모 주석이 제거된다
      Then `<!-- MOVE: ... -->` HTML 주석이 존재하지 않는다
      And `SdModalContentDef 구현 패턴` 및 `SdActivatedModalProvider 사용법`을 가리키는 `→ [...]` 참조 링크 2개는 유지된다

    Scenario: 동어반복 주석이 정리된다
      Then template 내부 주석에 "viewTitle을 viewTitle로 교체" 문구가 존재하지 않는다
      And `(확장 A/B 동일)` 형태로 축약된다

  Rule: `<sd-dock>` `[position]='bottom'` 누락 Anti-patterns 1건을 승격한다
    # 근거: WBS 3.4 범위, 설계 결정 D1·D2

    Scenario: 포인트 뒤에 Anti-patterns 섹션이 신설된다
      Then `**포인트:**` 불릿 다음 위치에 `## 🚫 흔한 실수 (Anti-patterns)` 제목이 있다
      And 도입 blockquote "공통 규칙(`mark` 오용, `setupCanDeactivate`/`injectViewTypeSignal` 호출 위치, page 컴포넌트의 topbar 소유 등)은 [레시피 공통 규칙](../_common-rules.md)을 참조한다. 이 섹션은 **modal 뷰 확장 고유 실수**만 다룬다." 가 있다
      And `### modal 하단 바의 <sd-dock>에 [position]='bottom'을 명시하지 않는다` 서브섹션이 있다

    Scenario: ❌/✅ 블록이 한 코드블록 안에 인라인 병기된다
      Then ❌ 예시에 `<sd-dock>`에서 `[position]` 속성이 생략되어 있다
      And ✅ 예시에 `<sd-dock [position]="'bottom'">` 형태로 명시되어 있다

    Scenario: 근거와 소스 링크가 있다
      Then 근거 1줄 설명이 "기본값 `\"top\"`이라 하단 바가 topbar 밑에 쌓여 겹친다"는 취지로 있다
      And `sd-dock.ts:97` 소스 인용이 포함된다

  Rule: 공통 규칙 재서술을 금지한다
    # 근거: 반-기법 "공통 규칙 재서술"

    Scenario: `injectViewTypeSignal()` 호출 시점 규칙을 재서술하지 않는다
      Then 본문·Anti-patterns 섹션에 "computed/effect에서 injectViewTypeSignal() 호출 금지" 블록을 재도입하지 않는다
      And 필요 시 `_common-rules.md` anchor로 1-hop 링크만 둔다

  Rule: 3인칭 서술·반-기법 회피
    # 근거: package-docs.md 매뉴얼 품질 검증

    Scenario: 1·2인칭 서술이 없다
      Then "you can", "당신이" 같은 표현이 없다
      And "추가한다", "재사용한다", "교체한다" 형태로 서술한다

    Scenario: CRITICAL·ALWAYS·NEVER 남발이 없다
      Then CRITICAL/ALWAYS/NEVER 키워드가 2회 이하로 등장한다
```

## 구현계획

### 배경

현행 `packages/angular/docs/recipes/crud-detail/extension-c-modal-view.md`(187줄)는 대부분 sibling diff 스타일을 준수하지만 (a) sibling A·B가 확정한 `## 🚫 흔한 실수 (Anti-patterns)` 섹션이 없고, (b) 말미 appendix `## setupCanDeactivate는 뷰 타입에 따라 분기`(L183-186)가 포인트 L178과 중복이면서 `sd-data-detail.base.ts:99` 깨진 소스 링크를 포함하며, (c) L24-25의 `<!-- MOVE: ... -->` 주석과 L117의 "viewTitle을 viewTitle로 교체" 동어반복 표기가 잔존한다. 본 재작성은 **템플릿 정합 + 불필요 섹션/주석 제거 + Anti-patterns 1건 승격**에 한정되며, 코드 diff 7단계와 포인트 본문은 보존한다.

### 목표

- sibling A·B와 동일 포맷의 `## 🚫 흔한 실수 (Anti-patterns)` 섹션 1건 승격 [근거: Rule "Anti-patterns 1건 승격"]
- 말미 appendix 제거로 깨진 소스 링크 근본 해소 [근거: Rule "정리 대상 요소 제거" — appendix]
- 이관 메모 HTML 주석 제거, 참조 링크는 유지 [근거: Rule "정리 대상 요소 제거" — 이관 메모]
- 동어반복 주석 정정 [근거: Rule "정리 대상 요소 제거" — 동어반복]

### 비목표

- 확장 C 동작 변경(`actionTplRef` 프록시 흐름, `setupCanDeactivate` 조건 로직, `close.emit` 시점, template 구조) [근거: 설계 결정 D6]
- 코드 7단계 번호 주석 재배열 [근거: 설계 결정 D6]
- 포인트 불릿 재배열/삭제(L178의 `setupCanDeactivate` 설명 포함) [근거: 설계 결정 D3 — 포인트에 핵심 수렴]
- `_common-rules.md` 내용 수정(Feature 1.1 범위)
- 확장 D/E/F 파일 연쇄 수정(각 Feature 범위)

### 설계

#### 수정 위치 1: L24-25 HTML 주석 제거 (링크 유지)

```diff
- <!-- MOVE: docs/provider-types.md#sdmodalcontentdef --> → [`SdModalContentDef` 구현 패턴](../../provider-types/sd-modal-content-def.md#구현-패턴) 참조
- <!-- MOVE: docs/providers.md#sdactivatedmodalprovider --> → [`SdActivatedModalProvider` 사용법](../../providers/sd-activated-modal-provider.md#usage) 참조
+ > 상세: [`SdModalContentDef` 구현 패턴](../../provider-types/sd-modal-content-def.md#구현-패턴)
+ > 상세: [`SdActivatedModalProvider` 사용법](../../providers/sd-activated-modal-provider.md#usage)
```

(sibling 스타일 정합: L26-28의 `> 상세:` 블록과 형식 통일)

#### 수정 위치 2: L117 동어반복 주석 축약

```diff
- <!-- 저장/삭제/복구/새로고침 (확장 A/B 동일 — viewTitle을 viewTitle로 교체) -->
+ <!-- 저장/삭제/복구/새로고침 (확장 A/B 동일) -->
```

#### 수정 위치 3: 포인트 L181 뒤에 Anti-patterns 섹션 신설

```markdown
## 🚫 흔한 실수 (Anti-patterns)

> 공통 규칙(`mark` 오용, `setupCanDeactivate` / `injectViewTypeSignal()` 호출 위치, page 컴포넌트의 topbar 소유 등)은 [레시피 공통 규칙](../_common-rules.md)을 참조한다. 이 섹션은 **modal 뷰 확장 고유 실수**만 다룬다.

### modal 하단 바의 `<sd-dock>`에 `[position]="'bottom'"`을 명시하지 않는다

```typescript
// ❌ [position] 생략 — 기본값이 "top"이라 하단 바가 <sd-topbar-container>의 상단에 쌓여
//    topbar 뒤에 겹치거나, topbar가 숨겨진 modal 뷰에서도 기대한 하단 위치에 나오지 않는다.
//    컴파일 에러 없이 silent failure.
@if (viewType() === "modal" && canEdit()) {
  <sd-dock class="p-sm-default flex-row gap-sm bdt bdt-theme-gray-lightest">
    <!-- 확인/취소/삭제/복구 버튼 ... -->
  </sd-dock>
}

// ✅ [position]="'bottom'"을 명시하여 <sd-dock-container> 하단에 고정
@if (viewType() === "modal" && canEdit()) {
  <sd-dock
    [position]="'bottom'"
    class="p-sm-default flex-row gap-sm bdt bdt-theme-gray-lightest"
  >
    <!-- 확인/취소/삭제/복구 버튼 ... -->
  </sd-dock>
}
```

**근거**: `SdDock.position`의 기본값이 `"top"`이다(`packages/angular/src/layout/dock/sd-dock.ts:97`). 하단 액션 바 용도로 `<sd-dock>`을 도입할 때 `[position]`을 생략하면 `<sd-dock-container>`가 상단부터 dock을 쌓기 때문에 topbar 영역 뒤에 깔리고, topbar가 없는 modal 뷰에서도 의도한 "모달 하단 고정"이 깨진다. 타입 시스템은 기본값으로 통과시키므로 컴파일러·린터가 경고하지 않는다.
```

#### 수정 위치 4: 말미 appendix 삭제 (L183-186)

```diff
- ## `setupCanDeactivate`는 뷰 타입에 따라 분기
-
- - 모달 뷰에서는 `SdActivatedModalProvider.canDeactivateFn`에 등록되고, 페이지 뷰에서는 라우트 guard로 등록된다. control 뷰에서는 아무 동작 하지 않는다 (`sd-data-detail.base.ts:99` + `setupCanDeactivate.ts:5`).
- - 레시피는 확장 C(modal 뷰)가 적용되면 `setupCanDeactivate(() => this.viewType() === "modal" || this._checkIgnoreChanges())` 형태로 호출한다. 모달에서는 `true`를 돌려 항상 이탈 허용하고(모달 자체 취소 버튼으로 제어), 페이지에서는 `_checkIgnoreChanges()`의 confirm 결과로 제어.
```

(핵심은 포인트 L178에 이미 수렴. 깨진 `sd-data-detail.base.ts:99` 링크 근본 제거)

#### 변경 없는 구간

- L1 브레드크럼 / L3 제목 / L5 선행 / L7 요약 / L9-22 도입 요소 / L26-28 상세 링크(viewTypeSignal·sd-dock) / L30-172 코드 7단계 / L174-181 포인트 → **보존**

#### 소스 시그니처 재검증 체크리스트

- `sd-dock.ts:97` `position = input<...>("top")` — Anti-patterns 근거 인용
- `sd-modal.provider.ts:140-151` `actionTplRef` setter 프록시 — 포인트 L177 주석 경로 정합 (기존 `sd-modal.provider.ts:140-150`은 유효 범위 내 소스 이동 없음, 유지)
- `injectViewTypeSignal.ts:7-38` `SdActivatedModalProvider` 우선 판정 — 본문 "모달에서는 `viewType() === "modal"`로 자동 판정" 서술 정합
- `setupCanDeactivate.ts:5-42` 모달/라우트 분기 — 포인트 L178의 "modal에서 true 반환 → 이탈 허용" 서술 정합

### 대안 검토

- **대안 1: Anti-patterns 2건(`[position]` 누락 + `setupCanDeactivate` 조건 누락) 승격**
  - 기각 사유: 사용자 결정으로 1건 확정. sibling A·B 선례(각 1건)와 정합. 반-기법 "섹션 남발" 회피.
- **대안 2: WBS 원안대로 `injectViewTypeSignal()` 호출 시점 ❌/✅ 승격**
  - 기각 사유: `_common-rules.md:16-30`에 동일 ❌/✅ 블록이 이미 존재. 재서술은 공통 규칙 위임 원칙에 반하며, 확장 C 고유 template 함정을 놓치게 된다.
- **대안 3: 말미 appendix 유지하고 깨진 링크(`sd-data-detail.base.ts:99`)만 `setupCanDeactivate.ts:5-13`으로 교체**
  - 기각 사유: 사용자 결정으로 삭제 채택. 포인트 L178과 중복이고 반-기법 "보조 섹션 나열" 위반.
- **대안 4: `<!-- MOVE: ... -->` 주석을 전부 제거하고 하위 링크도 함께 제거**
  - 기각 사유: 사용자 결정으로 링크 유지 채택. `SdModalContentDef` / `SdActivatedModalProvider` 상세로의 1-hop 접근성은 유지 가치가 있음.

### Vertical Slicing

단일 Markdown 파일 재작성이고 외부 소비자 API를 건드리지 않으므로 1 slice로 충분하다.

#### [x] Slice 1: 정리 + Anti-patterns 1건 승격

- **구현 내용:**
  1. L24-25 HTML 주석 2줄을 `> 상세:` 링크 2줄로 교체 (sibling 스타일 정합).
  2. L117 template 주석 "viewTitle을 viewTitle로 교체"를 "(확장 A/B 동일)"로 축약.
  3. L181 포인트 불릿 다음 줄에 `## 🚫 흔한 실수 (Anti-patterns)` 섹션을 신설한다.
     - 도입 blockquote, `### modal 하단 바의 <sd-dock>에 [position]='bottom'을 명시하지 않는다` 서브섹션, ❌/✅ 인라인 코드블록, 근거 1줄 + `sd-dock.ts:97` 소스 인용.
  4. L183-186 말미 appendix `## setupCanDeactivate는 뷰 타입에 따라 분기` 섹션을 제거한다.
  5. 3인칭 서술·CRITICAL/ALWAYS/NEVER 남발·1·2인칭 표현·용어 일관성을 재확인한다.
- **Scenarios:**
  - 파일 상단 구성요소 순서가 유지된다
  - 코드 diff 범위가 변경되지 않는다
  - 말미 appendix가 제거된다
  - 이관 메모 주석이 제거된다
  - 동어반복 주석이 정리된다
  - 포인트 뒤에 Anti-patterns 섹션이 신설된다
  - ❌/✅ 블록이 한 코드블록 안에 인라인 병기된다
  - 근거와 소스 링크가 있다
  - `injectViewTypeSignal()` 호출 시점 규칙을 재서술하지 않는다
  - 1·2인칭 서술이 없다
  - CRITICAL/ALWAYS/NEVER 남발이 없다
