# Feature 3.5 `crud-detail/extension-d-control-view.md` 재작성

## 참조 자료

- WBS: @.tasks/260422230050_angular-recipes-rewrite/wbs.md (Feature 3.5)
- 지침: @.claude/skills/sd-claude-docs/references/package-docs.md — T1 확장 템플릿(sibling), 반-기법, 매뉴얼 품질 검증 축
- 선행 Feature 산출물:
  - 공통 규칙: @packages/angular/docs/recipes/_common-rules.md (Feature 1.1 완료본)
  - 진입점: @packages/angular/docs/recipes/crud-detail.md (Feature 3.1 완료본)
  - 선행 확장: @packages/angular/docs/recipes/crud-detail/extension-a-edit-save.md (Feature 3.2 완료본)
  - 선행 확장: @packages/angular/docs/recipes/crud-detail/extension-b-delete-restore.md (Feature 3.3 완료본, Anti-patterns 1건 선례)
  - 형제 확장(동일 diff 스타일): @packages/angular/docs/recipes/crud-detail/extension-c-modal-view.md (현행, 동일 `<sd-dock-container>` 내부 modal 분기 블록)
- 원문(현행): @packages/angular/docs/recipes/crud-detail/extension-d-control-view.md (67줄, sibling diff 스타일 대체로 준수, Anti-patterns 섹션 부재)
- 소스 정합성:
  - @packages/angular/src/core/routing/injectViewTypeSignal.ts:7 — `"control"` 판정 조건(`reflectComponentType(component)?.selector !== elRef.nativeElement.tagName.toLowerCase()`)
  - @packages/angular/src/core/routing/setupCanDeactivate.ts:10-26 — control 뷰(호스트 tagName ≠ route selector)에서 guard 미등록
  - @packages/angular/src/layout/dock/sd-dock.ts:97 — `position` input 기본값 `"top"`
  - `@ng-icons/tabler-icons` — `tablerDeviceFloppy`, `tablerRefresh`, `tablerEraser`, `tablerRestore` (확장 A/B 기 도입)
- 네이밍/컨벤션: @packages/angular/CLAUDE.md

## 기준 코드

(이 Feature 구현 시 따라야 할 기존 문서 패턴)

### 유사 기능: Feature 3.3(완료) `extension-b-delete-restore.md`

- 파일 구조: `packages/angular/docs/recipes/crud-detail/extension-b-delete-restore.md:1-140`
  - L1 브레드크럼 / L3 제목 / L5 `> **선행:**` / L7 1~2문장 요약 / L9 (조건부) `> **적용 조건:**` 블록쿼트 / L11-16 `**이 확장이 도입하는 요소:**` 불릿 / L18-93 번호 주석 코드 / L95-100 `**포인트:**` / **L102-140 `## 🚫 흔한 실수 (Anti-patterns)` 섹션 (1건 승격 선례)**
- Anti-patterns 섹션 포맷(`extension-b-delete-restore.md:102-140`):
  - `## 🚫 흔한 실수 (Anti-patterns)` 제목
  - 도입 blockquote: "공통 규칙(…)은 [레시피 공통 규칙](../_common-rules.md)을 참조한다. 이 섹션은 **{확장 고유} 실수**만 다룬다."
  - `### {실수 이름}` (3인칭 서술 현재형)
  - ❌ 코드 ↔ ✅ 코드 한 블록에 인라인 병기
  - **근거**: 1줄 설명 → 공통 규칙 anchor 1-hop 링크

### 현행 원문의 이미 준수 중인 요소

- `extension-d-control-view.md:1` 브레드크럼 ✅
- `extension-d-control-view.md:3` `# 확장 D: control 뷰` 제목 ✅
- `extension-d-control-view.md:5` `> **선행:** 확장 A + 확장 B` 블록쿼트 ✅
- `extension-d-control-view.md:7` 1~2문장 요약 ✅
- `extension-d-control-view.md:9-13` `**이 확장이 도입하는 요소:**` 불릿 ✅
- `extension-d-control-view.md:15,17` `> 상세:` 1-hop 링크 2건 ✅
- `extension-d-control-view.md:19-59` 번호 주석(`// 1)` ~ `// 3)`) 코드 ✅
- `extension-d-control-view.md:61-66` `**포인트:**` 불릿 ✅

### 미반영 요소

1. **Anti-patterns 섹션 미존재**: WBS 3.5 범위 "❌/✅ control 뷰에서 `<sd-topbar-container>` 소유 금지(공통 규칙 참조)" 항목이 현재 산문 포인트로도 명시되지 않음. sibling 3.3의 `## 🚫 흔한 실수 (Anti-patterns)` 포맷으로 신설 필요.

### 공통 규칙 반영

- `_common-rules.md:32-55` "page 컴포넌트가 `<sd-topbar-container>`와 `<sd-topbar>`를 소유한다" 섹션에 ❌/✅ 블록과 근거가 이미 상세 기술됨 → 확장 D의 Anti-patterns는 **재서술하지 않고** 공통 규칙 anchor로 1-hop 위임한다.
- `_common-rules.md:32-55`의 ❌/✅ 예시는 **마스터-디테일 page 레이아웃** 맥락(`<app-sheet /> <app-detail />` 조합)이다. 확장 D는 **재사용 대상 컴포넌트 내부** 맥락(`@if (viewType() === "control")` 분기 내부)에서 동일 원칙이 어떻게 적용되는지를 **독자적 코드 시각화**로 보강한다(중복 서술 아님, 맥락 재수록).

### 프로젝트 컨벤션

- 링크 경로: 형제 확장 `./extension-*.md`, 진입점 `../crud-detail.md`, 공통 규칙 `../_common-rules.md` [근거: `extension-a-edit-save.md:1`, `extension-b-delete-restore.md:1,9,100,140`]
- 3인칭 서술 "추가한다·재사용한다·판정한다·동작하지 않는다" [근거: `extension-d-control-view.md:7,63-66`]
- ❌/✅ 한 코드블록 내 인라인 병기 [근거: `extension-b-delete-restore.md:108-138`, `extension-a-edit-save.md:208-224`]
- GitHub-style anchor: 백틱·괄호·꺾쇠 제거, 공백은 `-`, 영문 소문자화, 한글 그대로. 공통 규칙 헤더 `### page 컴포넌트가 \`<sd-topbar-container>\`와 \`<sd-topbar>\`를 소유한다` → anchor `#page-컴포넌트가-sd-topbar-container와-sd-topbar를-소유한다`

### 설계 결정

| # | 결정사항 | 선택 | 근거 |
|---|---|---|---|
| D1 | Anti-patterns 섹션 수록 건수 | **1건**("control 뷰 분기에 자체 `<sd-topbar>`를 추가한다")만 승격 | 사용자 결정(A 선택). WBS 3.5 명시 항목 1건 + sibling 3.3(1건)·3.7(1건) 선례 일관. 반-기법 "섹션 남발" 회피 |
| D2 | ❌ 예시 형태 | `@if (viewType() === "control" && canEdit())` 분기 내부에 `<sd-topbar>` 배치(page topbar와 중첩) | 원문 코드(`extension-d-control-view.md:30`)의 동일 분기 위치를 재사용하여 "이 자리에 topbar를 넣으면 안 된다"는 회귀 지점을 시각화 |
| D3 | ✅ 예시 형태 | 원문과 동일한 `<sd-dock>` 상단 바로 복원(간략화 버전) | 원문 코드 L30-50 재사용. 독자가 본문 코드와 ✅ 예시를 동일 형태로 인식 |
| D4 | 공통 규칙 재서술 금지 | 본문은 page topbar 소유 규칙을 재서술하지 않고 anchor 1-hop 링크로만 위임 | `_common-rules.md:32-55`에 이미 상세 기술, 중복은 반-기법 "Deeply nested 링크 체인" 및 "정보 파편화 방지" 원칙 위반 |
| D5 | 포인트 불릿 유지/이관 | 포인트 4건(`extension-d-control-view.md:63-66`) **전체 유지** | 포인트는 "동작 규칙/주의사항", Anti-patterns는 "실수 시각화" — 역할 분리(sibling 3.3 D4와 동일 원칙) |
| D6 | 코드 블록 범위 | **기존 3단계(imports / 파생 / template) 유지** | WBS 범위에 동작 변경 없음. 현행 코드가 이미 self-contained |
| D7 | `setupCanDeactivate` control no-op Anti-pattern 승격 | **기각** — 포인트 L66 산문 1줄 유지 | 사용자 선택 A 범위 외. 반-기법 "섹션 남발" 회피 |
| D8 | `<sd-dock> [position]` 생략 Anti-pattern 승격 | **기각** — 포인트 L64 산문 1줄 유지 | 사용자 선택 A 범위 외. 기본값과 동일 값 명시는 실제 오작동 아님 |
| D9 | 도메인 예시 | `ICustomer` / `app-customer-detail` 유지 | 진입점 3.1 / 확장 A / B와 독자 연속성 |

## 요구명세

```gherkin
Feature: 3.5 crud-detail/extension-d-control-view.md 재작성

  Background:
    Given 이 문서의 주 독자는 소비 프로젝트의 Claude Code(LLM 에이전트)이다
    And 타겟 파일은 packages/angular/docs/recipes/crud-detail/extension-d-control-view.md이다
    And Feature 1.1의 `_common-rules.md`에 "page 컴포넌트가 `<sd-topbar-container>`와 `<sd-topbar>`를 소유한다" 규칙이 존재한다
    And Feature 3.3의 `extension-b-delete-restore.md`에 `## 🚫 흔한 실수 (Anti-patterns)` 1건 승격 포맷이 확정되어 있다

  Rule: sibling diff 스타일 템플릿을 유지한다
    # 근거: 설계 결정 D6, sibling extension-b-delete-restore.md:1-100

    Scenario: 파일 상단 구성요소 순서가 유지된다
      Then 1행 브레드크럼(`← [CRUD 상세폼 레시피 진입점](../crud-detail.md)`)
      And `# 확장 D: control 뷰` 제목
      And `> **선행:** [확장 A: 편집/저장](./extension-a-edit-save.md) + [확장 B: 삭제/복구 토글](./extension-b-delete-restore.md)` 블록쿼트
      And 1~2문장 요약
      And `**이 확장이 도입하는 요소:**` 불릿
      And `> 상세: [\`injectViewTypeSignal\`]` 및 `> 상세: [\`<sd-dock> position 기본 "top"\`]` 1-hop 링크 2건
      And 번호 주석(`// 1)` ~ `// 3)`) TypeScript 코드 블록
      And `**포인트:**` 불릿 4건

    Scenario: 코드 diff 범위가 변경되지 않는다
      Then imports / 파생 / template 3단계 주석이 유지된다
      And `@if (viewType() === "control" && canEdit())` 블록 구조가 변경되지 않는다

  Rule: ❌/✅ "control 뷰 분기에 자체 `<sd-topbar>`를 추가한다" Anti-patterns 1건을 신설한다
    # 근거: WBS 3.5 범위, 설계 결정 D1·D2·D3

    Scenario: 포인트 뒤에 Anti-patterns 섹션이 신설된다
      Then `**포인트:**` 불릿 다음 위치에 `## 🚫 흔한 실수 (Anti-patterns)` 제목이 있다
      And 도입 blockquote "공통 규칙(topbar 소유권, `mark` 오용 등)은 [레시피 공통 규칙](../_common-rules.md)을 참조한다. 이 섹션은 **control 뷰 확장 고유 실수**만 다룬다."가 있다
      And `### control 뷰 분기에 자체 <sd-topbar>를 추가한다` 서브섹션이 있다

    Scenario: ❌/✅ 블록이 한 코드블록 안에 인라인 병기된다
      Then ❌ 예시에 `@if (viewType() === "control" && canEdit())` 블록 내부 `<sd-topbar>` 배치가 있다
      And ✅ 예시에 동일 블록 내부 `<sd-dock>` 상단 바 배치(원문 L30-50 간략화)가 있다

    Scenario: 근거와 공통 규칙 링크가 있다
      Then 근거 1줄 설명이 있다
      And `→ [공통 규칙: page 컴포넌트가 \`<sd-topbar-container>\`와 \`<sd-topbar>\`를 소유한다](../_common-rules.md#page-컴포넌트가-sd-topbar-container와-sd-topbar를-소유한다)` 1-hop 링크가 있다

  Rule: 공통 규칙 중복을 금지한다
    # 근거: WBS Feature 1.1 T3 위임 원칙, 반-기법 "Deeply nested 링크 체인" 회피

    Scenario: page topbar 소유 규칙 본문을 재서술하지 않는다
      Then 본문은 `_common-rules.md:32-55`의 "마스터-디테일 page 레이아웃" ❌/✅ 블록을 재서술하지 않는다
      And 필요 지점에서 공통 규칙 anchor 링크로 1-hop 위임한다

  Rule: 3인칭 서술·반-기법 회피
    # 근거: package-docs.md 매뉴얼 품질 검증

    Scenario: 1·2인칭 서술이 없다
      Then "you can", "당신이" 같은 표현이 없다
      And "추가한다", "판정한다", "동작하지 않는다" 형태로 서술한다

    Scenario: CRITICAL·ALWAYS·NEVER 남발이 없다
      Then CRITICAL/ALWAYS/NEVER 키워드가 2회 이하로 등장한다

  Rule: 소스 시그니처 정합성
    # 근거: 참조 자료 소스 정합성

    Scenario: 링크·API 레퍼런스가 현행 소스와 일치한다
      Then `injectViewTypeSignal()` 참조가 `../../utils/inject-routing-signals.md#injectviewtypesignal`로 유지된다
      And `<sd-dock>` position 참조가 `../../ui-layout/sd-dock.md`로 유지된다
      And `setupCanDeactivate` control no-op 포인트는 `setupCanDeactivate.ts:10-26` 조건과 정합한다
```

## 구현계획

### 배경

현행 `packages/angular/docs/recipes/crud-detail/extension-d-control-view.md`(67줄)는 이미 sibling diff 스타일 템플릿을 대체로 준수한다(브레드크럼·선행·요약·도입 요소·번호 코드·포인트). 다만 WBS 3.5에서 명시한 "❌/✅ control 뷰에서 `<sd-topbar-container>` 소유 금지(공통 규칙 참조)" 항목이 현재 산문 포인트로도 명시되지 않았고, sibling 3.3(`extension-b-delete-restore.md`)이 확정한 `## 🚫 흔한 실수` 포맷(1건 승격 + 도입 blockquote + ❌/✅ 인라인 + 근거 + 공통 규칙 1-hop 링크)이 미적용이다. 이번 재작성은 **Anti-patterns 섹션 1건 신설**에 한정되며, 코드 diff·포인트 본문은 보존한다.

### 목표

- sibling 3.3과 동일 포맷의 `## 🚫 흔한 실수 (Anti-patterns)` 섹션 1건 신설 [근거: Rule "Anti-patterns 1건 신설"]
- 공통 규칙 `_common-rules.md:32-55` "page 컴포넌트가 `<sd-topbar-container>`와 `<sd-topbar>`를 소유한다"로 1-hop 위임 [근거: Rule "공통 규칙 중복 금지"]
- 3인칭 서술 및 반-기법 축 재확인 [근거: Rule "3인칭 서술·반-기법 회피"]

### 비목표

- 확장 D 동작 변경(`<sd-dock>` 상단 바 구성, `viewType() === "control" && canEdit()` 분기 조건, 아이콘 세트) [근거: WBS 3.5에 동작 변경 없음, 설계 결정 D6]
- 포인트 불릿 4건(L63-66) 재배열/삭제 [근거: 설계 결정 D5]
- `<sd-dock> [position]` 생략 / `setupCanDeactivate` control no-op의 Anti-pattern 승격 [근거: 설계 결정 D7·D8]
- `_common-rules.md` 내용 수정(Feature 1.1 범위)
- 형제 확장 C/A/B 파일 연쇄 수정(각자 Feature 3.2/3.3/3.4 범위)

### 설계

#### 수정 위치 1: 포인트 L66 뒤에 Anti-patterns 섹션 신설

파일 마지막 줄(L66 직후)에 아래 섹션을 추가한다. 기존 내용은 보존.

```markdown

## 🚫 흔한 실수 (Anti-patterns)

> 공통 규칙(topbar 소유권, `mark` 오용, `setupCanDeactivate` 호출 위치 등)은 [레시피 공통 규칙](../_common-rules.md)을 참조한다. 이 섹션은 **control 뷰 확장 고유 실수**만 다룬다.

### control 뷰 분기에 자체 `<sd-topbar>`를 추가한다

```typescript
// ❌ control 분기용으로 별도 <sd-topbar> 추가 — 마스터 화면이 이 컴포넌트를
//    <app-customer-detail />로 포함할 때, 마스터 화면이 이미 소유한
//    <sd-topbar-container> + <sd-topbar>와 중첩되어 레이아웃이 깨진다.
@if (viewType() === "control" && canEdit()) {
  <sd-topbar>
    <h4>{{ viewTitle() }}</h4>
    <sd-button [theme]="'link-primary'" (click)="onSaveButtonClick()"> 저장 </sd-button>
    <!-- ... -->
  </sd-topbar>
}

// ✅ control 분기에는 <sd-dock> 상단 바만 둔다. page가 소유한 topbar와
//    중첩되지 않으며, 저장/새로고침/삭제/복구 버튼을 가로로 배치한다.
@if (viewType() === "control" && canEdit()) {
  <sd-dock class="p-default flex-row gap-default bdb bdb-theme-gray-lightest">
    <sd-button [theme]="'primary'" (click)="onSaveButtonClick()">
      <ng-icon [svg]="tablerDeviceFloppy" /> 저장 <small>(CTRL+S)</small>
    </sd-button>
    <sd-button [theme]="'info'" (click)="onRefreshButtonClick()">
      <ng-icon [svg]="tablerRefresh" /> 새로고침 <small>(CTRL+ALT+L)</small>
    </sd-button>
    <!-- 삭제/복구는 @if (!isNew() && canEdit()) 블록으로 (확장 B 조건 재사용) -->
  </sd-dock>
}
```

**근거**: control 뷰는 마스터 화면이 `<app-customer-detail [itemId]="..." />`처럼 디테일 영역으로 직접 삽입하는 구조다. 마스터 화면은 이미 `<sd-topbar-container>`와 `<sd-topbar>`를 소유하므로, 내부 컴포넌트가 동일 구조를 중첩하면 타이틀·액션 영역이 이중으로 렌더링되어 레이아웃이 깨진다. control 분기의 상단 액션 영역은 `<sd-dock-container>` 내부 `<sd-dock>`으로 구성한다. → [공통 규칙: page 컴포넌트가 `<sd-topbar-container>`와 `<sd-topbar>`를 소유한다](../_common-rules.md#page-컴포넌트가-sd-topbar-container와-sd-topbar를-소유한다)
```

#### 변경 없는 구간

- L1 브레드크럼 / L3 제목 / L5 선행 / L7 요약 / L9-13 도입 요소 / L15, L17 1-hop 링크 / L19-59 코드 3단계 / L61-66 포인트 → **보존**

#### 소스 시그니처 재검증 체크리스트

- `injectViewTypeSignal()` — `packages/angular/src/core/routing/injectViewTypeSignal.ts:7` 존재, control 판정 조건 유지
- `<sd-dock>` `[position]` 기본값 `"top"` — `packages/angular/src/layout/dock/sd-dock.ts:97` 유지
- `setupCanDeactivate` control no-op — `packages/angular/src/core/routing/setupCanDeactivate.ts:10-26` 조건(호스트 tagName ≠ route selector) 유지
- `tablerDeviceFloppy`, `tablerRefresh`, `tablerEraser`, `tablerRestore` — 확장 A/B에서 도입된 아이콘 재사용, `@ng-icons/tabler-icons` export 정합

### 대안 검토

- **대안 1: Anti-patterns 섹션 미생성(포인트 불릿 1줄로만 처리)**
  - 기각 사유: WBS 3.5 명시 범위 "❌/✅ ... 금지"는 **코드 블록 시각화**를 전제. sibling 3.3·3.7이 각각 1건 승격으로 포맷을 확정한 마당에 본 확장만 섹션 미생성 시 형제 간 일관성이 깨진다.
- **대안 2: `setupCanDeactivate` control no-op Anti-pattern 병행 승격(2건)**
  - 기각 사유: 사용자 선택 A(1건) 범위 외. 포인트 L66 산문 1줄("control 뷰에서는 `setupCanDeactivate`가 아무 동작 하지 않는다")로 충분하며, 반-기법 "섹션 남발" 회피.
- **대안 3: `<sd-dock> [position]="'top'"` 명시 Anti-pattern 병행 승격(2건)**
  - 기각 사유: 사용자 선택 A(1건) 범위 외. 기본값과 동일 값을 명시하는 것은 실제 오작동이 아니라 스타일 차이이며, 포인트 L64 산문으로 충분.
- **대안 4: 공통 규칙 `_common-rules.md:32-55` ❌/✅ 블록을 본문에 복사·재배치**
  - 기각 사유: 반-기법 "정보 파편화·중복 서술" 회피. 공통 규칙 업데이트 시 확장 D 본문도 동기화해야 하는 유지보수 부담 발생. 1-hop 링크가 `_common-rules.md` 원본 원칙에 부합.

### Vertical Slicing

단일 Markdown 파일 재작성이고 외부 소비자 API를 건드리지 않으므로 1 slice로 충분하다.

#### [x] Slice 1: Anti-patterns 섹션 1건 신설

- **의존:** 없음
- **구현 내용:**
  1. L66 포인트 마지막 불릿 다음 줄(파일 끝)에 `## 🚫 흔한 실수 (Anti-patterns)` 섹션을 신설한다.
     - 도입 blockquote, `### control 뷰 분기에 자체 <sd-topbar>를 추가한다` 서브섹션, ❌/✅ 인라인 코드블록, 근거 1줄 + 공통 규칙 anchor 링크(`#page-컴포넌트가-sd-topbar-container와-sd-topbar를-소유한다`).
  2. 3인칭 서술·CRITICAL/ALWAYS/NEVER 남발·매뉴얼 품질 검증 축(1·2인칭 서술, 용어 일관성) 재확인.
  3. 소스 시그니처 재검증 체크리스트 확인(없으면 즉시 수정).
- **Scenarios:**
  - 파일 상단 구성요소 순서가 유지된다
  - 코드 diff 범위가 변경되지 않는다
  - 포인트 뒤에 Anti-patterns 섹션이 신설된다
  - ❌/✅ 블록이 한 코드블록 안에 인라인 병기된다
  - 근거와 공통 규칙 링크가 있다
  - page topbar 소유 규칙 본문을 재서술하지 않는다
  - 1·2인칭 서술이 없다
  - CRITICAL/ALWAYS/NEVER 남발이 없다
  - 링크·API 레퍼런스가 현행 소스와 일치한다
