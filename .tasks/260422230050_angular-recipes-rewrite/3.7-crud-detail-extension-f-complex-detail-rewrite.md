# Feature 3.7 `crud-detail/extension-f-complex-detail.md` 재작성

## 참조 자료

- WBS: @.tasks/260422230050_angular-recipes-rewrite/wbs.md (Feature 3.7)
- 지침: @.claude/skills/sd-claude-docs/references/package-docs.md — T1(Recipe) 템플릿, 반-기법, 매뉴얼 품질 검증 축
- 선행 Feature 산출물:
  - 공통 규칙: @packages/angular/docs/recipes/_common-rules.md (Feature 1.1 완료본)
  - 진입점(최소 뼈대): @packages/angular/docs/recipes/crud-detail.md (Feature 3.1 완료본)
  - 선행 확장: @packages/angular/docs/recipes/crud-detail/extension-a-edit-save.md (Feature 3.2 완료본)
  - 동일 diff 스타일 sibling: @packages/angular/docs/recipes/crud-list/extension-a-inline-edit.md (Feature 2.2 완료본, `oneWayDiffs` + `isDeleted` 플래그 `🚫 흔한 실수` ❌/✅ 블록 보유)
- 원문: @packages/angular/docs/recipes/crud-detail/extension-f-complex-detail.md (187줄, 구지침 diff 스타일)
- 소스 정합성:
  - @packages/angular/src/data/sheet/sd-sheet.ts — `<sd-sheet>` `items`/`trackByFn`/`getItemCellStyleFn` input 시그니처
  - @packages/angular/src/data/sheet/sd-sheet-column.ts — `<sd-sheet-column>` `[key]`/`[header]`/`[fixed]` input
  - @packages/angular/src/data/sheet/sd-sheet-column-cell-template.ts — `<ng-template [cell]="…" let-item="item">` 바인딩
  - @packages/angular/src/controls/button/sd-anchor.ts — `<sd-anchor [theme]>`
  - @packages/core-common/src/types/uuid.ts — `Uuid.generate()` 반환 타입
  - @packages/core-common/src/extensions/arr-ext.types.ts:206 — `oneWayDiffs<TKey>(orgItems, keyPropNameOrGetValFn, options?)` 시그니처
  - @packages/core-common/src/extensions/arr-ext.ts:412 — `oneWayDiffs` 구현 (`type: "create" | "update" | "same"`)
  - @packages/angular/src/core/mark.ts — `mark(sig)` shallow copy notify 역할
- 네이밍/컨벤션: @packages/angular/CLAUDE.md Component Structure, Array 확장 side-effect import 규약 (@packages/core-common/CLAUDE.md "프로토타입 확장 활성화")

## 기준 코드

(이 Feature 구현 시 따라야 할 기존 문서 패턴)

### 유사 기능: Feature 3.2 재작성 완료본 `extension-a-edit-save.md`

- 파일 구조: `.tasks/260422230050_angular-recipes-rewrite/3.2-crud-detail-extension-a-edit-save-rewrite.md:192-264` — diff 스타일 extension 템플릿 레퍼런스
  - L1: `← [CRUD 상세폼 레시피 진입점](../crud-detail.md)` 브레드크럼
  - L3: `# 확장 F: 복합 상세 (내부 <sd-sheet>)` 제목
  - L5: `> **선행:** [확장 A: 편집/저장](./extension-a-edit-save.md)` 블록쿼트
  - L7: 1~2문장 요약 (무엇을 도입하는가)
  - L9: (조건부) `> **적용 조건:**` 블록쿼트 — 해당 없음(확장 F는 상호 배타적 분기가 아님)
  - L11: `**이 확장이 도입하는 요소:**` 헤더 + 불릿 (imports / 타입 확장 / 시트 함수 / 메서드 / 템플릿 / onSubmit 변경)
  - L18-180: 번호가 붙은 코드 블록 (`// 1) imports 추가`, `// 2) …`)
  - L182-188: `**포인트:**` 불릿 (주의사항·근거·공통 규칙 1-hop 링크)
  - `🚫 흔한 실수` 서브섹션 — ❌/✅ 블록 + 근거 1줄 + 공통 규칙 링크

### 유사 기능: Feature 2.2 재작성 완료본 `extension-a-inline-edit.md`

- `packages/angular/docs/recipes/crud-list/extension-a-inline-edit.md:375-402` — 동일한 "배열에서 row 물리 제거 → `oneWayDiffs` delete 미감지" ❌/✅ 블록 선례. 확장 F도 하위 컬렉션 row 제거에 같은 패턴 적용 (맥락은 상위 items → 하위 `boxes` 컬렉션으로 치환).

### 선행 3.2 최소 뼈대 + 확장 A 기준 일치 대상

- `packages/angular/docs/recipes/crud-detail/extension-a-edit-save.md:15-190` — 확장 F는 확장 A의 상태·파생·메서드를 재사용한다:
  - 확장 A L56 `perms = injectPermsSignal(["sales.customer"], ["use", "edit"])` → 변경 없음
  - 확장 A L59 `canEdit = computed(() => this.perms().includes("edit"))` → 변경 없음
  - 확장 A L61 `private _dataSnapshot?: ICustomer` → `ICustomer.boxes` 확장 후에도 동일 타입
  - 확장 A L133-155 `onSubmit()` → `_sdToast.try(...)` 블록 **내부만** 교체(하위 컬렉션 diff 계산 + 일괄 제출)
  - 확장 A L122 `<sd-form #formCtrl (formSubmit)="onSubmit()" class="flex-fill">` → 외곽 `<div class="flex-column fill">` + 내부 sheet 중첩 구조로 교체
- 진입점 3.1 `data = signal<ICustomer>({ ... })` 초기값 → `boxes: []` 필드 추가

### 공통 규칙 반영

- `_common-rules.md` "시트 셀 내부 컨트롤에 `[inset]="true" [size]="'sm'"`을 명시한다" 섹션 존재 — 확장 F는 본문에 재서술하지 않고 **포인트 불릿 1줄 + 1-hop 링크**로 위임한다.
- `_common-rules.md` "`mark(sig)`를 '저장 감지' 수단으로 사용하지 않는다" 섹션 존재 — 확장 F는 `mark(this.data)` 사용처에 1줄 요약 + 링크로 위임한다.
- `_common-rules.md` "삭제 방식은 DB 스키마에 따라 결정한다" 섹션 존재 — 확장 F의 하위 컬렉션 `isDeleted` 플래그는 **상위 테이블 soft-delete 여부와 무관**하다는 점을 고유 포인트로 강조 (공통 규칙의 상위 테이블 선택 기준과 다른 맥락).

### 프로젝트 컨벤션

- `oneWayDiffs`는 `@simplysm/core-common` side-effect import로 활성화: `import "@simplysm/core-common";` [근거: @packages/core-common/CLAUDE.md "프로토타입 확장 활성화", @packages/core-common/src/extensions/arr-ext.ts:412]
- `Uuid.generate()` 반환은 `Uuid` 인스턴스이므로 문자열 key로 쓰려면 `.toString()` 호출 [근거: @packages/core-common/src/types/uuid.ts]
- Component 공통 패턴: `@Component({ changeDetection: OnPush, encapsulation: None, standalone: true, ... })` [근거: @packages/angular/CLAUDE.md:91-108]
- 링크 경로: 형제 확장은 `./extension-*.md`, 진입점은 `../crud-detail.md`, 공통 규칙은 `../_common-rules.md` 상대 경로 [근거: `extension-a-edit-save.md:1,204`]

### 설계 결정

| #   | 결정사항                                                                 | 선택                                                                                                                                                   | 근거                                                                                                                                                                           |
| --- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | extension recipe의 전체 템플릿                                           | **기존 diff 스타일 유지** (브레드크럼 / 제목 / 선행 / 요약 / 도입 요소 / 번호 코드 / 포인트 / 🚫 흔한 실수)                                             | 이미 재작성된 sibling `extension-a-edit-save.md`(Feature 3.2)·`extension-a-inline-edit.md`(Feature 2.2)와 동일 템플릿. T1 진입점 템플릿은 진입점(3.1) 전용                    |
| D2  | Anti-patterns 섹션 구성                                                  | **`boxes` 하위 컬렉션 row 물리 제거 → `oneWayDiffs` delete 미감지 1건**만 ❌/✅ 블록으로 승격. `[inset]`/`[size]` 누락은 공통 규칙 링크로 위임        | 사용자 결정. WBS 3.7 inset/size anti-pattern은 `_common-rules.md`에 이미 ❌/✅ 블록 존재 → 중복 금지. `oneWayDiffs` delete 미감지는 하위 컬렉션 맥락에서 실제 LLM 회귀 지점    |
| D3  | sibling 2.2의 "items 물리 제거" ❌/✅ 블록과의 차별화                   | **맥락을 하위 컬렉션(`data().boxes`)으로 치환**하여 재수록. 2.2의 상위 `items` 배열이 아닌, 상세 폼 내부 sub-collection의 `isDeleted` 플래그 강조      | sub-collection 편집은 crud-list 본체와 의미가 달라 독자가 상위 `items` 패턴만 알고 있으면 하위 컬렉션에서 같은 오류를 반복 가능. 맥락 재수록이 self-contained 원칙에 부합       |
| D4  | 상위 테이블 `isDeleted` 유무와 하위 컬렉션 `isDeleted` 플래그의 관계     | **하위 컬렉션은 항상 `isDeleted` 플래그 사용**(상위 테이블 스키마와 무관)을 포인트 불릿로 명시                                                         | `oneWayDiffs`가 delete 타입을 지원하지 않는 **기술적 제약**에서 비롯 [근거: arr-ext.types.ts:206]. 상위 테이블의 soft-delete 선택 기준(공통 규칙)과 혼동 방지                  |
| D5  | 클라이언트 UUID 생성 설명                                                | **포인트 불릿로 유지** (별도 ❌/✅ 블록 승격하지 않음)                                                                                                  | 신규 row `id: Uuid.generate().toString()`은 `trackByFn` + `oneWayDiffs` key 요건이며 독자가 흔히 놓치는 지점이 아님. 반-기법 "섹션 남발" 회피                                  |
| D6  | `confirm("삭제 표시된 박스가 있습니다...")` UX 패턴                      | **코드에 유지 + 포인트 1줄 주석**                                                                                                                     | 삭제 플래그 혼재 시 사용자 확인은 실전 UX 요건. 원문(L159-161)에 이미 존재, 제거 시 회귀 위험                                                                                  |
| D7  | 도메인 예시                                                              | `ICustomer.boxes: ICustomerBox[]` 유지 (원문과 동일, 진입점 3.1 및 확장 A와 일관)                                                                      | 독자 연속성. Feature 3.2 D7과 동일 판단                                                                                                                                       |
| D8  | 코드 예시 범위                                                           | **diff만 노출** — 확장 A 위에 실제 변경·추가되는 imports·타입 확장·template 치환·메서드·`onSubmit` 내부 블록만 발췌                                   | self-contained 완전 컴포넌트를 재복사하면 진입점·확장 A와 중복 → 매뉴얼 품질 검증 축 "중복 금지" 위반                                                                         |
| D9  | `_sdToast` confirm 처리 방식                                             | **네이티브 `confirm` 유지** (원문 동일)                                                                                                                | 진입점·확장 A와 동일 패턴. 모달 provider를 끌어다 쓰지 않아 독자 학습 부담 최소                                                                                                |
| D10 | `oneWayDiffs` delete 미감지의 예외 — "신규 row는 물리 제거 허용"         | **Anti-pattern 블록 내부에 예외 주석으로 포함** (sibling 2.2와 동일 노출)                                                                              | 신규 row(id가 snapshot에 없음)는 diff가 발생하지 않으므로 물리 제거 안전. 이 예외를 누락하면 "모든 제거 금지"로 오해 [근거: extension-a-inline-edit.md:394-399]                  |

## 요구명세

```gherkin
Feature: 3.7 crud-detail/extension-f-complex-detail.md 재작성

  Background:
    Given 이 문서의 주 독자는 소비 프로젝트의 Claude Code(LLM 에이전트)이다
    And 타겟 파일은 packages/angular/docs/recipes/crud-detail/extension-f-complex-detail.md이다
    And Feature 1.1의 `_common-rules.md`가 T3 형식으로 존재한다
    And Feature 3.1의 `crud-detail.md` 진입점이 T1 형식으로 존재한다
    And Feature 3.2의 `extension-a-edit-save.md`가 diff 스타일 템플릿으로 존재한다

  Rule: extension recipe는 sibling과 동일한 diff 스타일 템플릿을 따른다
    # 근거: 설계 결정 D1, sibling `extension-a-edit-save.md` / `extension-a-inline-edit.md`

    Scenario: 파일 상단 6개 구성요소가 순서대로 있다
      Then 1행에 진입점으로의 브레드크럼 (`← [CRUD 상세폼 레시피 진입점](../crud-detail.md)`)이 있다
      And `# 확장 F: 복합 상세 (내부 <sd-sheet>)` 제목이 있다
      And `> **선행:** [확장 A: 편집/저장](./extension-a-edit-save.md)` 블록쿼트가 있다
      And 1~2문장 요약 문단이 있다
      And `**이 확장이 도입하는 요소:**` 불릿 블록이 있다
      And 번호 주석(`// 1)`, `// 2)`, …)이 붙은 TypeScript 코드 블록이 있다
      And 코드 블록 뒤 `**포인트:**` 불릿이 있다

    Scenario: self-contained 컴포넌트를 중복 복사하지 않는다
      Then 코드 블록은 확장 A 위에서 **변경·추가되는 diff**만 노출한다
      And 진입점 3.1·확장 A와 동일한 인터페이스·상태·메서드 본문을 재서술하지 않는다

  Rule: 확장 A 위의 diff가 정확하다
    # 근거: WBS "선행 A에 얹기", 확장 A `extension-a-edit-save.md:15-190`

    Scenario: imports 추가
      Then `SdSheet`, `SdSheetColumn`, `SdSheetColumnCellTemplate`, `SdAnchor`가 `@simplysm/angular`에서 추가 import된다
      And `Uuid`가 `@simplysm/core-common`에서 추가 import된다
      And `tablerCirclePlus` 아이콘이 추가 import된다
      And `import "@simplysm/core-common";` side-effect import가 `oneWayDiffs` 활성화용으로 추가된다

    Scenario: 데이터 타입 확장
      Then `ICustomer` 인터페이스에 `boxes: ICustomerBox[]` 필드가 추가된다
      And `interface ICustomerBox { id: string; seq: number; note: string; isDeleted: boolean; }`가 신설된다
      And `data` signal 초기값에 `boxes: []`가 추가되는 변경이 언급된다

    Scenario: 시트 함수·메서드·템플릿 추가
      Then `boxTrackByFn`, `getBoxCellStyleFn` 필드가 추가된다
      And `onAddBoxButtonClick`, `onToggleDeleteBoxButtonClick` 메서드가 추가된다
      And template의 main 영역 `<sd-form>` 내부 구조가 [단일 필드 블록 + 하위 컬렉션 도구 dock + 내부 `<sd-sheet>`]로 교체되는 diff가 있다

    Scenario: onSubmit 내부 블록 교체
      Then 확장 A의 `onSubmit` `_sdToast.try(...)` 블록 내부가 하위 컬렉션 diff 계산 + 일괄 제출로 교체된다
      And 블록 선두에 `if (this.data().boxes.some((b) => b.isDeleted)) { if (!confirm("...")) return; }` 확인이 있다
      And `const boxDiffs = this.data().boxes.oneWayDiffs(snapshotBoxes, "id");` 호출이 있다
      And 앱별 ORM 호출 주석이 create/update 분기를 보여준다

  Rule: 시그니처 정합성을 재검증한다
    # 근거: package-docs.md "정확성 검증", WBS 경계

    Scenario: @simplysm/angular 시트 API가 소스와 일치한다
      Then `<sd-sheet [items] [trackByFn] [getItemCellStyleFn]>` 바인딩이 `sd-sheet.ts:567,568,581`과 일치한다
      And `<sd-sheet-column [fixed] [key] [header]>` 바인딩이 `sd-sheet-column.ts`와 일치한다
      And `<ng-template [cell]="..." let-item="item">` 바인딩이 `sd-sheet-column-cell-template.ts`와 일치한다

    Scenario: oneWayDiffs 시그니처가 소스와 일치한다
      Then `data().boxes.oneWayDiffs(snapshotBoxes, "id")` 호출 형태가 `arr-ext.types.ts:206` 시그니처와 일치한다
      And 반환 `type`이 `"create" | "update" | "same"` 3종뿐임을 본문에서 명시한다

    Scenario: Uuid.generate 호출 형태가 소스와 일치한다
      Then `Uuid.generate().toString()`으로 문자열 id를 생성한다

  Rule: ❌/✅ 하위 컬렉션 row 물리 제거 anti-pattern을 1건 추가한다
    # 근거: WBS 3.7 범위의 "내부 시트 inset/size 누락 금지(공통 규칙 참조)" + 사용자 결정 D2(하위 컬렉션 물리 제거 ❌/✅ 승격)

    Scenario: 포인트 섹션 뒤에 별도 🚫 흔한 실수 서브섹션을 둔다
      Then 코드/포인트 이후 `**🚫 흔한 실수**` 스타일 섹션이 있다
      And "### `data().boxes`에서 기존 row를 물리 제거한다" anti-pattern이 포함된다
      And ❌ 예시와 ✅ 예시가 코드블록에 나란히 제시된다
      And ✅ 예외 주석으로 "신규 row(id가 snapshot에 없음)는 물리 제거해도 diff가 발생하지 않아 안전"이 포함된다
      And 근거 1줄과 `→ [공통 규칙: 삭제 방식](../_common-rules.md#삭제-방식은-db-스키마에-따라-결정한다)` 링크가 있다

  Rule: 공통 규칙 중복을 금지한다
    # 근거: WBS Feature 1.1 T3 `_common-rules.md` 위임 원칙

    Scenario: 중복된 규칙 본문을 포함하지 않는다
      Then 시트 셀 `[inset]/[size]` 규칙, `mark` 저장 감지 금지, soft-delete 선택 기준은 본문에 재서술하지 않는다
      And 필요한 곳에서 `_common-rules.md` 링크로 1-hop 위임한다
      And 상위 테이블 `isDeleted` 유무와 하위 컬렉션 `isDeleted` 플래그의 **차이**는 재서술 가치가 있으므로 포인트 1줄로 명시한다

  Rule: 3인칭 서술·반-기법 회피
    # 근거: package-docs.md 매뉴얼 품질 검증

    Scenario: 1·2인칭 서술이 없다
      Then "you can", "i can", "당신이", "저희가" 같은 표현이 없다
      And "추가한다", "중첩한다", "교체한다" 형태로 서술한다

    Scenario: CRITICAL 남발이 없다
      Then CRITICAL / ALWAYS / NEVER 키워드가 2회 이하로 등장한다
```

## 구현계획

### 배경

현행 `packages/angular/docs/recipes/crud-detail/extension-f-complex-detail.md`(187줄)는 구지침 시절 작성되어 (a) `oneWayDiffs` delete 미감지를 포인트 산문 문단으로만 설명하고 ❌/✅ 대비가 없으며, (b) 진입점 `crud-detail.md`의 구버전 `§13 규칙 1/2` anchor를 참조하고, (c) 하위 컬렉션 `isDeleted` 플래그와 상위 테이블 `isDeleted` 컬럼의 관계가 포인트 산문에 섞여 있어 독자가 혼동하기 쉽다. Feature 1.1 완료본(`_common-rules.md`)·Feature 3.1·3.2 완료본을 기준으로, sibling Feature 3.2(`extension-a-edit-save.md`) / Feature 2.2(`extension-a-inline-edit.md`)가 이미 확정한 diff 스타일 extension 템플릿과 ❌/✅ anti-pattern 노출 규범을 준수하여 재정렬한다. 기존 diff 코드의 내용(하위 컬렉션 시트 중첩, `oneWayDiffs` 일괄 저장, 클라이언트 UUID, `isDeleted` 플래그)은 소스 정합 검증을 거쳐 **보존**하되, 참조 링크·anti-pattern 노출 방식·3인칭 서술·공통 규칙 위임만 개정한다.

### 목표

- sibling extension-a(3.2·2.2)와 동일한 diff 스타일 템플릿을 갖춤 [근거: Rule "sibling과 동일한 diff 스타일 템플릿"]
- 확장 A 위의 diff가 정확히 정합(imports·타입 확장·template·onSubmit 내부 블록) [근거: Rule "확장 A 위의 diff가 정확하다"]
- `data().boxes` 하위 컬렉션 row 물리 제거 오류를 ❌/✅ 나란히 블록 + 근거 + 공통 규칙 링크로 전환 (신규 row 물리 제거 예외 주석 포함) [근거: 사용자 결정 D2·D10]
- 구버전 진입점 anchor(`crud-detail.md#규칙-1-…`, `crud-detail.md#규칙-2-…`) 제거 및 `_common-rules.md` 링크로 대체 [근거: 설계 결정]
- `@simplysm/angular` 시트 API / `oneWayDiffs` / `Uuid.generate` 소스 시그니처 재검증 [근거: Rule "시그니처 정합성"]
- 187줄 → 대략 190~220줄 범위 (anti-pattern ❌/✅ 블록 추가 상쇄)

### 비목표

- 본 확장의 **동작 변경**(예: `oneWayDiffs` key 전략, 신규 row UUID 생성 방식, `isDeleted` 토글 UX) — 현행 설계 유지 [근거: WBS 3.7 범위에 동작 변경 없음]
- 확장 B(삭제/복구) / C(modal) / D(control) / E(보조) 파일 수정 — 각자 Feature 3.3~3.6 범위
- `_common-rules.md` 내용 개정 — Feature 1.1에서 확정 완료
- 진입점 3.1 / 확장 A 3.2 파일 수정 — 각자 Feature 완료됨
- 신규 anti-pattern 발굴(예: 시트 외곽에 `<sd-form>` 누락, `boxTrackByFn` 누락) — 현행 포인트 수준 유지

### 설계

#### 파일 구조 (sibling과 동일 템플릿)

```markdown
← [CRUD 상세폼 레시피 진입점](../crud-detail.md)

# 확장 F: 복합 상세 (내부 `<sd-sheet>`)

> **선행:** [확장 A: 편집/저장](./extension-a-edit-save.md)

확장 A(편집/저장)를 전제로, 상세 폼 안에 **하위 컬렉션**(박스 목록, 품목 라인 등)을 편집하는 구조를 도입한다. `<sd-form>` 본문 내부에 `<sd-sheet>`를 중첩하고, 하위 컬렉션의 행 추가·수정·삭제는 `item.isDeleted = true` 플래그로 soft-delete하여 `ArrayOneWayDiffResult` 기반 일괄 저장에 포함시킨다.

**이 확장이 도입하는 요소:**

- **imports:** `SdSheet`, `SdSheetColumn`, `SdSheetColumnCellTemplate`, `SdAnchor`(신규 행 삭제 아이콘), `Uuid`, `oneWayDiffs`(side-effect import), `tablerCirclePlus` (`mark`는 확장 A에서 이미 도입)
- **데이터 타입 확장:** `ICustomer.boxes: ICustomerBox[]` + `interface ICustomerBox { id: string; seq: number; note: string; isDeleted: boolean; }`
- **시트 함수 (클래스 필드):** `boxTrackByFn`, `getBoxCellStyleFn`
- **메서드:** `onAddBoxButtonClick`, `onToggleDeleteBoxButtonClick`
- **템플릿:** main 영역 `<sd-form>` 내부에 [상단 단일 필드 블록 + 하위 컬렉션 도구 dock(`<sd-button>` "박스 추가") + 하위 `<sd-sheet>` 중첩] 구조로 교체
- **onSubmit 변경:** `_sdToast.try(...)` 블록 내부를 diff 계산(`data().boxes.oneWayDiffs(_dataSnapshot?.boxes, "id")`) + 일괄 제출로 교체
- **data 초기값:** `boxes: []` 필드 추가

> 상세: [`<sd-sheet>`](../../ui-data/sd-sheet.md) · [`<sd-sheet-column>`](../../ui-data/sd-sheet.md#sdsheetcolumn) · [`[cell]`](../../ui-data/sd-sheet.md#sdsheetcolumncelltemplate) · [`<sd-anchor>`](../../ui-form/sd-anchor.md)

\`\`\`typescript
// 1) imports 추가 — @simplysm/angular에 {SdSheet, SdSheetColumn, SdSheetColumnCellTemplate, SdAnchor} 추가.
//    @simplysm/core-common에서 {Uuid} 추가 + 프로토타입 확장 활성화용 side-effect import.
//    아이콘에 tablerCirclePlus 추가. (mark는 확장 A에서 이미 import됨)

// 2) 데이터 타입 확장 — ICustomer에 boxes: ICustomerBox[] 필드 추가, ICustomerBox 신설

// 3) data 초기값에 boxes: [] 추가

// 4) template — main 영역(<sd-dock-container> 안쪽 <sd-form> 내부)을 [단일 필드 + 도구 dock + 시트]로 교체

// 5) 시트 함수 (클래스 필드) 추가 — boxTrackByFn / getBoxCellStyleFn

// 6) 메서드 추가 — onAddBoxButtonClick / onToggleDeleteBoxButtonClick

// 7) onSubmit의 _sdToast.try(...) 블록 내부 교체 — boxDiffs 계산 + 일괄 저장
\`\`\`

**포인트:**

- **하위 컬렉션의 삭제는 `isDeleted: true` 플래그로 표현한다.** 이것은 상위 테이블의 삭제 방식(DB Table에 `isDeleted` 컬럼 유무로 결정 — [공통 규칙: 삭제 방식](../_common-rules.md#삭제-방식은-db-스키마에-따라-결정한다))과 **무관**하게, `oneWayDiffs`가 `"delete"` 타입을 지원하지 않기 때문에 하위 컬렉션에서는 항상 이 방식을 사용한다. `data().boxes`에서 row를 물리적으로 제거하면 `oneWayDiffs`가 해당 row를 감지하지 못한다(`type: "create" | "update" | "same"`만 반환, `packages/core-common/src/extensions/arr-ext.types.ts:206`). 서버는 `isDeleted: true` row를 soft-delete 또는 물리 삭제로 처리한다.
- **시트 셀 내부 컨트롤은 `[inset]="true" [size]="'sm'"` 명시 필수** — [공통 규칙: 시트 셀 `[inset]/[size]`](../_common-rules.md#시트-셀-내부-컨트롤에-insettrue-sizesm을-명시한다).
- **`data().boxes.push(newBox)` 같은 배열 mutation 후에는 `mark(this.data)`로 signal 참조를 갱신한다** — OnPush 템플릿 재렌더링용 통지. 값 비교(저장 감지)와는 별개: [공통 규칙: `mark(sig)`…](../_common-rules.md#marksig를-저장-감지-수단으로-사용하지-않는다).
- **`id`는 클라이언트에서 UUID로 생성** — `Uuid.generate().toString()`으로 문자열 key를 만든다. `trackByFn` + `oneWayDiffs`의 key로 사용한다. 서버가 발급한 PK가 별도로 있다면 별도 컬럼으로 관리하고 클라이언트 UUID는 row 식별자로만 쓴다.
- **삭제 플래그 혼재 시 `confirm`** — `this.data().boxes.some((b) => b.isDeleted)`이면 저장 직전 사용자 확인을 요청한다.

**🚫 흔한 실수**

> 공통 규칙(시트 셀 `[inset]/[size]`, `mark` 오용, 상위 테이블 soft-delete 선택 기준)은 [레시피 공통 규칙](../_common-rules.md)을 참조한다. 이 섹션은 **복합 상세(하위 컬렉션) 확장 고유 실수**만 다룬다.

### `data().boxes`에서 기존 row를 물리 제거한다

\`\`\`typescript
// ❌ 기존 row를 하위 컬렉션 배열에서 제거 — oneWayDiffs는 delete를 반환하지 않으므로
//    저장 시 서버는 이 row가 사라진 사실을 감지하지 못한다.
onRemoveBox(box: ICustomerBox): void {
  this.data.update((d) => ({
    ...d,
    boxes: d.boxes.filter((b) => b.id !== box.id),
  }));
}

// ✅ 삭제 의사는 isDeleted 플래그로 표현 → diff의 type: "update"로 전송
onToggleDeleteBoxButtonClick(box: ICustomerBox): void {
  box.isDeleted = !box.isDeleted;
  mark(this.data);  // OnPush 재렌더링 통지
}

// ✅ 예외: 신규 row(snapshot에 없는 클라이언트 UUID)는 저장 포기 시 물리 제거해도 된다
//    — snapshot에 존재하지 않으므로 diff 자체가 발생하지 않는다.
onCancelNewBox(box: ICustomerBox): void {
  this.data.update((d) => ({
    ...d,
    boxes: d.boxes.filter((b) => b.id !== box.id),
  }));
}
\`\`\`

**근거**: `oneWayDiffs`의 반환 `type`은 `"create" | "update" | "same"` 3종뿐이며 delete를 다루지 않는다(`packages/core-common/src/extensions/arr-ext.types.ts:206`). 기존 row를 화면에서 제거하면 diff가 발생하지 않아 서버 저장 경로를 우회한다. 하위 컬렉션 삭제는 상위 테이블의 soft-delete 여부와 무관하게 항상 `isDeleted` 플래그를 쓴다 — [공통 규칙: 삭제 방식](../_common-rules.md#삭제-방식은-db-스키마에-따라-결정한다)은 상위 테이블 단위 결정이며, 하위 컬렉션은 `oneWayDiffs` 기술적 제약이 원인이다.
```

#### 수정 · 축약 내역

- **참조 링크 교체:** 구버전 `../crud-detail.md#규칙-1-시트-셀-내부-컨트롤은-insettrue-sizesm-명시` · `../crud-detail.md#규칙-2-marksig는-저장-감지가-아니라-ui-동기화` → 각각 `../_common-rules.md#시트-셀-내부-컨트롤에-insettrue-sizesm을-명시한다` · `../_common-rules.md#marksig를-저장-감지-수단으로-사용하지-않는다`
- **포인트 재구성:** 4건 유지 + 삭제 플래그 혼재 `confirm` 1줄 추가. "하위 컬렉션 삭제는 상위 테이블 삭제 방식과 무관"은 포인트 1번에 명시 유지.
- **Anti-patterns 섹션 신설:** 포인트 뒤에 `**🚫 흔한 실수**` 서브섹션 추가, "`data().boxes` 물리 제거" 1건을 ❌/✅ 블록 + 신규 row 예외 + 근거 + 공통 규칙 링크로 기술
- **요약 문장 3인칭 정돈:** "… 도입한다"·"… 중첩한다"·"… 포함시킨다" 형식 유지
- **코드 주석 번호·변수명 유지:** 기능 동작 변경 없음. 단 번호 분할을 현행 5단계 → 7단계로 재배치해 sibling 2.2·3.2 스타일과 근접시킴

#### 소스 재검증 체크리스트

- `<sd-sheet>` input 시그니처 — `items: input<T[]>([])`, `trackByFn: input<(item: T, index: number) => unknown>`, `getItemCellStyleFn: input<(item: T, colKey: string) => string | undefined>` [근거: `packages/angular/src/data/sheet/sd-sheet.ts:567,568,581`] → 한 인자만 사용하는 기존 예시는 parameter-count 호환으로 유효
- `<sd-sheet-column>` `[fixed]` / `[key]` / `[header]` input [근거: `packages/angular/src/data/sheet/sd-sheet-column.ts`]
- `<ng-template [cell]="..." let-item="item">` 바인딩 [근거: `packages/angular/src/data/sheet/sd-sheet-column-cell-template.ts`]
- `oneWayDiffs<TKey>(orgItems, keyPropNameOrGetValFn, options?)` — 반환 `ArrayOneWayDiffResult<TItem>[]`의 `type`은 `"create" | "update" | "same"` [근거: `packages/core-common/src/extensions/arr-ext.types.ts:206`, `packages/core-common/src/extensions/arr-ext.ts:412`]
- `Uuid.generate()` — `Uuid` 인스턴스 반환, 문자열 필요 시 `.toString()` [근거: `packages/core-common/src/types/uuid.ts`]
- 프로토타입 확장 활성화 — `import "@simplysm/core-common";` [근거: `packages/core-common/CLAUDE.md` "프로토타입 확장 활성화"]
- `mark(sig)` 참조 경로 (`@simplysm/angular`) — 확장 A에서 이미 import됨, 확장 F에서는 `mark(this.data)` 호출부만 추가

### 대안 검토

- **대안 1: T1 진입점 템플릿(When to use / 전제조건 / 변형 / Anti-patterns / 관련 Entry)으로 확장까지 승격**
  - 기각 사유: 이미 재작성된 sibling `extension-a-edit-save.md`(Feature 3.2)·`extension-a-inline-edit.md`(Feature 2.2)가 diff 스타일을 선택했다. 같은 폴더 확장끼리 템플릿을 혼용하면 독자가 "이 파일 형식이 왜 다른가"를 학습해야 하는 추가 인지 부담이 발생한다.
- **대안 2: `inset`/`size` 누락 anti-pattern을 고유 ❌/✅ 블록으로 재수록**
  - 기각 사유(사용자 결정 D2): `_common-rules.md`에 이미 ❌/✅ 블록 존재 → 중복 금지. 포인트 1-hop 링크로 충분.
- **대안 3: `oneWayDiffs` delete 미감지를 포인트 문단으로만 유지 (❌/✅ 승격 없음)**
  - 기각 사유(사용자 결정 D2): 포인트 산문은 ❌/✅ 대비보다 시각적 식별성이 낮고, sibling 2.2에서 같은 안티패턴을 ❌/✅ 블록으로 서술한 선례가 있다. 확장 F는 하위 컬렉션 맥락으로 재수록하여 독자가 상위 items 패턴만 알고 있을 때 발생하는 회귀를 차단한다.
- **대안 4: 신규 row 예외("snapshot에 없으므로 물리 제거 안전")를 주석에서 제거**
  - 기각 사유(사용자 결정 D10): 예외를 누락하면 "모든 제거 금지"로 오해되어 `onCancelNewBox` 구현이 불가해진다. sibling 2.2가 같은 예외를 포함.

### Vertical Slicing

이 Feature는 단일 Markdown 파일 재작성이며 외부 소비자 API를 변경하지 않으므로, 의존 단계는 순차 1개로 충분하다.

#### [x] Slice 1: 파일 전체 재작성 (템플릿 정합 + diff 본문 + Anti-patterns 블록)

- **구현 내용:**
  1. 상단 구성(브레드크럼 / 제목 / 선행 / 요약 / 도입 요소) 정리 — sibling 3.2·2.2 구조와 일치.
  2. 코드 블록(번호 주석 5단계 → 7단계로 재배치) — imports / 데이터 타입 확장 / data 초기값 / template / 시트 함수 / 메서드 / onSubmit 내부 교체. 주석을 3인칭 서술로 미세 조정, 확장 A 위 diff 정합성 재확인.
  3. **포인트** 불릿을 sibling 스타일로 축약·3인칭화. `[inset]/[size]`·`mark` 관련 포인트는 공통 규칙 링크 1-hop으로 축소. "하위 컬렉션 삭제는 상위 테이블 삭제 방식과 무관"을 포인트 1번에 명시. 삭제 플래그 혼재 `confirm` 포인트 1줄 추가.
  4. **🚫 흔한 실수** 서브섹션 신설 — "### `data().boxes`에서 기존 row를 물리 제거한다" 한 건을 ❌/✅ 블록(신규 row 예외 주석 포함) + 근거 + 공통 규칙 링크로 기술.
  5. 구버전 진입점 anchor(`../crud-detail.md#규칙-1-…`, `../crud-detail.md#규칙-2-…`) 제거 및 `../_common-rules.md#…`로 링크 재지향.
  6. 소스 시그니처 재검증(`<sd-sheet>` input, `oneWayDiffs` 반환 type, `Uuid.generate().toString()`, side-effect import).
- **Scenarios:**
  - 파일 상단 6개 구성요소가 순서대로 있다
  - self-contained 컴포넌트를 중복 복사하지 않는다
  - imports 추가 / 데이터 타입 확장 / 시트 함수·메서드·템플릿 추가 / onSubmit 내부 블록 교체
  - @simplysm/angular 시트 API·`oneWayDiffs`·`Uuid.generate`가 소스와 일치한다
  - 포인트 섹션 뒤에 별도 🚫 흔한 실수 서브섹션을 둔다 (신규 row 예외 주석 포함)
  - 중복된 규칙 본문을 포함하지 않는다
  - 1·2인칭 서술이 없다 / CRITICAL 남발이 없다
