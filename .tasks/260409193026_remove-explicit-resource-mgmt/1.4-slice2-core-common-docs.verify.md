# Slice 2: core-common 패키지 문서 업데이트 — LLM 검증

## 검증 항목

### Scenario: core-common 문서의 using 코드 예시 변경
- [x] packages/core-common/CLAUDE.md: `using` 코드 예시 2건이 try-finally + dispose() 패턴으로 변경됨 — grep 결과 0건
- [x] .claude/references/sd-simplysm14/core-common/usage.md: `using` 코드 예시 1건이 try-finally + dispose() 패턴으로 변경됨 — grep 결과 0건
- [x] .claude/references/sd-simplysm14/core-common/docs/features.md: `using` 코드 예시 2건이 try-finally + dispose() 패턴으로 변경됨 — grep 결과 0건
- [x] .claude/references/sd-simplysm14/core-common/docs/types.md: `using` 코드 예시 1건이 try-finally + dispose() 패턴으로 변경됨 — grep 결과 0건
- [x] .claude/references/sd-simplysm14/core-common/docs/utils.md: `await using` 코드 예시 3건이 try-finally + await close() 패턴으로 변경됨 — grep 결과 0건

### Scenario: core-common features.md에서 Symbol.dispose 시그니처 및 설명 제거
- [x] EventEmitter 시그니처에서 `[Symbol.dispose]()` 행 제거됨 — grep 결과 0건
- [x] DebounceQueue 시그니처에서 `override [Symbol.dispose]()` 행 제거됨 — grep 결과 0건
- [x] SerialQueue 시그니처에서 `override [Symbol.dispose]()` 행 제거됨 — grep 결과 0건
- [x] 메서드 테이블에서 `[Symbol.dispose]()` 행 제거됨 — grep 결과 0건

### Scenario: core-common types.md에서 Symbol.dispose 시그니처 및 설명 제거
- [x] LazyGcMap 메서드 테이블에서 `[Symbol.dispose]()` 행 제거됨 — grep 결과 0건

### Scenario: core-common utils.md에서 Symbol.asyncDispose 시그니처 및 설명 제거
- [x] ZipArchive 시그니처에서 `[Symbol.asyncDispose]()` 행 제거됨 — grep 결과 0건
- [x] ZipArchive 메서드 테이블에서 `[Symbol.asyncDispose]()` 행 제거됨 — grep 결과 0건

### Scenario: core-common 문서의 리소스 관리 안내 문구 변경
- [x] packages/core-common/CLAUDE.md: `using` 참조가 try-finally + dispose() 안내로 변경됨 — "반드시 `try-finally` 블록에서 `dispose()`를 호출한다" 확인
- [x] .claude/references/sd-simplysm14/core-common/docs/types.md: `using` 참조가 try-finally + dispose() 안내로 변경됨 — "반드시 `try-finally` 블록에서 `dispose()`를 호출해야" 확인
