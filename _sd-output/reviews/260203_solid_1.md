# solid 패키지 코드 리뷰

## 개요

- **리뷰 대상**: `packages/solid` 패키지
- **리뷰 범위**: 전체 소스 코드 및 테스트 코드
- **제외 사항**: 없음
- **리뷰 일자**: 2026-02-03
- **리뷰 버전**: v4 (3차 재리뷰 반영, 테스트 실행 검증 포함)

## 리뷰 결과 요약

| 관점 | 결과 |
|------|------|
| 코딩지침 | ✅ ESLint 0 에러, TypeCheck 0 에러, Chrome 84+ 호환 |
| 성능 | ✅ 양호 |
| 안정성 | ⚠️ 개선 필요 (Dropdown 스크롤/위치 처리) |
| 유지보수성 | 🔴 테스트 실패 1건, 테스트 커버리지 개선 필요 |
| 가독성 | ✅ 양호 |
| 사용성 | ⚠️ 개선 필요 (Dropdown UX, SidebarMenu 반응성) |

## 테스트 실행 결과

```
Test Files  1 failed | 9 passed (10)
Tests       1 failed | 148 passed (149)
```

**실패 테스트**: `Dropdown.spec.tsx` - 애니메이션 클래스 검증 불일치

## 발견 사항 (총 9개)

### Critical (1개) - 즉시 수정 필요

#### 6. [Critical] Dropdown 테스트 애니메이션 클래스 검증 실패

- **위치**: `packages/solid/tests/components/overlay/Dropdown.spec.tsx:328`
- **관점**: 유지보수성
- **현재 상태**: 테스트가 `transition-all`을 검증하지만 실제 코드는 `transition-[opacity,transform]` 사용
- **결정 사항**: ✅ 테스트 수정 (Critical 상향)
- **테스트 실행 결과**:
  ```
  Expected: true
  Received: false

  expect(dropdown.classList.contains("transition-all")).toBe(true)
  ```
- **영향**: CI/CD 파이프라인 실패 가능, 개발 워크플로 방해
- **수정 방향**:
  ```typescript
  // 변경 전
  expect(dropdown.classList.contains("transition-all")).toBe(true);

  // 변경 후
  expect(dropdown.classList.contains("transition-[opacity,transform]")).toBe(true);
  ```

### 코드 수정 필요 - Minor (3개)

#### 1. [Minor] SidebarMenu pathname 반응성 처리

- **위치**: `packages/solid/src/components/navigation/SidebarMenu.tsx:99-104`
- **관점**: 사용성
- **현재 상태**: 마운트 시점에만 펼침 상태 계산
- **결정 사항**: ✅ 반응적 업데이트로 변경
- **상세 설명**:
  - 현재 `findSelectedPath` 함수가 마운트 시점에만 실행됨
  - pathname 변경 시 펼침 상태가 업데이트되지 않음
  - 딥링크로 직접 접근 시 부모 메뉴가 펼쳐지지 않는 문제
- **수정 방향**:
  - `initialOpenItems` 계산을 `createMemo`로 감싸서 반응적으로 변경
  - `MenuItem`의 `onMount` 로직을 `createEffect`로 변경하여 pathname 변경에 반응

#### 2. [Minor] Dropdown 팝업 내부 스크롤 시 닫힘 문제

- **위치**: `packages/solid/src/components/overlay/Dropdown.tsx:257-268`
- **관점**: 사용성, 안정성
- **현재 상태**: 모든 스크롤 이벤트에서 팝업 닫힘
- **결정 사항**: ✅ 팝업 내부 스크롤 제외 처리
- **상세 설명**:
  - `capture: true`로 모든 스크롤 이벤트 감지
  - 팝업 내부에 긴 목록이 있을 때 스크롤하면 닫혀버림
  - 팝업의 `overflow-y-auto`와 `maxHeight` 속성이 무의미해짐
- **수정 방향**:
  ```typescript
  const handleScroll = (e: Event) => {
    const popup = popupRef();
    if (popup?.contains(e.target as Node)) return;
    setOpen(false);
  };
  ```

#### 3. [Minor] Dropdown position 혼용 문제

- **위치**: `packages/solid/src/components/overlay/Dropdown.tsx:146-159`
- **관점**: 안정성
- **현재 상태**: CSS에서 `fixed`, 스타일 계산에서 `absolute` + `scrollY` 혼용
- **결정 사항**: ✅ `fixed`로 통일하고 `scrollY` 제거
- **상세 설명**:
  - Portal로 document.body에 렌더링되므로 `fixed`가 적절
  - 현재 코드에서 `style.position = "absolute"` 설정이 `fixed` 클래스를 덮어씀
  - `scrollY`는 `fixed` 포지셔닝에서 불필요
- **수정 방향**:
  - `style.position = "absolute"` 라인 제거 (기본 `fixed` 유지)
  - `+ window.scrollY` 제거
  - `bottom` 계산 시 `viewportHeight - rect.top` 사용 (scrollY 없이)

### 테스트 추가 - Suggestion (5개)

#### 4. [Suggestion] SidebarMenu 테스트 파일 부재

- **위치**: `packages/solid/tests/components/navigation/` (누락)
- **관점**: 유지보수성
- **결정 사항**: ✅ 테스트 파일 추가
- **테스트 케이스**:
  - 메뉴 아이템 렌더링
  - 중첩 메뉴 렌더링
  - pathname에 따른 선택 상태
  - pathname에 따른 부모 메뉴 자동 펼침
  - 외부 링크 새 탭 열기
  - 내부 링크 SPA 라우팅

#### 5. [Suggestion] SidebarUser 테스트 파일 부재

- **위치**: `packages/solid/tests/components/navigation/` (누락)
- **관점**: 유지보수성
- **결정 사항**: ✅ 테스트 파일 추가
- **테스트 케이스**:
  - 기본 렌더링 (children 표시)
  - menus 미제공 시 클릭 비활성화
  - menus 제공 시 클릭으로 드롭다운 토글
  - 메뉴 아이템 클릭 시 onClick 호출 및 드롭다운 닫힘
  - aria-expanded 속성 동작

#### 7. [Suggestion] Collapse 테스트 파일 위치 일관성

- **위치**: `packages/solid/tests/Collapse.spec.tsx`
- **관점**: 유지보수성
- **결정 사항**: ✅ 테스트 파일 이동
- **수정 방향**:
  - `packages/solid/tests/components/disclosure/` 디렉토리 생성
  - `Collapse.spec.tsx` 파일 이동
  - import 경로 수정 (`../src` → `../../../src`)

#### 8. [Suggestion] useRouterLink 훅 테스트 파일 부재

- **위치**: `packages/solid/tests/hooks/` (누락)
- **관점**: 유지보수성
- **결정 사항**: ✅ 테스트 파일 추가
- **테스트 케이스**:
  - 일반 클릭: SPA 라우팅 (useNavigate 호출)
  - Ctrl/Cmd + 클릭: 새 탭 열기 (window.open 호출)
  - Shift + 클릭: 새 창 열기 (window.open 호출, 크기 옵션)
  - state 전달 기능
  - e.preventDefault(), e.stopPropagation() 호출 확인

#### 9. [Suggestion] mergeStyles 유틸리티 테스트 파일 부재

- **위치**: `packages/solid/tests/utils/` (누락)
- **관점**: 유지보수성
- **결정 사항**: ✅ 테스트 파일 추가
- **테스트 케이스**:
  - object 스타일 병합
  - string 스타일 파싱 및 병합
  - mixed 스타일 병합 (string + object)
  - kebab-case → camelCase 변환
  - undefined 값 무시
  - 빈 문자열 처리
  - 값 덮어쓰기 (나중 값 우선)

## 긍정적 사항

### 코딩지침 준수

- ✅ ESLint 0 에러, TypeScript 0 에러
- ✅ Chrome 84+ CSS 호환성 준수 (aspect-ratio, inset, :is(), :where() 미사용)
- ✅ SolidJS 패턴 올바르게 사용 (createSignal, createMemo, splitProps 등)
- ✅ Props 구조 분해 대신 `props.xxx` 접근 패턴 준수
- ✅ Compound Components 패턴 적절히 사용 (`ListItem.Children`)

### 접근성

- ✅ `role="tree"`, `role="treeitem"`, `role="group"` 적절히 적용
- ✅ `aria-expanded`, `aria-selected`, `aria-disabled`, `aria-level` 속성 사용
- ✅ 키보드 네비게이션 지원 (Arrow keys, Home, End, Space, Enter)
- ✅ Roving tabindex 패턴 구현
- ✅ `prefers-reduced-motion` 미디어 쿼리 지원 (ripple directive)

### 반응형 설계

- ✅ 데스크탑/모바일 breakpoint 처리 (`SM_MEDIA_QUERY`)
- ✅ 사이드바 toggle 시맨틱 명확 (데스크탑: 열림이 기본, 모바일: 닫힘이 기본)
- ✅ backdrop 및 자동 닫힘 처리

### 코드 품질

- ✅ 컴포넌트별 적절한 책임 분리
- ✅ Context API 활용한 상태 공유
- ✅ `createPropSignal` 훅으로 controlled/uncontrolled 패턴 일관성 있게 처리
- ✅ JSDoc 주석으로 컴포넌트 사용법 문서화
- ✅ document 이벤트 리스너 모두 onCleanup으로 적절히 해제 (메모리 누수 없음)

### 테스트 커버리지 (기존)

- ✅ Button, List, ListItem, Dropdown, Sidebar, SidebarContainer 테스트
- ✅ Collapse 테스트 (위치 이동 권장)
- ✅ ripple directive, createPropSignal hook 테스트
- ✅ ConfigContext, usePersisted 테스트

## 결론

solid 패키지는 전반적으로 코딩 지침을 잘 준수하고 있으며, 접근성과 반응형 설계가 잘 구현되어 있습니다.

### 발견 사항 요약

| 심각도 | 항목 수 | 내용 |
|--------|---------|------|
| **Critical** | 1개 | Dropdown 테스트 실패 (CI/CD 영향) |
| Minor | 3개 | SidebarMenu 반응성, Dropdown 스크롤, Dropdown position |
| Suggestion | 5개 | 테스트 추가/이동 |

### 즉시 수정 필요

**Dropdown 테스트 실패** (항목 #6)
- 현재 테스트가 실패하고 있어 개발 워크플로에 영향
- `transition-all` → `transition-[opacity,transform]` 수정 필요

## 수정 작업 우선순위

### 🔴 Critical (즉시)
1. **Dropdown 테스트 수정** - 테스트 실패 해결

### 🟠 높음 (사용성/안정성)
2. Dropdown 내부 스크롤 처리
3. Dropdown position 통일

### 🟡 중간 (UX)
4. SidebarMenu pathname 반응성

### 🟢 낮음 (테스트 보강)
5-9. 테스트 파일 추가/이동
