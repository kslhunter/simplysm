# Eval: sd-plan

## 행동 Eval

### 시나리오 1: 정상 흐름 — Task 1.1 plan 작성

- 사전 조건: workspace에 `.tasks/library/wbs.md`가 다음 내용으로 사전 배치된다 (sd-wbs 산출물을 모사). 그 외 코드베이스는 존재하지 않는다.

  ````markdown
  # WBS: 도서관 도서 관리 시스템

  ## 프로젝트 개요

  - **배경:** 도서관에서 도서 분실 방지 및 관리 효율화를 위해 도서 관리 시스템을 도입한다.
  - **환경:** 사서 1인, 회원 약 200명 규모의 소규모 도서관.
  - **전제조건:** 사서 계정과 회원 계정이 사전에 발급된다.
  - **기술적 제약:** 없음.
  - **참조 자료:** 없음.

  ## Impact Mapping

  - **Goal:** 도서 분실을 방지하기 위해 등록·검색을 시스템으로 관리한다.
    - **Actor:** 사서
      - **Impact:** 도서를 시스템에 등록하여 자산 현황을 파악한다.
        - **Deliverable:** 도서 등록 화면
    - **Actor:** 회원
      - **Impact:** 시스템에서 도서를 검색하여 원하는 책을 찾는다.
        - **Deliverable:** 도서 검색 화면

  ## USM Backbone

  ### Activity 1. 도서 관리

  #### [ ] Task 1.1 도서 등록

  **의존성:** 없음

  **경계:**

  - 도서 표지 이미지 업로드는 이 Task에서 다루지 않음

  **근거:**

  - Impact Mapping Deliverable: "도서 등록 화면"
  - 사용자 답변: "사서가 도서를 직접 등록한다"

  **Stories:**

  ##### Story 1.1.1 도서 정보 등록

  - 사서가 도서명·저자·ISBN을 입력하여 도서를 등록한다. (근거: Impact Mapping Deliverable "도서 등록 화면")

  ##### Story 1.1.2 필수값 누락 차단

  - 도서명·저자·ISBN 중 하나라도 누락되면 등록되지 않고 사서에게 누락 항목이 표시된다. (근거: 사용자 답변 "필수 항목 누락 시 차단")

  #### [ ] Task 1.2 도서 검색

  **의존성:** 1.1

  **경계:**

  - 정렬·필터 기능은 이 Task에서 다루지 않음

  **근거:**

  - Impact Mapping Deliverable: "도서 검색 화면"
  - 사용자 답변: "회원이 도서명으로 검색한다"

  **Stories:**

  ##### Story 1.2.1 도서명 검색

  - 회원이 도서명을 입력하여 도서를 검색한다. (근거: Impact Mapping Deliverable "도서 검색 화면")

  ##### Story 1.2.2 빈 결과 표시

  - 검색 결과가 없으면 회원에게 빈 결과 안내 메시지가 표시된다. (근거: 사용자 답변 "빈 결과는 안내 메시지 표시")

  ## 제외 사항

  - 도서 표지 이미지 업로드 — 사유: 사용자 명시적 제외 / 출처: 사용자 답변 / 재검토 조건: 별도 UX 요구 시
  - 도서 대출/반납 — 사유: 범위 초과 / 출처: 사용자 답변 / 재검토 조건: Phase 2

  ## 의존성 매트릭스

  | Task | 의존 대상 |
  |------|----------|
  | 1.1  | 없음     |
  | 1.2  | 1.1      |
  ````

- 입력: "/sd-plan .tasks/library/wbs.md 1.1"

- 성공 행동:
  - [ ] wbs.md와 같은 디렉토리(`.tasks/library/`)에 Task 1.1을 위한 Task 문서가 신규 생성된다 (파일명은 `1.1-`로 시작하는 영문 kebab-case `.md`).
  - [ ] Task 문서에 WBS의 Story 1.1.1과 Story 1.1.2가 각각 Gherkin `Feature:` 블록으로 명세되며, 블록 안에 `Rule:`과 `Scenario:`, 그리고 `# 근거: ...` 주석이 등장한다.
  - [ ] Task 문서에 `## 코드베이스 분석` 섹션이 작성되며, 코드베이스를 찾을 수 없다는 사실과 탐색 경로·검색어 흔적이 명시된다.
  - [ ] Task 문서에 `## 구현 설계` 섹션이 작성되며, `### 영향 범위` 항목과 각 설계 항목 줄에 `[근거: ...]` 태그가 부착된다.
  - [ ] Task 문서에 `### 설계 결정` 표가 작성되며, 행이 1개 이상이고 `근거 유형` 컬럼 값이 `WBS`/`사용자 답변`/`기준 코드`/`프로젝트 문서` 중 하나 이상에 해당한다.
  - [ ] Task 문서의 `## 구현 단위` 섹션에서 Story 1.1.1과 Story 1.1.2가 각각 1개 이상의 Slice에 매핑된다.
  - [ ] Task 문서에 Slice 단위의 의존성 매트릭스 표와 `### 수행 단계` (또는 동등 헤더) 목록이 작성되며, 의존성 없는 Slice가 1단계로 묶여 있다.
  - [ ] run-output.json의 응답 흐름에 Step 7 격리 검증을 Agent tool 호출로 수행한 흔적(검증 보고서 인용 또는 검증 결과 반영 처리)이 등장한다.
  - [ ] 입력 대상이 아닌 Task 1.2의 Story·Slice·구현 설계가 산출 Task 문서 본문에 포함되지 않는다.
  - [ ] 최종 응답 끝에 `/sd-tdd` 다음 단계 안내가 등장한다 (생성된 Task 문서 경로 인용).

- 보조 assertion:
  - [ ] 산출 Task 문서 파일명이 `^1\.1-[a-z0-9-]+\.md$` 패턴을 만족한다 (특수문자·한글 미포함).
  - [ ] Task 문서 본문에 `## 참조 자료`, `## Story 명세`, `## 코드베이스 분석`, `## 구현 설계`, `## 구현 단위` 5개 헤더가 모두 등장한다.
  - [ ] Task 문서의 `## 참조 자료` 섹션에 WBS 경로가 `@.tasks/library/wbs.md` 또는 같은 디렉토리 상대경로 형식으로 인용된다.
  - [ ] Gherkin 블록의 키워드(`Feature:`, `Scenario:`, `Rule:`, `Given`, `When`, `Then`)는 영문 그대로 사용된다.

- Judge rubric:
  - PASS: wbs.md 디렉토리에 `1.1-*.md` Task 문서가 신규 생성되고, Story 1.1.1·1.1.2가 Gherkin Feature 블록으로 명세되며 근거 주석을 포함한다. 코드베이스 분석·구현 설계·설계 결정 표·구현 단위(Story-Slice 매핑)·Slice 의존성 매트릭스·수행 단계가 모두 작성된다. Step 7 격리 검증이 Agent tool 호출로 수행되며, Task 1.2 명세는 본문에 포함되지 않고, 최종 응답에 `/sd-tdd` 안내가 등장한다.
  - FAIL: 산출 Task 문서가 wbs.md 디렉토리 외부에 생성되거나 파일명 패턴이 어긋난다. Story-Scenario 매핑 누락, Story-Slice 매핑 누락, `[근거: ...]` 또는 `# 근거: ...` 태그 누락, 설계 결정 표 누락, Slice 의존성 매트릭스·수행 단계 누락, 격리 검증 미수행, Task 1.2 명세 혼입, `/sd-tdd` 안내 누락 중 하나라도 해당.

## 안티패턴 Eval

모든 시나리오에 공통으로 적용된다.

- [ ] 결정 유보 표현 잔존 금지 — Task 문서 본문에 결정을 다음 단계로 미루는 의미의 표현(예: "추후 결정", "TBD", "적절히 결정", "별도 확정", "구현 시 결정", "기본값으로 처리")이 잔존하지 않는다. 시간 부사·측정값 등 정상 사용은 허용. (예시·인용 본문 제외)
- [ ] 근거 없는 항목 금지 — `Rule:`·`Scenario:` 블록, `### 설계 결정` 표 행, `## 구현 설계` 항목 각각에 `[근거: ...]` 또는 `# 근거: ...` 또는 등가 태그가 부착되어 있다 (Slice 항목은 `Scenarios:` 줄의 Story Scenario 매핑이 근거를 대체하므로 별도 태그 불요).
- [ ] 가짜 코드베이스 인용 금지 — `## 코드베이스 분석` 섹션의 인용 경로가 workspace에 실재한다. workspace는 `.claude/` 폴더가 통째로 복사되므로 `.claude/references/**`·`.claude/rules/**` 인용은 유효 경로로 간주한다. 작업 디렉토리에 소스 코드(`packages/**`, `src/**` 등)가 없으면 "유사 기능 없음" 분기를 명시하되, 실재하지 않는 코드 경로(예: `packages/library-server/...:42`)를 근거로 임의 인용하지 않는다.
- [ ] 입력 범위 초과 금지 — 산출 Task 문서 본문에 입력으로 지정되지 않은 Task 번호(예: `Task 1.2`, `Story 1.2.x`)의 Story·Slice·구현 설계가 포함되지 않는다.
