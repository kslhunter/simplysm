---
description: git add . 후 커밋 생성
---

## Step 1: Stage all changes

Run `git add .` to stage all changes.

## Step 2: Create commit

Now use the `commit-commands:commit` skill to create a commit with the staged changes.
