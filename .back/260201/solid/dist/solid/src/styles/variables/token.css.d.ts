export declare const tokenVars: {
    font: {
        family: {
            sans: `var(--${string})`;
            mono: `var(--${string})`;
        };
        size: {
            sm: `var(--${string})`;
            base: `var(--${string})`;
            lg: `var(--${string})`;
            h6: `var(--${string})`;
            h5: `var(--${string})`;
            h4: `var(--${string})`;
            h3: `var(--${string})`;
            h2: `var(--${string})`;
            h1: `var(--${string})`;
        };
        lineHeight: {
            tight: `var(--${string})`;
            normal: `var(--${string})`;
            relaxed: `var(--${string})`;
        };
    };
    spacing: {
        auto: `var(--${string})`;
        none: `var(--${string})`;
        xxs: `var(--${string})`;
        xs: `var(--${string})`;
        sm: `var(--${string})`;
        base: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        xxl: `var(--${string})`;
        xxxl: `var(--${string})`;
        xxxxl: `var(--${string})`;
    };
    radius: {
        none: `var(--${string})`;
        xs: `var(--${string})`;
        sm: `var(--${string})`;
        base: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        xxl: `var(--${string})`;
        full: `var(--${string})`;
    };
    shadow: {
        sm: `var(--${string})`;
        base: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
    };
    duration: {
        base: `var(--${string})`;
        slow: `var(--${string})`;
    };
    overlay: {
        muted: `var(--${string})`;
        base: `var(--${string})`;
        light: `var(--${string})`;
    };
    zIndex: {
        base: `var(--${string})`;
        backdrop: `var(--${string})`;
        dropdown: `var(--${string})`;
    };
};
//# sourceMappingURL=token.css.d.ts.map