export declare const lightTheme: string, themeVars: {
    control: {
        primary: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
        secondary: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
        success: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
        warning: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
        danger: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
        info: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
        gray: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
        slate: {
            base: `var(--${string})`;
            hover: `var(--${string})`;
            active: `var(--${string})`;
            muted: `var(--${string})`;
        };
    };
    surface: {
        base: `var(--${string})`;
        elevated: `var(--${string})`;
        muted: `var(--${string})`;
        inverted: `var(--${string})`;
    };
    text: {
        base: `var(--${string})`;
        muted: `var(--${string})`;
        inverted: `var(--${string})`;
    };
    border: {
        base: `var(--${string})`;
        muted: `var(--${string})`;
    };
};
export declare const darkTheme: string;
//# sourceMappingURL=theme.css.d.ts.map