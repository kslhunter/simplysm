import {SdStyleProvider} from "../../provider/SdStyleProvider";

export const stylesSdDropdown = (vars: SdStyleProvider) => /* language=LESS */ `
  sd-dropdown {
    > div {
      position: relative;
    }
  }`;