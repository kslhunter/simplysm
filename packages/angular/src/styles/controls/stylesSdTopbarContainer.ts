import {SdStyleProvider} from "../../provider/SdStyleProvider";

export const stylesSdTopbarContainer = (vars: SdStyleProvider) => /* language=LESS */ `
  sd-topbar-container {
    display: block;
    position: relative;
    width: 100%;
    height: 100%;
    padding-top: 36px;
  }`;