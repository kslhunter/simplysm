import {SdStyleProvider} from "../../provider/SdStyleProvider";

export const stylesSdList = (vars: SdStyleProvider) => /* language=LESS */ `
  sd-list {
    display: block;
    user-select: none;
  }`;