import {publish} from "./lib/publish";

publish("core");
publish("excel");
publish("mail");
publish("storage");
publish("pack");
publish("database");
publish("socket-common");
publish("socket-server");
publish("socket-client");
publish("angular");
